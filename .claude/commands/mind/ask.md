Consulta o mind para responder uma pergunta.

Executa `node .systems/memory/scripts/kb/query.js "$ARGUMENTS"` a partir da raiz do workspace.

O argumento é a pergunta do usuário. Se nenhum argumento for fornecido, peça ao usuário o que ele quer saber.

Mostre a resposta diretamente ao usuário. Não salva nada — é uma consulta efêmera.
