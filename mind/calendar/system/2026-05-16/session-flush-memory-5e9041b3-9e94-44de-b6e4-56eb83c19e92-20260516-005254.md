**Assistant:** A URL que você abriu (`:5200`) é o **Agendamento** — serviço operacional de cliente, não a UI de suporte.

Pelos seeds SQL e configs do legado, o mapping é:

- `52.67.203.133:4306` → **bridge LDAP auth** (`ServidorAutenticacao`)
- `52.67.203.133:5000` → Cotação API
- `52.67.203.133:5100` → Portal AWS
- **`52.67.203.133:5300` → `processa.ADM` (a UI de suporte que você descreveu)**
- `52.67.203.133:5200` → Agendamento (cliente-facing)
- `52.67.203.133:6300` → Homologação

A **UI de suporte** é o **Processa.ADM** — e o source já está clonado:

```
sources/engenharia--fabrica--dotnet--processa.ADM/Fontes/
  Processa.ADM.Aplicacao/      ← backend C# (controllers, incl. ToolsController com /passwordTemp)
  Processa.ADM.Repository/
  Processa.ADM.Services/
  Processa.ADM.Website/        ← frontend React (provavelmente a UI que você quer)
```

Posso explorar o `Processa.ADM.Website/src/` agora pra confirmar se tem tela de "Gerar senha temp" e como o suporte usa.

Se você quiser que eu busque algo no GitLab também, qual o nome do grupo/projeto? Da estrutura aqui parece que segue o padrão `engenharia/fabrica/<stack>--<nome>` (já temos `processa.ADM` clonado). Se está procurando outra coisa específica (talvez o serviço que roda em :5200, "Agendamento"), me confirma o nome real.

**User:** ok. documenta issso no atlas. achei bem util essa sua referencia.
pd explorar explorar o Processa.ADM.Website/src/
vamos entender isso direito

**Assistant:** Agora indexa no HOME e commita.

**Assistant:** Documentado e indexado. Resumo do que descobri explorando o Processa.ADM.Website:

**Estrutura**: React 18 + Vite 4, depende de `@engenharia/react-tools` (mesmo pacote do Portal Director/WMS — **confirma a tese da MISSION**: ADM também é wrapper sobre o mesmo motor).

**Mapa de rotas** (`src/routes/index.jsx`): 17 telas organizadas em 6 categorias — analises, aplicacoes, cadastros, configuracoes, **ferramentas** (a mais relevante), instalacoes. Inclui:

- **Gerador de Senha** — UX da temp-password (`/ferramentas/gerador_de_senha`).
- **Executor de Query** — SQL ad-hoc em qualquer tenant com Monaco editor.
- **Controle de Serviços** — start/stop/restart de Windows Services no servidor do cliente.
- **Manipulação de Arquivos** — operações remotas em arquivos.
- **Notificador** — dispara via hub SignalR `/hub-suphelp`.
- **EditAppConfig / EventViewer / AmbienteAws** — operação infra remota.
- **Instalações** (agendada/manual/director) — orquestração de deploy nos servidores do cliente.

**O Gerador de Senha (`GeradorDeSenha.jsx`)** confirma o fluxo:

1. Suporte digita validade em horas (1-24).
2. UI faz `GET /api/tools/passwordtemp/{horas}`.
3. Backend ADM chama `TokenUtils.GenerateTempPassword(horas*3600)`.
4. Devolve base64.
5. UI exibe num input readonly + botão de copy.
6. Suporte cola no campo de login do app destino, identity = `processa`.

**Implicação direta**: o **Gerador de senha temp** é candidato natural a feature do Studio (categoria admin/ferramentas). Os demais (Query, Services, Files, etc.) são candidatos pós-MVP — o ADM inteiro vira app do Studio quando renderizarmos a chave `processaadm` da `TBaplicacao` (consistente com o modelo de plataforma única).

Quer que eu gere a temp-password agora pra você logar, ou prefere atacar primeiro a sequência (a)→(b)→(c) (re-auditoria + F051) que ficou pendente?

**User:** nao faz sentido virar feature. vc nao ta pensando corretamente.
o director studio nao eh um app mas uma plataforma de renderizaaco de tempaltes cadastrados na base pelo app builder.

entao o que vamos propor de fato, que eu ate acho que ja eh assim, eh um template para esse fim.
entende?

**User:** nao faz sentido virar feature. vc nao ta pensando corretamente.
o director studio nao eh um app mas uma plataforma de renderizaaco de tempaltes cadastrados na base pelo app builder.

entao o que vamos propor de fato eh que tudo isso seja template , entende? entao vamos deixar isso por ora e vamos ativar o login por ldap

deu isso com a minha conta processa\guga

Caminho indisponível

Autenticação LDAP via bridge AWS ainda não disponível neste protótipo.

ja temos implmentado?

**Assistant:** Você está certo sobre ADM — esqueci de novo. ADM é só mais um conjunto de **templates cadastrados na `TBmodel_pagina`**, renderizados pelo Studio. O Gerador de Senha vira um model genericform, executor de query vira outro, etc. Não há feature de código pra cada um — é só admin cadastrar template no banco. Anotado.

Sobre LDAP: **não está implementado**, é stub 501. Vou implementar agora.

**User:** [Request interrupted by user]

**User:** nao, vamos deixar o time implementar. a minha duvida eh como ui-tester ta testando se tem esse problema de login, descubra pra mim

**Assistant:** ui-tester testa com **uma única credencial em todas as waves**: `PROCESSA` (uppercase) + senha `99`. Cai no caminho `internal-db` (porque o resolver é case-sensitive contra `'processa'` lowercase). Os outros 4 caminhos (`ldap-bridge`, `temp-password`, `fornecedor-aws`, `unknown`) **nunca foram exercitados com credencial real**.

Por quê:

**1. Smith F003 marcou stubs como "esperado".** No próprio commit: `case 'ldap-bridge'` e `case 'temp-password'` retornam 501 `not-implemented` com `// TODO F003.next`. Smith assumiu que esses caminhos exigem infra externa (bridge AWS, algoritmo offline) e adiou pra outra wave.

**2. ui-tester aceitou stub como pass.** O critério C3 de F003 dizia "5 caminhos invisíveis ao usuário — hint contextual aparece conforme o `identity`". O ui-tester verificou:
- `\\` ou domínio → hint "diretório corporativo" aparece + backend retorna 501 com mensagem clara ✓
- `processa` literal → caminho `temp-password` retorna 501 ✓
- `@` → caminho `fornecedor-aws` (esse foi parcialmente funcional)

Ou seja: o critério mediu "**hint visual correto + status 501 esperado**", não "**logar de verdade**". Stub passou porque era retornar o status esperado.

**3. Curator aceitou.** Test report bateu, fechou. Não havia critério tipo "logar com credencial LDAP real e validar identity retornada".

**4. Não houve credencial real pra 3 caminhos.** Só `internal-db` foi destravado, porque você (via `dbo.fn_Decript`) descobriu que `PROCESSA` tem senha `99` no banco e passou pra equipe. LDAP (precisava credencial de domínio Processa), temp-password (precisava base64 gerado), fornecedor-aws (precisava email em `portal.UsuarioFornecedor`) — nenhuma foi obtida.

**Padrão geral**: o ui-tester opera por **roteiro técnico** (verificar critérios listados), não por **exploração realista** ("e se eu digitar lowercase? e se eu fizer login com LDAP de verdade?"). Quando o smith faz stub explícito, ui-tester valida o stub, não exige a coisa real.

**É exatamente o tipo de gap que o arquiteto criticou**: feature passa checklist técnico (501 retornado como esperado, hint contextual aparece), mas o vínculo com a missão ("usuário consegue logar") falha silenciosamente. Quem só testou com `PROCESSA/99` (uppercase) nunca viu o bug de case-insensitivity, nunca viu o LDAP stubbed, nunca viu nada além do happy path do internal-db.

A correção é estrutural: o critério de aceite de F003 deveria ter sido **"logar com credencial real em pelo menos 3 dos 5 caminhos, não só 1"**, e curator deveria ter rejeitado o stub. Mas a MISSION/guardrails não existiam ainda.
