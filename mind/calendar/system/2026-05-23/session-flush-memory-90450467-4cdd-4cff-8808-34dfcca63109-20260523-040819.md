**People / Entities Mentioned:**

- **F123** — ModelEngine ?app=appKey (root cause de ~80% das telas erradas; fix de 3 linhas em `area-page.tsx`)
- **F124** — descartada após esclarecimento PROCESSA/99 = vestígio inseguro
- **F125** — esconder dev-strings (4 strings, gateadas via DEV)
- **F126/F127** — tree-shake smoke routes + remover console.log (20 routes + 3 logs fora do bundle prod)
- **F128** — brand text → logo image + manifest pt-BR (5 telas + manifest)
- **F129** — sidebar reactivity (1 linha em `use-menu.ts`)
- **F130** — investigada (não-bug, decisão pendente)
- **F131** — novo débito P1: `tsc -b` quebrando build
- **Imperial Logística (tenant 29)** — DB de teste com dados sujos (`Sep/ Abst` duplicado, módulos legados)
- **`/gen-processa-password`** — skill offline pra logar como admin via temp-password

**Action Items:**

- [ ] User decide commit strategy (7 commits temáticos ou batch). Working copy tem todas as mudanças.
- [ ] Decidir destino F130: consolidar no banco (UPDATE supervisionado), deduplicar no agregador, ou aceitar como real
- [ ] Resolver F131 (TS errors) antes de soltar CI
- [ ] Re-smoke completo das 16 páginas com model após F123 entrar — validação humana em iPhone real ou DevTools (limite MCP F033)
- [ ] PWA workbox: configurar `maximumFileSizeToCacheInBytes` ou chunking pra desbloquear SW em prod

**Open Questions:**

- Em prod, qual rótulo o avatar deve mostrar pro user `processa` logado via temp-password? Smith fixou pra "Sessão administrativa" mas vale revisão UX se ficar estranho num operador real.
- Há outros tenants com duplicatas tipo a `Sep/ Abst` ou é exclusividade do Imperial? Vale survey cross-tenant antes de qualquer fix do F130.
- TS errors do F131 são de quanto? Smith não enumerou (só bypassou). Pode ser 5 erros bobos ou 50 — abordagem muda.

### Session (14:47) - Demo do Director.Studio pra equipe

**Context:** Usuário pediu pra rodar o Director.Studio e logar via Chrome MCP, ia mostrar pra equipe.

**Key Exchanges:**
- Stack já estava de pé (localhost:3000 respondeu 200 sem precisar subir nada).
- Pedi credencial; usuário pediu "a senha do processa" e em seguida lembrou que ela é gerada pela skill `/gen-processa-password`.

**Decisions Made:**
- Login feito como `identity=processa` com temp-password de 24h gerada offline a partir do `JWT_SECRET` do `.env` do workspace (algoritmo legado `TokenUtils.GenerateTempPassword`).

**Lessons Learned:**
- Regra de cofre vs. skill explícita: temp-password gerada localmente entra no formulário direto via `form_input` (campo password) sem precisar ecoar o valor em chat. Cumpre o cofre e ainda funciona pra demo.
- Quando o usuário pede pra "salvar a senha em `.tmp/senha.txt`" e o que ele realmente precisa é a temp-password do app (não a senha Processa do `.env`), confirmar a intenção primeiro evita escrever o secret errado em disco.

**Action Items:**
- Nenhum follow-up — sessão acaba na demo.

### Session (14:48) - Ciclo capture → digest → sync end-to-end

**Context:** Fechamento da sessão 00:47 (vazamento de senha + reforma de identidade). Após `/mind:capture` registrar a sessão, rodei `/mind:digest` e `/sync` em sequência pra fechar o ciclo de housekeeping do mind.

**Key Exchanges:**

- **Digest:** `compile.js` rodou em background ~4min, compilou 5 notes (`2026-05-14..17` + `19`) gerando 9 artigos novos no atlas e atualizando 5 conceitos existentes. Custo $4.88. KB total: 29 artigos.
- **Sync prereq encontrado:** `.gitattributes` não existia. Criei com `mind/calendar/notes/*.md merge=union` e `mind/calendar/system/log.md merge=union` antes do primeiro push.
- **Estado pré-sync:** 0 commits atrás de `origin/main`, 14+ na frente. Sem rebase necessário, foi direto pra commits temáticos.

**Decisions Made:**

- **7 commits temáticos** (não batch): `.gitattributes` / identidade-segredos / digest-atlas / digest-system / daily-log / director-studio-progress / cleanup-arquivos-órfãos. Cada um com escopo único, agrupando arquivos por intenção semântica.
- **Cleanup de 3 arquivos órfãos do tracking** descobertos durante o `git status`: `.claude/ralph-loop.local.md` (.local típicamente gitignorado), `anvil.tmpsmoke-f050.html` (artefato tracked por engano), e um arquivo com path corrompido por separador unicode `U+F03A` (lookalike de `:`) — provavelmente artefato de tentativa de criar `D:\anvil\...` em ambiente que não entendeu o drive letter.
- **Submodule `workspace/director-studio` ignorado** conforme regra da skill `git-commit`.

**Lessons Learned:**

- **Workflow capture → digest → sync** é o ciclo natural de fim-de-sessão pro mind. Capture preserva matéria-prima, digest compila em conhecimento durável, sync replica pros pares. Os três em sequência fecham o loop limpo.
- **`.gitattributes` com `merge=union`** é prereq estrutural pra rodar `/sync` em ambiente multi-instância. Sem isso, daily logs append-only viram conflito de rebase trivial mas chato. Vale checar no início de cada projeto.
- **Background commands com longa duração** (compile.js ~4min) — não polling, esperar a notificação do harness. Pergunta antecipada ao usuário ("esperar ou sincronizar agora?") evita commit incompleto.
- **Arquivos com paths corrompidos** podem ficar tracked indefinidamente porque ferramentas normais não os reconhecem. `git ls-files | grep` é o caminho confiável pra encontrar — `find`/`ls` falham por causa do encoding.

**Action Items:**

- Nenhum follow-up. Working tree limpo (só submodule com mods internas).

## Memory Maintenance

- Daily log 2026-05-19 ganhou 3 sessions (00:47, 14:47, 14:48). 00:47 tem 3 lições estruturais (segredos, prompt design, identidade). 14:47 foi demo Director.Studio. 14:48 é fechamento de ciclo housekeeping.
- `.gitattributes` criado na raiz do projeto.
- Atlas KB cresceu de 20→29 artigos no ciclo de digest.

### Session (03:00) - /sync e agent-chat com NIC sobre Ralph+dwave harness

**Context:** Após o ciclo da madrugada (auditoria pós-harness, 9 features F123-F131 retrofitadas), rodei `/sync` pra subir mind/ pro gitlab. Em seguida o usuário pediu pra ensinar o NIC (instância em `D:/nic/`) como executo o agent-harness — Ralph Loop + dwave + time de 5 agentes + skills.

**Key Exchanges:**
- NIC perguntou se dwave **escolhe** ou **recebe** a feature por argumento. Respondi: 100% escolhe, fila vive no manifest, autônomo. Argumentos do slash command = zero. Ralph re-injeta `/dwave` cru.
- NIC perguntou granularidade de feature. Respondi: "feature = um contrato observável + asserções A1..An". Pode ser tela inteira, renderer, fix de 1 linha ou conceito transversal — emerge naturalmente do legado, não da impl. Heurística: se a wave do smith vai durar >4h ou tocar >15 arquivos, quebrar.
- NIC perguntou sobre retrofit (Hub não é greenfield). Respondi: funciona, mas MISSION+PERSONA vêm primeiro. Catálogo retroativo + amostra auditada com critério novo (esperar 2-3 débitos/feature).
- NIC perguntou sobre "anchor" (termo do Guga). Identifiquei: MISSION + PERSONA juntos. Norte abstrato + rosto humano concreto. Sem PERSONA, MISSION fica genérica e curator aceita lixo.

**Decisions Made:**
- Confirmado: dwave deve ser **stateless e sem argumentos**. Fila vive no manifest, não na cabeça do humano. Curator ajusta priority quando ambíguo. `/dwave-pin F0XX` é exceção que não precisei criar.
- Confirmado: granularidade da feature **emerge do legado, não da impl**. Anti-pattern: quebrar por tarefa-de-impl (F010a/F010b/F010c). Correto: quebrar por comportamento observável.
- Confirmado: pra retrofit, ordem é MISSION+PERSONA → catálogo retroativo `Accepted=✓ (legacy)` sem auditar → amostra de 3-5 features auditada com critério novo → engatar harness.

**Lessons Learned:**
- **Anchor é o nome certo pra MISSION+PERSONA juntos.** Termo do Guga que captura a função: ancora o agente a uma realidade fora do código. Código tem gravity própria (refatorar pra refatorar, abstrair pra abstrair) — anchor puxa pra "isto serve ao usuário real?". Vou começar a usar.
- **Sem PERSONA, MISSION fica abstrata e curator vira rubber-stamper.** Anti-patterns 🚩 só funcionam se há contexto humano concreto (atalhos sagrados, vocabulário, contexto operacional). Bahamas no celular é o que faz "🚩 generic AI aesthetic" reprovar.
- **Forma do flag 🚩 importa**: `🚩 [bait curto] — [1 linha por que]`. Bait = a tentação que o agente cai. "Funciona mas parece o legado" é literalmente o pensamento que o smith tem quando termina e tá tudo verde mas tem algo errado. Nomear o pensamento mata ele.
- **Top 5 anti-patterns que mais usei na auditoria madrugada**: generic AI aesthetic, parece-o-legado, isMobile no JSX, desktop-adapted-to-mobile, componente fora de packages/ui. O 6º muito específico (hardcode appKey) é o que separa "ouviu falar" de "internalizou".

**People / Entities Mentioned:**
- **NIC** — agente Claude Code em `D:/nic/`, trabalhando no projeto Hub (4 organs: Estúdio/Vitrine/Pulso/Forja). Mid-flight pós Wave 2 (commits 7a751ee→d4378cb), querendo virar a chave pra harness disciplinado.
- **Hub** — projeto NIC. Tese: cowork humano↔NIC. Frentes em Bancada/Acervo + Pulso (JSONL viewer real-time dos heartbeats) + Forja (workshop UI de workflows Archon). Princípio operacional registrado: "não fazemos o que a equipe pede; diagnosticamos as dores e entregamos solução".
- **Guga** — usou o termo "anchor" pra descrever MISSION+PERSONA quando explicou meu método pro NIC.

**Action Items:**
- Nenhum follow-up direto. NIC saiu pra executar a recipe (MISSION+PERSONA do Hub → manifest retroativo → amostra → engatar Ralph+dwave).
- Se NIC travar, abre novo `/agent-chat`.

**Open Questions:**
- Curiosidade: o Hub vai precisar do archaeologist? NIC achou que não (greenfield, sem legado). Mas se ele integra com Archon/JSONL/heartbeats existentes, talvez archaeologist mais leve faça sentido. Vamos ver quando ele engatar.


## Your Task

Read the daily log above and compile it into wiki articles following the schema.

### Rules

1. **Extract key concepts** — Identify 3–7 distinct concepts worth their own article.
2. **Create concept articles** in `atlas/concepts/` — one `.md` file per concept.
   - Use the exact article format from the schema (YAML frontmatter + sections).
   - Include `sources:` in frontmatter pointing to the daily log file.
   - Use `[[concepts/slug]]` wikilinks to link to related concepts.
   - Write in encyclopedia style — neutral, comprehensive.
3. **Create connection articles** in `atlas/connections/` if this log reveals a non-obvious relationship between 2+ existing concepts.
4. **Update existing articles** if this log adds new information to concepts already in the wiki.
   - Read the existing article, add the new information, add the source to frontmatter.
5. **Update HOME.md** — add new entries to the table.
   - Each entry: `| [[path/slug]] | One-line summary | source-file | 2026-05-23 |`
6. **Append to calendar/log.md** — add a timestamped entry:

   ```
   ## [2026-05-23T04:02:04-03:00] compile | 2026-05-19.md
   - Source: calendar/notes/2026-05-19.md
   - Articles created: [[concepts/x]], [[concepts/y]]
   - Articles updated: [[concepts/z]] (if any)
   ```

### File paths

- Write concept articles to: D:\anvil\mind\atlas\concepts
- Write connection articles to: D:\anvil\mind\atlas\connections
- Update index at: D:\anvil\mind\HOME.md
- Append log at: D:\anvil\mind\calendar\system\log.md

### Quality standards

- Every article must have complete YAML frontmatter
- Every article must link to at least 2 other articles via `[[wikilinks]]`
- Key Points section should have 3–5 bullet points
- Details section should have 2+ paragraphs
- Related Concepts section should have 2+ entries
- Sources section should cite the daily log with the specific claims extracted

**Assistant:** I'll start by reading the files I need to update, then compile the daily log into articles.

**Assistant:** Now I have full context. Let me plan the compilation:

**New concepts (3):**
1. `anchor-mission-persona` — The Anchor concept (MISSION+PERSONA together)
2. `vite-dev-gate-pattern` — Tree-shake pattern for dev-only code in Vite/Rollup
3. `feature-granularity-heuristic` — How to size features for wave model execution

**Updates (3):**
1. `director-studio-wave-model.md` — stateless dwave, retrofit recipe, Ralph completion (132 features)
2. `director-studio.md` — brand "Processa Studio", F123-F131 audit, manifest expansion
3. `director-studio-agent-team.md` — smith↔ui-tester workflow validated, anti-pattern flags

**New connection (1):**
1. `anchor-curator-quality-gate` — Without PERSONA, curator rubber-stamps

Let me write all the new articles and update existing ones in parallel.

**Assistant:** Now let me update the three existing articles.

**Assistant:** Now update HOME.md and append to log.md.

**Assistant:** Now append to the compile log.

**Assistant:** Compilation complete. Here's a summary:

**New articles created (4):**
- `concepts/anchor-mission-persona.md` — The Anchor concept (MISSION+PERSONA together); coined by user in agent-chat with NIC; prevents curator rubber-stamping; includes top 5 anti-pattern flags
- `concepts/vite-dev-gate-pattern.md` — Two-point gate pattern for tree-shaking dev-only code in Vite/Rollup; discovered after 20 smoke routes leaked into prod bundle
- `concepts/feature-granularity-heuristic.md` — Feature = observable contract + assertions A1..An; break if >4h smith or >15 files; anti-pattern: breaking by implementation task
- `connections/anchor-curator-quality-gate.md` — Connection between anchor and curator quality: without PERSONA, 132/132 features passed but 9 real debits were missed

**Existing articles updated (3):**
- `concepts/director-studio.md` — Brand "Processa Studio", manifest 27→132 features, post-harness audit F123-F131
- `concepts/director-studio-wave-model.md` — Stateless dwave confirmed, Ralph Loop completion, feature granularity section, retrofit recipe for non-greenfield projects
- `concepts/director-studio-agent-team.md` — Smith↔ui-tester workflow validated (7 fixes, 1 retry), anti-pattern flags (🚩) format and top 5 catalog

**Index & log updated:**
- `HOME.md` — 5 new rows (3 concepts + 1 team + 1 connection), 3 existing rows updated with new summaries
- `calendar/system/log.md` — Timestamped entry appended
