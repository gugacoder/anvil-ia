**File:** 2026-05-13.md

# Daily Log: 2026-05-13

## Sessions

### Session (15:37) — Pipeline de digest e protocolo de credenciais Processa

**Pipeline de memória — entendimento corrigido**

Investiguei por que `/mind:digest` reportou "all daily logs are up to date" mesmo havendo arquivos em `mind/calendar/system/2026-05-12` e `2026-05-13`. Causa: `compile.js` lê exclusivamente de `NOTES_DIR = mind/calendar/notes/` (via `listRawFiles()` em `.systems/memory/scripts/kb/utils.js:124`). Os arquivos `session-flush-memory-*.md` e `log-memory.md` em `calendar/system/<data>/` são saída do `flush.js` (camada bruta de fim de sessão) e **não** são input do `compile.js`.

Caminho correto do pipeline:
1. Fim de sessão → `flush.js` grava session-flushes em `calendar/system/<data>/`
2. `/mind:capture` consolida a sessão em `calendar/notes/<data>.md` (daily log estruturado)
3. `/mind:digest` (compile.js) lê `calendar/notes/` e gera artigos em `atlas/concepts|connections|works/`

Commit `008c4c9` removeu os session-flushes antigos de 2026-05-12 e 2026-05-13 (eles ainda não tinham sido consolidados em daily notes; ficaram obsoletos).

**Protocolo de credenciais Processa — estabelecido**

Definimos protocolo para acesso ao share SMB `\\172.27.3.10\svn` (procedures do time de dev) e a quaisquer recursos com credencial de domínio Processa:

- A senha **nunca** é memorizada pelo agente.
- A senha vive em `.tmp/senha-processa.txt` (formato `user:` / `pass:`), fora do git, sob controle do usuário.
- Se o arquivo não existir, o agente cria o template e pede ao usuário para preencher.
- No momento do uso: ler o arquivo, aplicar a credencial (ex: `New-SmbMapping`), seguir o trabalho sem citar a senha no chat.

Protocolo validado ponta-a-ponta nesta sessão. Acesso ao share confirmado: raiz contém `Suporte/` e `trunk/` (mais `.svn/` interno do SVN). Procedures podem ser consultadas como referência; quando for trazer algo, vai para `sources/procedures/` (sem autorização para modificar o share).

Memória salva: `feedback_credenciais_processa.md` (+ `MEMORY.md` index criado).

## Memory Maintenance

- Criado `MEMORY.md` (índice de memórias do projeto).
- Criado `feedback_credenciais_processa.md` documentando o protocolo acima.

### Session — XmlToJsonNode e hipótese de bypass do Pipeliner

**Conhecimento filed**: criado [[xml-to-json-node]] em `atlas/concepts/` documentando a convenção da plataforma (atributos `Type`, `Array`, `Omitir`) que o Pipeliner usa pra converter XML→JSON. Registrado no HOME.

**Hipótese aberta levantada pelo usuário**: se os XMLs estiverem em pasta de rede acessível pelo **DBdirector**, importar **direto via procedure** — sem passar pelo conversor do Pipeliner.

Shape proposto:
- Procedure "varredora" lista XMLs da pasta.
- Para cada XML, chama uma segunda procedure que recebe o XML como parâmetro.
- Pipeliner vira só agendador: executa periodicamente uma procedure **sem parâmetros**.
- Ganho percebido: 100% do poder dentro da procedure (parse, validação, ingestão tudo em T-SQL).

Status: hipótese, não decidida. Não validei viabilidade no source.

**Instrução do usuário**: "inquira o analista quando estivermos conversando sobre esse assunto". Perguntas pra levantar quando o tema voltar:

- Como o DBdirector enxergaria a pasta de rede? UNC pelo service account do SQL Server? Linked server? CLR? `xp_cmdshell`?
- Quem deposita os XMLs ali — produtor externo, processo da Processa, scheduled task?
- O Pipeliner hoje faz algo além de converter+despachar que se perderia? (auth, retry, logging) — checar em `Pipeliner.Service/RequestService.cs`.
- Volume/tamanho dos XMLs — `OPENROWSET BULK` + tipo `xml` aguenta?
- Como listar arquivos: `xp_dirtree`? `sys.dm_os_enumerate_filesystem`? CLR?
- Idempotência: como marcar XML como "consumido"? Mover pra `processados/`, tabela de controle, lock de arquivo?
- XML inválido: pra onde vai? Pasta `erro/`, tabela DLQ?
- Substitui o Pipeliner pra esse fluxo, ou Pipeliner ainda dispara a procedure?

### Session (15:50) — Migração credenciais para .env e tarefas pendentes

**Migração `.tmp/senha-processa.txt` → `.env`**

Unificamos o esquema de credenciais. Agora:
- `.env.example` versionado na raiz com as chaves (rede + DBdirector).
- `.env` (gitignored) preenchido pelo usuário com os valores reais.
- Agente lê `.env` no momento do uso, nunca memoriza valores.

Chaves atuais: `PROCESSA_NET_USER`, `PROCESSA_NET_PASS`, `DIRECTOR_DB_HOST`, `DIRECTOR_DB_INSTANCE`, `DIRECTOR_DB_NAME`, `DIRECTOR_DB_USER`, `DIRECTOR_DB_PASS`, `DIRECTOR_DB_TRUST_CERT`.

Arquivos antigos `.tmp/password.txt` e `.tmp/senha-processa.txt` removidos. Acesso ao share `\\172.27.3.10\svn` validado via `.env`. DBdirector pendente — usuário vai preencher e em seguida testamos conexão.

Memória `feedback_credenciais_processa.md` atualizada com o novo protocolo.

**Tarefa pendente — investigação de procedures que leem arquivos da rede**

Antes de propor uma procedure nova pro fluxo de ingestão de XMLs (hipótese da sessão anterior — DBdirector lê XMLs direto da pasta de rede), preciso vasculhar o share `\\172.27.3.10\svn\trunk\` procurando procedures que já façam leitura de arquivo da rede. Objetivo: reusar técnicas já adotadas pelo time (ex: `xp_dirtree`, `OPENROWSET BULK`, CLR, linked server, `xp_cmdshell`) ao invés de introduzir um padrão novo.

Plano (a executar sob demanda, sem queimar tokens à toa): busca dirigida por padrões textuais no SVN — `OPENROWSET`, `xp_dirtree`, `xp_cmdshell`, `BULK INSERT`, `CLR`, `\\` em strings de procedures — listar procedures candidatas, ler só as relevantes.

**Decisão de design: opção em `TBopcoes`**

Vamos precisar de uma chave em `TBopcoes` (tabela de configuração do Director) para indicar a pasta raiz dos XMLs. Detalhe para confirmar com o analista: nome da chave, escopo (por cliente? global?), formato do valor (UNC absoluto? template com placeholder?).

**Perguntas adicionais pro analista (acumulam às da sessão anterior):**
- Quais técnicas de leitura de arquivo de rede já estão em uso pelo time em procedures do DBdirector?
- Existe convenção/chave em `TBopcoes` para pastas de integração? Padrão de nomenclatura?
- O service account do SQL Server tem permissão de leitura em pastas de rede hoje? Em quais?

### Session (16:53) — Conexão DBdirector, frente pipeliner-ingestao-xml e princípio "analista decide"

**Conexão SQL Server validada**

Conexão com DBdirector via `.env` funcionando:
- Server: `172.27.0.121\SQL2k19`
- DB: `DBdirector`
- Login: `sl`
- Versão: SQL Server 2019 Developer Edition (15.0.2130.3)

Connection string usada (PowerShell `System.Data.SqlClient`): `Server=...;Database=...;User Id=...;Password=...;TrustServerCertificate=true;Encrypt=true;`.

**Frente nova: [[pipeliner-ingestao-xml]]**

Criada `mind/effort/on/pipeliner-ingestao-xml/` com três arquivos:
- `pipeliner-ingestao-xml.md` — frente principal (objetivo, arquitetura proposta, fluxo, perguntas pro analista).
- `manifesto-scriptagem.md` — rastreamento vivo dos artefatos a replicar no Director real (procedures, tabelas, opções, pipeline). Vivo por uma restrição importante: **não temos acesso ao sistema de scriptagem do Director ERP** — o analista replica no sistema real depois.
- `hipoteses.md` — 7 hipóteses iniciais com status.

Pasta de protótipos: `sources/procedures/pipeliner-ingestao-xml/` (T-SQL e DDL vão pra lá quando começar a prototipagem).

**Arquitetura proposta (tentativa):**
- Par de procedures: `sp_xml_varrer` (sem params, scheduled pelo Pipeliner a cada 10min) → `sp_xml_processar` (recebe path).
- Pasta raiz cfg em `TBopcoes`, com convenção de filesystem pra idempotência:
  ```
  <raiz>/              ← pendentes (varredura lê só aqui, não recursivo)
  <raiz>/importado/    ← sucesso
  <raiz>/falha/        ← erro
  ```
  Sugestão das 3 pastas veio do usuário. Ganho: idempotência gratuita, sem precisar de tabela de controle de "consumido".
- Tabela de log `TBingestao_xml_log` no estilo `TBintegracao_cobranca_bancaria_log` — duplo papel: vestígio operacional + controle.

**Princípio "analista decide" (memorizado)**

Em frentes que vão pra Director ERP: **agente propõe, analista decide**. Salvo em `feedback_analista_decide.md`. Nomes de procedures/tabelas/chaves de `TBopcoes` ficam marcados como tentativos até o analista revisar. O ponto de partida é pra acelerar a conversa, não substituir a decisão dele.

**Próximos passos (sem prazo, sob demanda):**
1. Estudar source do Pipeliner pra entender modelo de cadastro de pipeline (H1).
2. Inventário de técnicas de leitura de arquivo no SVN `\\172.27.3.10\svn\trunk` (H2).
3. Inspecionar `TBintegracao_cobranca_bancaria_log` no DBdirector como referência de schema de log (H5).

## Memory Maintenance

- Atualizada `feedback_credenciais_processa.md` para apontar pra `.env` (migração do `.tmp/senha-processa.txt`). Chaves cobrem rede Processa + DBdirector.
- Criada `feedback_analista_decide.md` registrando o princípio de proposta vs. decisão.
- `MEMORY.md` atualizado com as duas entradas.


## Your Task

Read the daily log above and compile it into wiki articles following the schema.

### Rules

1. **Extract key concepts** — Identify 3–7 distinct concepts worth their own article.
2. **Create concept articles** in `atlas/concepts/` — one `.md` file per concept.
   - Use the exact article format from the schema (YAML frontmatter + sections).
   - Include `sources:` in frontmatter pointing to the daily log file.
   - Use `[[concepts/slug]]` wikilinks to link to related concepts.
   - Write in encyclopedia style — neutral, comprehensive.
3. **Create connection articles** in `atlas/connections/` if this log reveals a non-obvious relationship between 2+ existing concepts.
4. **Update existing articles** if this log adds new information to concepts already in the wiki.
   - Read the existing article, add the new information, add the source to frontmatter.
5. **Update HOME.md** — add new entries to the table.
   - Each entry: `| [[path/slug]] | One-line summary | source-file | 2026-05-13 |`
6. **Append to calendar/log.md** — add a timestamped entry:

   ```
   ## [2026-05-13T23:05:54-03:00] compile | 2026-05-13.md
   - Source: calendar/notes/2026-05-13.md
   - Articles created: [[concepts/x]], [[concepts/y]]
   - Articles updated: [[concepts/z]] (if any)
   ```

### File paths

- Write concept articles to: D:\abulder\mind\atlas\concepts
- Write connection articles to: D:\abulder\mind\atlas\connections
- Update index at: D:\abulder\mind\HOME.md
- Append log at: D:\abulder\mind\calendar\system\log.md

### Quality standards

- Every article must have complete YAML frontmatter
- Every article must link to at least 2 other articles via `[[wikilinks]]`
- Key Points section should have 3–5 bullet points
- Details section should have 2+ paragraphs
- Related Concepts section should have 2+ entries
- Sources section should cite the daily log with the specific claims extracted

**Assistant:** Let me start by reading the required files.

**Assistant:** Now let me check what exists in the concepts and connections directories.

**Assistant:** I've identified 5 new concepts and 1 connection to extract. Let me create them all.

**Assistant:** Now update HOME.md and create the log entry.

**Assistant:** Compilação concluída. Aqui o resumo:

**5 concept articles criados:**

| Artigo | Resumo |
|--------|--------|
| `concepts/mind-pipeline` | Pipeline flush→capture→digest e por que `/mind:digest` não lê de `calendar/system/` |
| `concepts/dbdirector` | Instância SQL Server 2019, connection string, credenciais via `.env` |
| `concepts/pipeliner-service` | Serviço Windows orquestrador — agenda procedures, converte XML→JSON, despacha HTTP |
| `concepts/tbopcoes` | Tabela key-value de configuração do Director ERP |
| `concepts/idempotencia-filesystem` | Padrão de 3 pastas (pendente/importado/falha) para processamento idempotente |

**1 connection article criado:**

| Artigo | Resumo |
|--------|--------|
| `connections/pipeliner-dbdirector-bypass` | Hipótese de bypass — DBdirector lê XMLs direto, Pipeliner vira cron job |

**Não atualizado:** `concepts/xml-to-json-node` — já estava completo da criação no mesmo dia.

**HOME.md** atualizado com 6 novas linhas (5 concepts + 1 connection). **log.md** criado com o registro da compilação.
