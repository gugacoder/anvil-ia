**File:** 2026-05-16.md

---
title: "2026-05-16"
aliases: [2026-05-16]
tags: [calendar, note]
created: 2026-05-16
updated: 2026-05-16
---

# 2026-05-16 — notas do dia

- Escavação F019 — `PowerSelect3` (typeahead+async). Descoberta crítica: F019 está **mal-rotulada no manifest**. O componente que vive em `react-tools/components/PowerSelect3/` **não é typeahead async** — é um modal client-side de filtro local sobre `selectOptions[]` injetado pelo pai. O typeahead/async real está em `react-tools/components/PowerSelect.js` (V1, ainda na public API como `PowerSelect`) e o `useSelectFields` hook (carga via `/proc/`, `/select/`, `/selectquery/`). Coexistência de 3 versões: V1 (`PowerSelect.js`, async + react-select), V2 (`PowerSelect2.js`, react-select sem fetch), V3 (`PowerSelect3/PowerSelect.js`, modal próprio + filtro client). Contrato cobre as 3 + endpoint convention via `useSelectFields`.
- Notas de auditoria: nenhum dos 3 implementa debounce, mínimo de chars, cancel de requisição, cache (selectOptions cache é responsabilidade do **consumidor**, não do PowerSelect — Filtros.js mantém var-de-módulo `selectOptions` global; Form/Dashboard mantêm prop). Typeahead "remoto" como expectativa do manifest **não existe no legado** — toda filtragem por digitação é client-side sobre lista já carregada.
- Sub-contrato candidato: `select-options-endpoint.md` (regra `useSelectFields.sendRequest` — `/proc/*` POST com state, `/selectquery/{key}?parametro_id=` GET, `{api}` GET cru, `selectDataType:fixedList` curto-circuito). Já era candidato apontado por F016; F019 reforça.
- Manifest atualizado: F019 muda `Contract` para [[power-select]] e ganha nota de re-rotulagem (rótulo legado "typeahead+async" é ilusório).

## Sources

- escavação direta em `sources/engenharia--fabrica--javascript--react-tools/src/components/{PowerSelect.js,PowerSelect2.js,PowerSelect3/PowerSelect.js,Filtro/Filtros.js,GenericForm/Form.js,DashBoard/DashBoardBox.js,UserPreference/DataGridPrintingModal.js}` + `src/hooks/useSelectFields.js` + `src/index.js`.


## Your Task

Read the daily log above and compile it into wiki articles following the schema.

### Rules

1. **Extract key concepts** — Identify 3–7 distinct concepts worth their own article.
2. **Create concept articles** in `atlas/concepts/` — one `.md` file per concept.
   - Use the exact article format from the schema (YAML frontmatter + sections).
   - Include `sources:` in frontmatter pointing to the daily log file.
   - Use `[[concepts/slug]]` wikilinks to link to related concepts.
   - Write in encyclopedia style — neutral, comprehensive.
3. **Create connection articles** in `atlas/connections/` if this log reveals a non-obvious relationship between 2+ existing concepts.
4. **Update existing articles** if this log adds new information to concepts already in the wiki.
   - Read the existing article, add the new information, add the source to frontmatter.
5. **Update HOME.md** — add new entries to the table.
   - Each entry: `| [[path/slug]] | One-line summary | source-file | 2026-05-19 |`
6. **Append to calendar/log.md** — add a timestamped entry:

   ```
   ## [2026-05-19T00:54:38-03:00] compile | 2026-05-16.md
   - Source: calendar/notes/2026-05-16.md
   - Articles created: [[concepts/x]], [[concepts/y]]
   - Articles updated: [[concepts/z]] (if any)
   ```

### File paths

- Write concept articles to: D:\anvil\mind\atlas\concepts
- Write connection articles to: D:\anvil\mind\atlas\connections
- Update index at: D:\anvil\mind\HOME.md
- Append log at: D:\anvil\mind\calendar\system\log.md

### Quality standards

- Every article must have complete YAML frontmatter
- Every article must link to at least 2 other articles via `[[wikilinks]]`
- Key Points section should have 3–5 bullet points
- Details section should have 2+ paragraphs
- Related Concepts section should have 2+ entries
- Sources section should cite the daily log with the specific claims extracted

**Assistant:** Let me start by reading the files I need to update and the daily log source.

**Assistant:** Now let me analyze the daily log. I see these distinct concepts:

1. **power-select-versions** - The 3 coexisting PowerSelect versions in react-tools (V1 async, V2 react-select, V3 modal)
2. **select-options-endpoint** - The `useSelectFields` hook and endpoint convention (`/proc/`, `/select/`, `/selectquery/`)
3. Update **react-tools** with the newly discovered PowerSelect component details

Let me also check if there's an existing concept for either of these.

**Assistant:** No existing articles for these concepts. Now I'll create the new articles, update the existing react-tools article, update HOME.md, and append to log.md — all in parallel where possible.

**Assistant:** Now update the existing react-tools article and update HOME.md + log.md.

**Assistant:** Now update HOME.md and log.md.

**Assistant:** Done. Here's the compile summary:

**Articles created (2):**
- `concepts/power-select-versions.md` — The 3 coexisting PowerSelect versions in react-tools (V1 async+react-select, V2 react-select no fetch, V3 modal client-side). Key finding: F019 was mislabeled as "typeahead+async" but the actual component is client-side only. Remote typeahead doesn't exist in the legacy codebase.
- `concepts/select-options-endpoint.md` — The `useSelectFields` hook convention with 4 endpoint patterns (`/proc/*` POST, `/selectquery/{key}` GET, `{api}` GET, `fixedList` short-circuit). Sub-contract candidate shared between F016 and F019.

**Articles updated (1):**
- `concepts/react-tools.md` — Added Key Point about PowerSelect 3-version coexistence, links to both new articles, audit findings (no debounce/cancel/cache in any version).

**Index & log updated:**
- `HOME.md` — 2 new rows + react-tools row updated
- `calendar/system/log.md` — compile entry appended
