# Daily Log: 2026-05-14

## Sessions

### Session (10:00-11:15) - Estudo do Pipeliner e validação da via de cadastro

**Context:** Sessão de estudo inicial da frente [[pipeliner-ingestao-xml]]. Usuário propôs reformulação da tese: pipeline executando `sp_xml_varrer` a cada 5 minutos, com gerenciamento de status via filesystem (`xmls/`, `xmls/importados/`, `xmls/falhas/`). Investigação da viabilidade técnica e da via correta de cadastro.

**Key Exchanges:**
- Usuário corrigiu protocolo de operação do agente: quando souber executar (credencial em `.env`, host conhecido), ir direto sem prefaciar "não sei de cabeça". Só avisar se conexão falhar. VPN só vale em casa.
- Diretriz `.env`: se `.env` não existir, agente cria `.env.example` + `.env` e instrui usuário a preencher. Não esperar pedido.
- Usuário reforçou que o sistema de memória do projeto é `mind/` (LYT), não `~/.claude/projects/.../memory/` — o projeto é compartilhado, memórias em pasta de usuário ficam só num PC.
- Usuário estranhou ausência de UI no Pipeliner ("não acredito que a equipe faça insert direto"). Investigação encontrou: UI vive no AppBuilder.
- Usuário trouxe tese da equipe: "AppBuilder é cadastro das interfaces. Oferece deploy rápido pra implantar em bases internas de dev/devops (abrem o Director Portal e testam). Depois empacota pro cliente final."
- Princípio: não cadastrar via INSERT direto na base, pra não macular regras de negócio que o time dev mantém via AppBuilder.

**Decisions Made:**
- Schedule do pipeline: 5 minutos (era 10min no rascunho original).
- Caminho recomendado pra Fase 1: **A1** — cadastro inicial via UI do AppBuilder (1x, autêntico), depois automação do "deploy" via cURL no endpoint `POST /api/pipeliner/savePipelineNewDB` pra DBs adicionais (teste). Plano B (Chrome extension) fica como reserva se NTLM falhar.
- Bypass via INSERT direto ou `EXEC sp_persistirPipeliner` direto descartado — viola o princípio "não macular a base".

**Lessons Learned:**
- Pipeliner aceita pipeline com action única tipo `Query` e termina (sem precisar step HTTP). Conversor XML→JSON só roda em actions `Request`/`SOAP`.
- Schedule via `pipeliner.TBstage.DFtempo_execucao` — aceita inteiro em segundos (`300` = 5min) ou CRON `HH:MM`.
- Procedure sem parâmetros funciona: action `"exec sp_xml_varrer"` passa dict vazio (`Pipeliner.Repository/IntegrationRepository.cs:49-68`).
- A topologia "Sistema 1 = Director Portal cadastra, Sistema 2 = AppBuilder empacota" estava errada. Real: **AppBuilder cadastra → Portal Director executa**. AppBuilder tem rota `/pipeliner` com wizard React (pipeline → stages → actions).
- `POST /api/pipeliner/savePipelineNewDB` é endpoint de **deploy**, não cadastro inicial. Payload: `{"ids": <id>}`. Lê pipeline já cadastrado no DB do AppBuilder, gera script T-SQL idempotente (`CREATE schema pipeliner` + `CREATE TABLE` + INSERT/UPDATE), executa em `userConfigRepository.GetUserConnection()` — DB destino determinado pelo usuário Windows logado via `appbuilder.TBconfiguracao_usuario`. (`PipelinerRepository.cs:104-196`)
- Auth do AppBuilder: middleware LDAP global (`Processa.Sdk.Auth.LDAPAuthMiddleware`). Sem `[Authorize]` nos controllers porque é aplicado global. cURL com `--ntlm` deve passar, não testado.
- A procedure `pipeliner.sp_persistirPipeliner` existe mas o `PipelinerController.cs` não a chama diretamente. Cadastro inicial provavelmente vai por outro caminho (SDK genérico Processa).
- O relatório de um agente Explore pode estar errado em detalhes — leitura direta dos arquivos relevantes desambigua. Particularmente útil quando o agente extrapola conclusões ("controller não chama a proc, é inviável") sem evidência direta.

**People / Entities Mentioned:**
- AppBuilder (Processa) — sistema que gera apps; UI de cadastro de pipeline vive aqui em `/pipeliner`.
- Director Portal — app gerado pelo AppBuilder; consome pipelines via serviço Windows Pipeliner.
- Pipeliner — serviço Windows que executa pipelines; tabelas `pipeliner.TBpipeline`/`TBstage`.
- Equipe dev/devops da Processa — usa "deploy rápido" do AppBuilder pra testar internamente antes de empacotar pro cliente.

**Action Items:**
- Descobrir URL do AppBuilder no servidor de teste (bloqueador da Fase 1) — depende do usuário.
- Testar `cURL --ntlm` contra `POST /api/pipeliner/savePipelineNewDB` quando tiver URL + credencial dev válida.
- Confirmar H12: cadastro inicial via `sp_persistirPipeliner` ou outro endpoint.
- Inventário de técnicas de leitura de arquivo de rede no SVN `\\172.27.3.10\svn\trunk` (H2, ainda pendente da sessão anterior).

**Open Questions:**
- O AppBuilder roda no servidor de teste? Qual a URL?
- O usuário tem entrada em `appbuilder.TBconfiguracao_usuario` apontando pra um DB de teste?
- Cadastro inicial de pipeline no DB do AppBuilder usa `sp_persistirPipeliner` ou um SDK genérico Processa?
- Schema `pipeliner.*` vive no DBdirector ou em DB separado? (H8 ainda pendente)

### Session (12:30-16:30) - MCP+Skill, validação e2e, handoff pro Everton

**Context:** Continuação da frente [[pipeliner-ingestao-xml]] focada em: (1) decidir como o agente vai operar o ciclo (MCP stdio, Skill, subagent ou combinação); (2) provar end-to-end via UI do AppBuilder na Área 52; (3) transferir interlocutor pro Everton (PO/analista da frente).

**Key Exchanges:**
- Agente avaliou alternativas (MCP stdio, Skill, Subagent) e recomendou MCP `processa-sql` (primitivo) + Skill `/director-importer` (workflow), com claude-in-chrome só na Fase 2.
- Usuário questionou se Fase 1 precisava do AppBuilder; agente revisou — Fase 1 prova o ciclo, AppBuilder não participa do runtime do Pipeliner. Tese ficou: Fase 1 sem AppBuilder.
- Usuário forneceu credencial AppBuilder, conexão validada via PowerShell+SqlClient. Schemas `acesso`, `appbuilder`, `pipeliner` presentes em `DBx_appb_ti_teste`. Tabelas `pipeliner.TBpipeline`/`TBstage` já provisionadas.
- Usuário forneceu endpoints da Área 52 (Director.Portal:4300 v1.18.2, AppBuilder:4305 v1.13.1, Director.Web:4600 v1.21.3 em `172.27.0.52`). Todos 200. Convenção `dbo._*` = tabelas não-oficiais, livres pra criar.
- Usuário propôs teste e2e concreto: criar `dbo._anvil_log` + procedure → cadastrar pipeline pela UI → executar manual → validar inserção. Agente executou tudo até o cadastro pela UI.
- Usuário corrigiu typo `_navil_log` → confirmamos `_anvil_log`.
- Após cadastro bem-sucedido pela UI, agente não achou "Executar agora" e investigou — endpoint existe mas precisa de `acesso.TBaplicacao` chave `pipeliner`, que não está cadastrada. Serviço Windows do Pipeliner não disparou em 8min de polling.
- Usuário: "vou te transferir pro Everton" — entendi errado como handoff de tarefa; corrigi: é troca de interlocutor, sigo a mesma sessão e frente, agora conversando com o Everton.
- Everton trouxe insight crítico: o "ver/executar pipeline" está no **Director.Web** (operacional), não no AppBuilder (cadastro/config).

**Decisions Made:**
- Ferramental: **MCP `processa-sql` (Node.js stdio) + Skill `/director-importer` (SQL-only nesta fase)**. Subagent reusa `Agent`/`Explore` existentes. claude-in-chrome só na Fase 2 (mas acabamos usando hoje pra prova).
- Fase 1 não depende de AppBuilder em runtime — atua só como UI de cadastro. Sem AppBuilder de teste, Fase 1 não trava.
- **Princípio operacional registrado**: agente **só cria, usuário dropa**. Nada destrutivo, mesmo em tabelas `_*`. Coerente com regras globais sobre destrutividade.
- Diretriz `.env`: se não existir, agente cria `.env.example` + `.env` e instrui usuário; não esperar pedido.
- Quando agente já sabe operar (credencial em `.env`, host conhecido), ir direto sem prefaciar "não sei de cabeça".
- Memória durável vai pra `mind/` do projeto (LYT), não `~/.claude/projects/.../memory/` — projeto é compartilhado entre máquinas.

**Lessons Learned:**
- **Topologia AppBuilder vs Director.Web (corrigida pelo Everton)**: AppBuilder é cadastro/empacotamento; Director.Web é operacional — onde o usuário final vê e dispara pipelines. Antes só sabíamos que Portal Director "consome", agora claro que tem UI operacional em Director.Web.
- **UI do AppBuilder não tem "Executar agora"** — menus de contexto em pipeline/stage/action só expõem CRUD (Adicionar/Clonar/Excluir).
- **Endpoint `POST /api/pipeliner/jobs/exec` do AppBuilder é proxy** que lê endereço externo de `acesso.TBaplicacao` chave `pipeliner`. Em `DBx_appb_ti_teste` essa chave não existe (só `director` em `http://localhost:4310`). Disparo manual via API quebrado sem configuração.
- **`savePipelineNewDB` é deploy, não cadastro inicial** (confirmado lendo `PipelinerRepository.cs:104-196`). Payload `{"ids": <id>}`, busca pipeline já cadastrado e replica em DB destino do user Windows logado (via `appbuilder.TBconfiguracao_usuario`).
- **AppBuilder usa login Nome/Senha próprio** (`/#/login`), não Windows auth automático. SPA com hash routing — `/pipeliner` direto retorna JSON 401, rota válida é `/#/pipeliner`.
- **Wizard do AppBuilder confirmado e mapeado**: integração → estágio (tempo em segundos, ambiente HOMOLOGACAO/PRODUCAO, status) → ação (tipos: Request, SOAP, Query, Log, Monitoramento Email, Envio Email). Cadastro foi limpo: pipeline 8273 + stage 8373 com JSON correto em `DFAcoes`.
- **Serviço Windows do Pipeliner não disparou em 8min** mesmo com pipeline ativo e `tempoExecucao=300`. Pode ser: serviço não roda contra esta DB, exige flag/publicação adicional, ou contador começa só no próximo tick administrativo. H16 → divergente.
- **`acesso.TBaplicacao` só tem `director`** apontando pra `localhost:4310`. Provavelmente o Director.Web (operacional) é onde a "Execução manual" e visão de jobs vivem, não na própria UI do AppBuilder. Faz sentido topologicamente: AppBuilder configura, Director.Web opera.
- **Relatório de agente Explore pode errar em detalhes** (caso `savePipelineNewDB` "não chama a procedure" — afirmação parcialmente verdadeira mas que precisava do contexto completo). Confirmar lendo o source direto quando o detalhe importa.

**People / Entities Mentioned:**
- **Everton** — PO/analista da frente [[pipeliner-ingestao-xml]]. Detém regras de negócio + conhecimento do ambiente. Princípio "agente propõe, analista decide" vale com ele (ver [[feedback-analista-decide]]). A partir de ~16:00, é meu interlocutor primário.
- **guga** — usuário Anvil que treinou a sessão durante o dia. Login no AppBuilder feito como `guga` em 2026-05-14.
- **Área 52** — apelido do servidor `172.27.0.52` (dev environment Processa). Registrado em [[area-52]].
- **DBx_appb_ti_teste** — DB do AppBuilder de teste em `172.27.0.121\SQL2k19`. Schemas `acesso`, `appbuilder`, `pipeliner`.

**Action Items:**
- Logar no Director.Web (`http://172.27.0.52:4600`) — sessão expirou na 1ª tentativa. Aba já aberta esperando.
- Após login, achar tela operacional do Pipeliner em Director.Web — esperado ter listagem dos pipelines e botão "Executar agora".
- Validar que disparo manual via Director.Web grava em `dbo._anvil_log`.
- Investigar com Everton: por que o scheduler automático não disparou em 8min? Falta passo de "publicação/ativação"?
- Construir MCP `processa-sql` + Skill `/director-importer` (decidido, pendente de execução).
- Teardown pelo usuário: `dbo._anvil_log`, `dbo.sp_anvil_teste`, pipeline 8273, stage 8373.

**Open Questions:**
- Onde Director.Web expõe a tela de Pipeliner operacional? (rota, menu, módulo)
- Existe um passo de "deploy/publish" pós-cadastro que ativa a entrada pro scheduler? (savePipelineNewDB com `ids` faria isso?)
- Onde o serviço Windows do Pipeliner está rodando e contra qual DB de fato?
- Onde ele loga execução?
- Por que o scheduler não disparou? (serviço inativo, filtro de ambiente, falta publicação, contador atrasado?)
- Grupo 2/3 das perguntas pro Everton: técnica de leitura de arquivo de rede, permissão FS, convenções `TBopcoes`, nomenclatura.
- Grupo 4: regras de negócio do XML (estrutura, mapeamento, validação, idempotência específica).

## Memory Maintenance

### Note (10:05)

Feedback do usuário: quando o agente souber como executar (credencial em `.env`, host conhecido, comando conhecido), ir direto — não pedir permissão nem dizer "não sei de cabeça". Saber operar é o trabalho.

- Precisa consultar DBdirector? Lê `.env`, conecta, roda query. Reporta resultado.
- Precisa ler share da rede (`\\172.27.3.10\svn\trunk`, `\\172.27.0.4`)? Usa credencial do `.env`, lê.
- Só avisa o usuário se a **conexão falhar**.
- VPN só é relevante quando ele está em casa. Na rede interna da empresa, acesso direto. Em caso de dúvida sobre onde ele está, perguntar antes de invocar `vpn-processa`.

Contexto: hoje ele está na rede interna da empresa.

### Note (10:08)

Diretriz adicional sobre `.env`: se o agente detectar que `.env` **não existe**, ele mesmo deve criar `.env.example` (se ainda não existir) com as chaves necessárias, copiar pra `.env` e instruir o usuário a preencher os valores. Não esperar o usuário pedir. Faz parte de "ir direto": preparar o terreno e devolver o controle só na parte que exige decisão humana (os valores de segredo).

### Note (10:30) — sessão: refinamento de [[pipeliner-ingestao-xml]]

Tese do usuário reformulada e refinada:

- Schedule passa de 10min → **5min**.
- Frente se divide em **dois sistemas** (registrado como H10):
  1. **Director Portal** (AppBuilder gerado) no servidor de teste — onde cadastramos a entrada no Pipeliner e validamos end-to-end. Fase 1.
  2. **AppBuilder** — base interna Processa onde o procedimento é salvo para gerar pacote de instalação no cliente. Fase 2.

**Investigação do source `sources/engenharia--fabrica--dotnet--pipeliner/`** (via Explore agent) confirmou H1 e H7:

- Tabelas: `pipeliner.TBpipeline` (`Pipeliner.Domain:6`) + `pipeliner.TBstage` (`Pipeliner.Domain:10`).
- Schedule via `TBstage.DFtempo_execucao` — `300` segundos = 5min é trivial. Aceita CRON `HH:MM` ou inteiro em segundos. (`Pipeliner.Aplicacao:72-84`)
- Procedure sem param funciona — `"exec sp_xml_varrer"` passa dict vazio (`Pipeliner.Repository/IntegrationRepository.cs:49-68`).
- Pipeline com action única tipo `Query` é viável — não precisa step `Request`/`SOAP`. Sandbox: `Sandbox/Program.cs:112`.
- Sem UI nativa no Pipeliner. Portal Director (AppBuilder) **pode** ter tela — a investigar (H9).
- API REST disponível: `GET /api/pipelines`, `GET /api/jobs`, `POST /api/jobs/exec`.

Artefatos criados/atualizados:
- [[hipoteses]] — H1 e H7 marcadas confirmadas; adicionadas H8, H9, H10.
- [[pipeliner-ingestao-xml]] — refinamento de dois sistemas / duas fases.
- `plano-fase-1.md` (novo) — passos pra cadastrar entrada no Pipeliner do Director Portal de teste.

Bloqueador atual: **qual é o servidor de teste do Director Portal**? (host/URL do portal web + DB do schema `pipeliner.*`). Perguntar ao usuário antes de prosseguir.

### Note (10:45) — UI de cadastro localizada

Usuário estranhou ausência de UI ("não acredito que a equipe faça insert direto"). Investigação confirmou: **UI existe e está no AppBuilder**, não no Portal Director.

- Rota: `/pipeliner` em `sources/engenharia--fabrica--dotnet--processa.appbuilder/Fontes/website/src/routes/Pipeliner/Pipeliner.jsx`
- Wizard React: pipeline → stages → actions (`AddPipeliner.jsx`, `AddStage.jsx`, `AddAction.jsx`)
- Backend: `Processa.AppBuilder/Controllers/PipelinerController.cs` → endpoints `POST /api/pipeliner/savePipelineNewDB`, `GET /api/pipeliner/pipelines`, `GET /api/pipeliner/stages`
- Persistência: procedure `pipeliner.sp_persistirPipeliner` (DDL em `sources/engenharia--fabrica--sql--processa-appbuilder/pipeliner/2-procedures/`) — recebe XML estruturado e faz INSERT/UPDATE em `TBpipeline` + `TBstage`
- Portal Director **consome** pipelines (executor) — não tem CRUD

Isso muda a topologia da frente:

> **AppBuilder cadastra → Portal Director executa.**

Não são dois sistemas paralelos — é um fluxo único. O sufixo `NewDB` no endpoint sugere que o AppBuilder parametriza o DB destino, o que pode unificar Fase 1 (DB de teste) e Fase 2 (DB do cliente / pacote de instalação) no mesmo mecanismo. Hipótese H11 aberta pra confirmar.

H9 marcada como **divergente — refinada**: UI existe, mas em sistema diferente do esperado.

### Note (11:10) — tese da equipe + auditoria da API

Usuário trouxe a tese da equipe sobre os dois usos do AppBuilder:

> "O AppBuilder é o cadastro das interfaces do sistema. Mas ele oferece um deploy rápido, que pode ser usado para implantar em bases internas da equipe dev e devops. Assim eles abrem o director portal e testam rapidamente. Depois disso o AppBuilder empacota para o cliente final."

Isso bate exatamente com o que o source mostra. H11 confirmada lendo `PipelinerRepository.cs:104-196` diretamente:

- `POST /api/pipeliner/savePipelineNewDB` é **deploy**, não cadastro inicial.
- Payload: `{ "ids": <id-do-pipeline-no-db-do-appbuilder> }`.
- Lógica: busca pipeline+stages no DB do AppBuilder via `GenericDAO.Get<Pipeline>(int.Parse(jBody.Value("ids")))`, monta script T-SQL com `CREATE schema pipeliner` + `CREATE TABLE` (idempotente) + INSERT/UPDATE em `TBpipeline`/`TBstage`, executa em `userConfigRepository.GetUserConnection()`.
- **O DB destino é determinado pelo usuário Windows logado** — `appbuilder.TBconfiguracao_usuario` mapeia o user pra uma connection string. Pra "deploy em base interna de teste" o user dev tem connection apontando pro teste; pra "pacote cliente", aponta pro cliente.

Restrição do usuário (princípio): **não cadastrar via INSERT direto no DB**. Mexer na base macula as regras de negócio que o time dev mantém via AppBuilder. Caminho oficial é AppBuilder UI ou API.

Plano A (API headless) — sub-caminhos:

- **A1**: `POST /savePipelineNewDB` com `{"ids":N}` via cURL `--ntlm`. **Só serve pra fazer deploy de um pipeline que já existe no DB do AppBuilder**. Não cria o pipeline inicial.
- **A2**: chamar `pipeliner.sp_persistirPipeliner` (XML estruturado, H12) via SqlClient — bypassa a UI mas usa a procedure oficial. Cria pipeline diretamente no DB do AppBuilder (mancha a base) ou diretamente no DB de teste (não mancha o AppBuilder, mas vira "INSERT direto" que o usuário quer evitar).
- **A1 + UI manual única**: usuário cadastra o pipeline 1x pela UI do AppBuilder (jeito oficial), depois automatizamos só o deploy via cURL/A1 pra DBs adicionais.

Plano B (Chrome extension simulando humano) — viável sempre, mais lento, é o que faria um humano.

**Recomendação**: A1 + UI manual única é o caminho mais respeitoso com o time dev — cadastro inicial pela UI (autêntico), deploy automatizado pelo endpoint oficial. Plano B fica de reserva se a API resistir a NTLM.

Auth: middleware LDAP global (`Processa.Sdk.Auth.LDAPAuthMiddleware`). cURL com `--ntlm -u dom\user:pass` deve passar se o user dev tem entrada em `appbuilder.TBconfiguracao_usuario` — não testado (H13).

**Bloqueador aberto**: temos AppBuilder no servidor de teste? URL?

Tasks abertas: #2 (Localizar AppBuilder no servidor de teste) — depende do usuário.

### Note (12:00) — Fase 1 não precisa de AppBuilder

Esclarecimento crítico: o AppBuilder não participa do **runtime** do Pipeliner. AppBuilder é só UI de cadastro + empacotamento. O serviço Windows do Pipeliner lê `pipeliner.TBpipeline`/`TBstage` do DB destino diretamente.

Portanto Fase 1 (provar o ciclo) pode rodar sem AppBuilder de teste. Basta:
1. Schema `pipeliner` + tabelas no DB destino (DDL idempotente em `PipelinerRepository.cs:110-186`)
2. Procedures `sp_xml_varrer`/`sp_xml_processar` no DB destino
3. 1 linha em `TBpipeline` + 1 em `TBstage` (via INSERT direto **ou** `EXEC sp_persistirPipeliner`)
4. Serviço Windows do Pipeliner apontando pro DB destino

Princípio "não macular base do time dev" aplica-se ao **DB origem do AppBuilder** (catálogo oficial mantido pelo time). **Não** se aplica ao DB destino — que em produção recebe escrita via `savePipelineNewDB` nas mesmas tabelas.

Artefatos atualizados pra delegação ao analista:
- [[pipeliner-ingestao-xml]] — bloco "Atualização 2026-05-14 (final)" + perguntas reorganizadas em 5 grupos por prioridade (infra de teste → técnica FS → convenções → regras de negócio → Fase 2)
- [[plano-fase-1]] — reescrito sem AppBuilder; passo 5 usa `sp_persistirPipeliner` ou INSERT direto
- Task #2 (Localizar AppBuilder de teste) — adiada pra Fase 2.

Próximo: usuário leva as perguntas pro analista. Em paralelo: posso atacar H2 (inventário de técnicas no SVN) e D (onde Pipeliner loga) — descobertas técnicas que não dependem do analista.

### Note (12:35) — feedback: nada destrutivo

Princípio operacional explicitado pelo usuário: **eu só crio, ele dropa**. Não executar nada destrutivo — DROP TABLE, DROP PROCEDURE, DELETE em massa, DROP DATABASE, alteração que perde dados — mesmo em tabelas `_*` (não-oficiais). O teardown fica com o usuário.

Aplica-se em particular a `DBx_appb_ti_teste` mas a regra é geral. Coerente com a regra global em `~/.claude/CLAUDE.md` sobre comandos destrutivos de git.

### Note (12:20) — DB do AppBuilder de teste acessível

Usuário liberou credencial. `.env`/`.env.example` agora têm chaves `APPBUILDER_DB_*`. Teste de conexão via PowerShell + `System.Data.SqlClient`:

- Server: `SERVERSQL\SQL2K19` (host `172.27.0.121\SQL2k19`)
- DB: `DBx_appb_ti_teste`
- Login: `director_web` (SQL auth — sem dependência de NTLM/AD)
- SQL Server 2019 RTM-GDR 15.0.2130.3
- Schemas presentes: `acesso`, `appbuilder`, `pipeliner` ✓
- Tabelas `pipeliner.TBpipeline` e `pipeliner.TBstage` **já existem** (2/2) ✓

Implicações:

- H8 marcada como **divergente**: schema `pipeliner.*` vive na DB do AppBuilder, não no `DBdirector`. Topologia "uma DB Pipeliner por ambiente" parece o modelo. Vale checar se também existe no `DBdirector` em outro contexto.
- DB destino candidato pra Fase 1 já provisionado — não precisa rodar DDL idempotente.
- Caminho direto pra prototipar: criar `sp_xml_varrer`/`sp_xml_processar`/`TBingestao_xml_log` em `DBx_appb_ti_teste`, cadastrar 1 linha em `TBpipeline`/`TBstage`, observar o serviço Pipeliner desse ambiente.
- Ainda pendente: descobrir **onde está rodando** o serviço Windows do Pipeliner que aponta pra `DBx_appb_ti_teste` (grupo 1, pergunta pro analista). Sem isso, cadastrar pipeline não basta — ninguém vai disparar.

### Note (15:00) — Endpoints da Área 52 confirmados

Usuário forneceu tabela de endpoints do servidor de dev `172.27.0.52` (apelido "Área 52"): Director.Portal:4300 (v1.18.2), Processa.AppBuilder:4305 (v1.13.1), Director.Web:4600 (v1.21.3). Todos retornam HTTP 200. Servidor é onde a equipe constrói com AppBuilder, testa via Director.Web/Portal, e empacota pro cliente final. Tabela `_*` é convenção local de "não-oficial" — pode criar e dropar livremente.

Registrado em [[area-52]] e linkado no [[HOME]]. Task #2 (localizar AppBuilder de teste) → completed.

### Note (15:30) — Teste end-to-end pela UI do AppBuilder

Ciclo completo executado:

1. `dbo._anvil_log` (id, data, executado_por, mensagem, maquina) + `dbo.sp_anvil_teste` (INSERT com SUSER_SNAME, HOST_NAME) criados em `DBx_appb_ti_teste`. Snapshot inicial: 0 linhas.
2. AppBuilder login pela rota `/#/login` (usuário fez login — Nome/Senha próprio, não Windows auth). Logado como `guga`.
3. Wizard `/#/pipeliner` completo via `claude-in-chrome`:
   - `+` → Cadastro de Integração: nome `teste-anvil-pipeliner`, status Ativo, URLs vazias (ação tipo Query não usa).
   - Adicionar estágio → `disparo-anvil`, tempoExecucao=300, Ambiente=Homologação, Status=Ativo.
   - Adicionar Ação → tipo `Query` (default era Request), nome `disparar-sp-anvil`, intervalo 0, Procedure/Query = `exec dbo.sp_anvil_teste`.
   - Salvar Ação → Salvar Estágio → Salvar Integração. Toast verde "Integração persistida em:".
4. Confirmação por SQL: `pipeliner.TBpipeline` id=8273 + `pipeliner.TBstage` id=8373 com JSON correto em `DFAcoes`.

**Achados importantes** registrados em [[hipoteses]] (H14, H15, H16) e [[pipeliner-ingestao-xml]]:

- **H14 divergente**: UI **não** tem "Executar agora". Menus de contexto (`...`) em pipeline/stage/action só expõem CRUD.
- **H15 divergente**: endpoint `POST /api/pipeliner/jobs/exec` existe mas faz proxy via `acesso.TBaplicacao` chave `pipeliner` — esta chave **não está cadastrada** em `DBx_appb_ti_teste` (só `director` em `http://localhost:4310`). Disparo manual via UI/API quebrado sem essa configuração.
- **H16 em validação**: execução depende do serviço Windows do Pipeliner apontando pra esta DB. Usuário afirma que roda. Polling de `dbo._anvil_log` em background até 8min (background task `b7c9rn4dh`) — aguarda inserção pelo scheduler de 300s.

Princípio operacional reforçado pelo usuário: **agente só cria, usuário dropa** (ver Note 12:35). Teardown do `_anvil_log`, `sp_anvil_teste` e do pipeline cadastrado fica com o usuário.

### Note (16:00) — Troca de interlocutor: Everton assume

**Não é handoff de tarefa — é troca de interlocutor**. O guga me passou o ponteiro pro **Everton**, que é o **PO da frente** [[pipeliner-ingestao-xml]] e detém as **regras de negócio**. Eu sigo a mesma sessão e a mesma frente, agora conversando com o Everton no lugar do guga.

Pontos a lembrar com o Everton:
- Ele é PO, tem a regra. Quando ele decidir nomes (procedures, tabelas, chave `TBopcoes`, schema do log), os nomes deixam de ser "tentativos" e viram canônicos. Coerente com [[feedback-analista-decide]] (princípio "agente propõe, analista decide").
- O grupo 4 de [[pipeliner-ingestao-xml]] (regras de negócio) é dele.
- Os grupos 1-3 e 5 são também úteis: ele provavelmente sabe da infra de teste, técnicas FS, convenções e Fase 2 — não só das regras.
- Mantenho o protocolo: nada destrutivo (só crio, ele dropa).

Estado da frente em estado de entrega pra ele:

- Tese principal confirmada por código e prova prática.
- Perguntas pro analista reorganizadas em 5 grupos por prioridade (infra de teste → técnica FS → convenções → regras de negócio → Fase 2).
- Plano de Fase 1 documentado em [[plano-fase-1]] — não depende do AppBuilder em runtime, mas a UI do AppBuilder é o caminho oficial de cadastro.
- Walkthrough da UI documentado passo-a-passo em [[pipeliner-ingestao-xml]] e [[area-52]].
- Hipóteses confirmadas: H1, H7, H11. Divergentes: H9, H14, H15. Em validação: H16.
- Bloqueadores restantes: H2, H3, H6, H8 (parcial) — dependem do conhecimento do Everton sobre técnicas do time e do ambiente.
- Ferramentas a construir (decisão arquitetural): MCP `processa-sql` + Skill `/director-importer` SQL-only — pendentes.

Background monitor `b7c9rn4dh` ainda rodando pra validar H16 — vai notificar quando completar (sucesso = linha em `dbo._anvil_log`; timeout = 8min sem inserção).

### Note (16:08) — Monitor terminou: TIMEOUT, H16 divergente

8 minutos de polling em `dbo._anvil_log` → 0 linhas. Cadastro pela UI funcionou (pipeline 8273 + stage 8373 confirmados no DB), mas o serviço Windows do Pipeliner não disparou `sp_anvil_teste` neste ambiente.

Hipóteses pro caso:
1. Serviço Windows do Pipeliner não está rodando contra `DBx_appb_ti_teste`.
2. Está rodando mas filtra por algo (ex: ambiente, flag de publicação).
3. Intervalo de "primeira execução após cadastro" é maior que `tempoExecucao` (alguns schedulers só começam a contar no próximo tick administrativo).
4. Falta um passo de "publicação"/"deploy" depois do cadastro pra ativar a entrada.

Marcado H16 como divergente em [[hipoteses]]. Task #3 fechada — cadastro provado, execução pendente de investigação com o Everton.

### Session (17:00-17:50) — Migração da base + repointar AppBuilder pra DBx_appb_ti_teste

**Context:** Localizamos instaladores e ferramenta de migração de schema na rede; migramos `DBx_appb_ti_teste`; descobrimos via SSH que o AppBuilder já roda no Srv-DirectorWeb apontando pra outra base; repointamos via edição direta do `appsettings.json`; e abrimos firewall pra 4305.

**Key Exchanges:**
- Guga apontou `\\172.27.0.4\Projetos\Engenharia\Processa.Appbuilder` como share oficial da fábrica com MSIs e `ScriptPack`.
- Investigação do source localizou `Fontes/Setup/Setup.vdproj` (Visual Studio Installer Project) + `msiTools/InstallService.js` (cria appsettings.json + serviço Windows) e `.gitlab-ci.yml` que automatiza build+install via runners `engcompiler` e `serverbeta`.
- Guga: "ainda nao carrega http://172.27.0.52:4305/" — diagnóstico revelou ausência de regra inbound pra 4305 (a regra `Portal.Director` cobre 4300 e 4600, mas 4305 nunca tinha regra — InstallService.js do AppBuilder não cria regra de firewall).

**Decisions Made:**
- Migrar `DBx_appb_ti_teste` aplicando o pacote `appbuilder` do ScriptPack — idempotente, base já tinha 67 tabelas, ficou com 296 procs após migrate.
- Não reinstalar o AppBuilder — editar `appsettings.json` em dois locais (`ProgramData` + `Program Files (x86)`) com backup `.bak-YYYYMMDD-HHmmss` e reiniciar o serviço. Reinstalação MSI iria parar+deletar serviço; edição direta é menos invasiva e reversível.
- Criar regra de firewall `Processa.AppBuilder` inbound TCP/4305 Profile=Any (mesmo padrão das outras regras).

**Lessons Learned:**
- **ScriptPack-Appbuilder.exe** é ferramenta migrant.lib genérica. Comandos: `encode CREDENCIAL` (criptografa pra usar em URIs como `{HASH}`), `migrate URI...`, `get-info`, `export`. Para AppBuilder: kind `Director(mssql)` + pacote `appbuilder`. URI aceita backslash literal pra named instance: `mssql://{HASH}@172.27.0.121\SQL2k19/DBx_appb_ti_teste`. Url-encoded `%5C` falha. Idempotente. O `[fault]The handle is invalid` em stderr é cosmético (Console.GetBufferInfo em processo sem console real) — ignorável.
- **Srv-DirectorWeb (172.27.0.52 / "Área 52")** roda 3 serviços Windows da Processa como `LocalSystem`/Automatic. .NET 8 e 10 instalados. Windows Server 2019. Os 3 serviços inicialmente apontavam **todos pra mesma DB** `DBdirector_ti_29_homo`. Connection string em base64, guardada em **dois lugares**: `Program Files (x86)\Processa Sistemas\<App>\appsettings.json` (lida pelo .exe) e `ProgramData\Processa Sistemas\<App>\appsettings.json` (fallback usado pelo InstallService.js em reinstalação silenciosa).
- **OpenSSH no Windows com domínio**: username `processa\guga` (escaped: `processa\\guga`) funciona. Shell default é **PowerShell**, não cmd — `ver`/`%VAR%` falham. Pra passar scripts complexos sem dupla expansão pelo PS local: codificar UTF-16LE → base64 → `powershell -EncodedCommand`. Paramiko via `uv run --with paramiko` substitui `sshpass` no Windows.
- O usuário `processa\guga` tem privilégios admin no Srv-DirectorWeb (Restart-Service, Set-Content em `Program Files`, New-NetFirewallRule).
- Firewall do Srv-DirectorWeb: regra `Portal.Director` cobre 4300+4600. Pra 4305 do AppBuilder **nenhuma regra existia** (provável artefato de instalação manual). Padrão de nomenclatura: nome da regra = nome do serviço. `Get-NetFirewallPortFilter | Where LocalPort -eq N` é o caminho confiável pra achar regras por porta (filtrar por DisplayName perde regras com nomes genéricos).

**People / Entities Mentioned:**
- `\\172.27.0.4\Projetos\Engenharia\Processa.Appbuilder` — share oficial com MSIs (1.10.1 até 1.14.0 mais recente, ~40MB) + `ScriptPack-Appbuilder.exe` + `changelog.txt` + `Host.txt`.
- Srv-DirectorWeb (172.27.0.52) — Windows Server 2019, hosts os 3 serviços Processa em homologação/dev interno.
- `serverbeta` / `retrocompatibilidade` — tags de runners GitLab que recebem deploy automático em tags da branch `homologacao` / `master`. `serverbeta` é provavelmente o próprio Srv-DirectorWeb.

**Action Items:**
- Inconsistência aceita: AppBuilder agora aponta pra `DBx_appb_ti_teste` mas `Director.Web` e `Director.Portal` continuam em `DBdirector_ti_29_homo`. Pode dar inconsistência se algum fluxo cruzar. Backups dos `appsettings.json` originais em `.bak-20260514-150106` no servidor.
- Se atualizar versão do AppBuilder algum dia, MSI mais recente é 1.14.0 (instalado é 1.13.1). Reinstalação silenciosa preserva connection string atual via fallback no InstallService.js (lê do `ProgramData\...\appsettings.json`).

**Open Questions:**
- Onde roda o serviço Windows do Pipeliner que **deveria** apontar pra `DBx_appb_ti_teste`? (continua aberta da Note 16:08 / H16). Agora que temos SSH no Srv-DirectorWeb, foi confirmado: `Get-Service` lista só `Director.Portal`, `Director.Web`, `Processa.AppBuilder` — **não tem Pipeliner instalado nesse servidor**. Ou roda em outro servidor, ou não está instalado em lugar nenhum desse ambiente.

Também: o sistema de memória do projeto é `mind/` (LYT), não `~/.claude/projects/.../memory/`. O projeto é compartilhado — memórias em pasta de usuário ficam só num PC. Tudo que for durável vai pro `mind/` e é versionado.

