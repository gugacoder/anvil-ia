---
title: "2026-05-17"
aliases: [2026-05-17]
tags: [calendar, note]
created: 2026-05-17
updated: 2026-05-17
---

# 2026-05-17 — notas do dia

## Escavação F113 — `acesso.TBaplicacao` cross-tenant survey

Gate enabler de F110 (proxy multi-app) e cascata F092a-d (Cotacao tabs proc-based). Briefing pedia probe SQL2k19 + universo de appKeys + sub-survey Imperial + asserções para [[tbaplicacao-registry]].

### Execução

- Probe `D:/anvil/.tmp/probe-tbapl-cross.ps1` contra `172.27.0.121\SQL2k19` (SQL auth `sl/123` lido de `workspace/director-studio/.env`).
- 97 bases `DBdirector_*` candidatas em `sys.databases`. 89 com `acesso.TBaplicacao` populada (7 sem a tabela, 1 sem login). 970 linhas totais.
- Outputs: `tbapl-cross.csv` (full), `tbapl-universe.{txt,csv}` (agregação por DFchave), `tbapl-cotacao.txt` (foco cotacao 86 tenants), `tbapl-sample5.txt` (Imperial+Pereira+Castro+StarToys+Le_Ze full rows), `tbapl-errors.txt` (8 falhas), `probe-imperial-full.txt`.

### Achados-chave

- **Imperial cotacao confirmado existente** (gate principal F113): `DFid=5, DFendereco='http://127.0.0.1:5000', DFdominio='cotacao', DFhabilitado=0`. Endereço é dev/loopback — em produção real seria `http://52.67.203.133:5000` com `DFdominio=<tenant-slug>` (padrão observado em Castro=`castro`, Pereira=`pereira`, Le_Ze=`lecomze`, etc.).
- **23 appKeys distintos cross-tenant**. Núcleo estável ≥87 bases: `portal-aws, portal-director, edoc, pipeliner, cotacao-integrador, processaadm, cotacao, processa-sped, integrador-aws`. Opt-in: `wms (56), agent (43), gerenciamento-de-integracoes (35), checkin (34)`. Cauda one-off: `processa (9), mercadologic, centralmercadologic, aplicacao-teste, qualidade, rebaixa-de-preco, folha-de-pagamento, concentrador, retaguarda-varejo, testenivel, processa-adm`.
- **Imperial NÃO tem appKey `agent`** — implicação para F093 (cutover `configuracoes_email` via `agent.sp_*` quebra silenciosamente em Imperial sem mitigação). Manifest F093 ganhou nota; mitigação a decidir (seed pré-cutover / fallback Cotacao / detecção de ausência).
- **Auth-scheme legado mapeado** via `AppClientService.BuildHeader` (PortalDirector.Services/AppClientService.cs:43-56): switch por `DFchave`. `case "cotacao"` → `Basic base64("processa|{DFdominio}:99")`. `default` → `Bearer <JWT>` com `DirectorIdentity` **fixa** (`Id=1, Name="processa", CodEmpresa=0, NomeEmpresa="Processa") { Domain = DFdominio }` — **não propaga identity do usuário corrente**. Header HTTP extra `Domain: {DFdominio}` em **todas** as chamadas. URL final `{DFendereco}/api/proc/{proc}` literal (não normaliza trailing slash; preserva path-suffix como `checkin/`).
- `DFchave` é o **discriminador estável cross-tenant** — `DFid_aplicacao` varia per-tenant (cotacao=4/5/6/7/8/11 dependendo da base — Imperial=5, Bazinho=11, Castro=7, PCP=8, Vicosense=4, Bergao=6).
- `DFhabilitado=0` **NÃO bloqueia** chamadas via proxy multi-app — só esconde do menu home (`acesso.obter_aplicacoes_pagina_home` filtra `WHERE DFhabilitado=1 AND DFchave NOT IN ('portal-aws')`).
- Data-quality bug observado: `processa-sped` tem `DFporta=5003` mas `DFendereco='http://localhost:5005'` em todos os tenants — seed inconsistente, sem efeito em runtime porque clients ignoram `DFporta` (R17).
- 5 caminhos de resolução documentados no contrato: (1) `PortalAwsClient` bridge dedicado para `portal-aws` (lê 2 colunas, usa identity do usuário corrente, JWT inter-tenant); (2) `AppClientService.ExecProc/GetProc` resolver genérico (modelo de F110, switch auth por chave, identity de serviço fixa); (3) `CotacaoIntegradorClient` Basic estática hardcoded para `cotacao-integrador` (não usa `DFdominio`); (4) `EdocController` para `edoc`; (5) `acesso.obter_aplicacoes_pagina_home` projeção para menu home com filtros.

### Contrato publicado

[[tbaplicacao-registry]] com schema completo (13 colunas + constraints), 5 caminhos de resolução, tabela do universo cross-tenant 23 appKeys, snapshot Imperial, e **24 asserções R1-R24** mecanicamente verificáveis. F110 implementação obrigada a replicar lookup por `DFchave` + switch auth-scheme + header `Domain` + URL literal + ignorar `DFhabilitado`.

### Manifest

- F113 todo → accepted (gate resolvido; curator pendente).
- F110 ganhou nota detalhada das 5 obrigações de paridade extraídas de R4-R9.
- F093 ganhou nota cross-tenant sobre Imperial-sem-agent (mitigação obrigatória antes de implementar).

## Nova frente — Imperial NFE → pedido de separação

Brief lido em `D:\nic\mind\effort\on\teams\implantacao\imperial-logistica-nfe-pedido-separacao.md` (mind do Nic). Imperial é operador logístico; NFE precisa virar romaneio, não entrada fiscal. Primeiro impedimento sistêmico que a dupla Nic+Anvil tenta resolver sem escalar pra devs Processa.

Spec do Everton ainda não chegou — handshake Nic↔Anvil via `agent-chat` travado até spec consumível. Decidido avançar coleta em paralelo: Everton (spec de negócio) e Luiz Paulo (ID da interface do importador NFE atual + contexto técnico Director). Sem o ID da interface não conseguimos mapear ponto de bifurcação no fonte.

Frente criada: [[imperial-logistica-nfe-separacao]].

## Sources

- Probe scripts: `D:/anvil/.tmp/probe-tbaplicacao.ps1`, `D:/anvil/.tmp/probe-tbapl-cross.ps1`.
- Outputs: `D:/anvil/.tmp/tbapl-cross.csv`, `tbapl-universe.{txt,csv}`, `tbapl-cotacao.txt`, `tbapl-sample5.txt`, `tbapl-errors.txt`, `probe-imperial-full.txt`, `probe-dblist.txt`.
- Fontes legado: `sources/engenharia--fabrica--sql--portal-director/portal.director/criacao/acesso.TBaplicacao.sql`, `pos-script.sql`, `programacao/acesso.obter_aplicacoes_pagina_home.sql`; `sources/engenharia--fabrica--dotnet-core--director/Fontes/PortalDirector.{Api,Services,Repositories,Aplicacao}/...` (PortalAwsClient, CotacaoIntegradorClient, EdocController, SpedRepository, AppClientService, AcessoAplicacaoRepository, GenericPagesRepository, UtilsController).
