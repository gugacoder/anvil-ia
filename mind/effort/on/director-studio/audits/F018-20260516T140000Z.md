---
title: "F018 audit-pass (3ª auditoria pós-fix D6)"
feature: F018
auditor: archaeologist
timestamp: 2026-05-16T14:00:00Z
contract: "[[date-components]]"
diff: "commit eb492ac em workspace/director-studio (preset 'last-2-days' em RangePreset / computeRangePreset)"
previous_audit: "[[F018-20260516T130000Z]]"
---

# F018 — audit-pass (re-auditoria pós-fix D6)

## Verdict

Pass. D6 agora coberto. Sem regressão nas demais asserções reauditadas (D4, D5, D9, D11, D13, D14). D1–D3, D7, D8, D10, D15, D16 sem regressão. D12 e D17 seguem aceitos como nota.

## D6 — coberto

Fix em `packages/ui/src/lib/date-csv.ts`:

- Linha 182: `"last-2-days"` adicionado ao union `RangePreset`.
- Linhas 236–239: `computeRangePreset('last-2-days')` retorna `{from: formatISODate(addDays(today,-1)), to: formatISODate(today)}` — exatamente `[hoje-1d, hoje]`, paridade com `DateInterval` default (`useDefaultDate=true`, citação `DateInterval.js:19-27`).
- `computeRangeDateTimePreset` (linha 257–263) deriva de `computeRangePreset`, então `last-2-days` em modo datetime produz `{from:'<hoje-1d> 00:00', to:'<hoje> 23:59'}` automaticamente. Cobertura simétrica date/datetime sem código duplicado.

Consumidor que portar filtro legado `DateInterval` com `useDefaultDate=true` declara `preset="last-2-days"` no `FormFieldDateRange` e obtém o mesmo default.

## Não-regressão dos demais presets

Diff é puramente aditivo: uma branch nova no switch de `computeRangePreset`. Casos `today`, `yesterday`, `last-7-days`, `last-30-days`, `this-month`, `last-month` intocados. Verificado em date-csv.ts:229–253.

D5 (mount-effect em SingleDateInner), D9 (range `today`), D11 (range `this-month`), D13/D14 (TimeRange), D4 (allowedDates dual-format) — nenhum tocados pelo commit. Continuam passando conforme auditoria anterior.

## Recomendações de housekeeping (não-bloqueantes)

- Atualizar `routes/smoke-f018.tsx` para exercitar `preset="last-2-days"` (recomendação 2 do veto anterior; não verifiquei se foi feito — não bloqueia, é cobertura de smoke).
- Registrar no contrato `[[date-components]]` o mapping `DateInterval default ↔ preset="last-2-days"` na seção "Notas de implementação para o Studio".
- D12 e D17 ainda precisam virar nota no contrato/manifest (pendência do veto original, não deste fix).

## Resumo

F018 fechado em pass. Smith liberado da frente. Curator pode atualizar manifest com `Status=done` e registrar as divergências aceitas (D12, D17) no contrato.
