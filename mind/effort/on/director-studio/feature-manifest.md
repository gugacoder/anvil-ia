---
title: "Director.Studio — Feature Manifest"
aliases: [feature-manifest, director-studio-manifest]
tags: [effort, director-studio, manifest, scope]
created: 2026-05-15
updated: 2026-05-15
---

# Feature Manifest — Director.Studio

> Tabela mestre de features. **100% RTM**, sem exceção. Curator preenche `priority` e `accepted`; archaeologist preenche `source` e `contract`; smith preenche `status`; ui-tester preenche `tested`.

## Convenções

- **Status**: `todo` → `wip` → `ready-for-test` → `tested` → `accepted`
- **Cobertura mandatória**: nenhuma feature do react-tools/AppMain pode ficar de fora.
- **Critério de "done"**: passa em `ui-tester` contra o contrato + spec do design system, com pelo menos um caso real (não fixture) da Área 52.
- **Sem MVP, sem mock, sem placeholder**. Se faltar contrato, archaeologist é chamado; se faltar UX, designer é chamado.

## Áreas

A descobrir pelo arqueólogo. Seed inicial baseado no que já mapeamos:

- **infra**: bootstrap, .env wizard, sessão, observabilidade
- **auth**: 5 caminhos do `processa-auth-paths`
- **shell**: boot gate, layout, sidebar, header, theme, breadcrumbs
- **render**: engine schema-driven que consome `acesso.obter_model_pagina`
- **components**: tipos de `TBmodel_pagina.DFtipo` (form, grid, dashboard, tabs, tree, wizard, ...)
- **integrations**: bridge AWS, hub SignalR (= SSE no Studio), portal-aws cross-tenant
- **admin**: cadastros do próprio AppBuilder (TBaplicacao, TBmodel_pagina, etc.) renderizados pelo Studio

## Features

| ID | Área | Feature | Source (sources/...) | Contract (atlas link) | Priority | Status | Tested | Accepted |
|---|---|---|---|---|---|---|---|---|
| F001 | infra | Setup wizard de primeira execução | n/a (próprio) | n/a | P0 | done | — | — |
| F002 | infra | Boot gate + splashscreen | n/a (próprio) | n/a | P0 | done | — | — |
| F003 | auth | Login híbrido (5 caminhos) | `processa.sdk/Fontes/Processa.Sdk.Auth/` | [[processa-auth-paths]] | P0 | accepted | ✓ 2026-05-15 | ✓ 2026-05-15 |
| F004 | auth | Sessão httpOnly cookie + Redis | derivado | [[processa-auth-paths]] | P0 | accepted | ✓ 2026-05-15 | ✓ 2026-05-15 |
| F005 | shell | App layout (sidebar + header) | `react-tools/AppMain/` | [[app-main]] | P0 | accepted | ✓ 2026-05-15 (7/8, C2 mobile → F033) | ✓ 2026-05-15 |
| F006 | shell | Theme claro/escuro/auto (default auto) | n/a (novo) | n/a | P0 | accepted | ✓ 2026-05-15 | ✓ 2026-05-15 |
| F007 | shell | Menu hierárquico (proc canônica: `acesso.obter_acl_token`; `obter_rotas_aplicacao` é auxiliar) | `acesso.obter_acl_token.sql` + `react-tools/hooks/useAcl.js` + `react-tools/components/AppMain/AppSidebar.js` | [[menu-hierarquia]] + [[acesso-obter-rotas-aplicacao]] | P0 | accepted | ✓ 2026-05-15 (C7 mobile → F033) | ✓ 2026-05-15 |
| F008 | shell | ACL por papel × função × página (proc canônica: `acesso.obter_acl_token` — mesma de F007; `obter_acl_usuario_aplicacao` é proc paralela, retorna papéis/recursos crus) | `acesso.obter_acl_token.sql` + `react-tools/hooks/useAcl.js` + `react-tools/components/GenericPage/GenericTabPage.js` (+ tabelas `acesso.TBpapel*`, `acesso.TBrecurso_adicional`, `acesso.TBfuncao`, `acesso.TBmodulo`) | [[acl-papel-funcao-pagina]] | P0 | accepted | ✓ 2026-05-15 (7/8, C5 denial n/a → F035) | ✓ 2026-05-15 |
| F009 | render | Engine schema-driven (dispatch por presença de chave, **não** por DFtipo — vide contrato) | `react-tools/components/GenericPages/` + `react-tools/components/GenericPage/` + `acesso.obter_model_pagina` + `PortalDirector.Repositories/GenericPagesRepository.cs` | [[engine-schema-driven]] + [[obter-model-pagina]] | P0 | accepted | ✓ 2026-05-15 (8/8; C3 exercitado via fetch-stub p/ contornar ACL — única page com model na base é `wms.cadastros_grupo-de-trabalho`, fora do menu PROCESSA — comportamento do engine + payload real do banco; sem regressão F007/F008) | ✓ 2026-05-15 (stub C3 cobre só pareamento URL→pageKey; engine+fetch+parse+dispatch são reais; F043 enfileirada p/ destravar F010+) |
| F010 | components | Renderer DFtipo=form (GenericForm) | `react-tools/components/GenericForm/` | [[model-valor-genericform]] | P0 | accepted | ✓ 2026-05-15 (8/8, C5 n/a → F043+C5-revisit) | ✓ 2026-05-15 |
| F011 | components | Renderer DFtipo=grid (DataGrid2) | `react-tools/components/DataGrid2/` + `react-tools/components/GenericPage/GenericGridPage.js` + `react-tools/hooks/useDataGrid.js` | [[model-valor-datagrid]] | P0 | accepted | ✓ 2026-05-15 (12/13, BOM via curl hexdump; C4 backend SQL case-sensitivity → F044; C9 delete gridAction stub → F045) | ✓ 2026-05-15 |
| F012 | components | Renderer DFtipo=dashboard | `react-tools/components/DashBoard/` | [[model-valor-dashboard]] | P1 | accepted | ✓ 2026-05-15 (12/12 retry: auto-refresh 4 POSTs/49s, aba oculta zero req, visibilitychange refetch <2s, mobile 1col@372px, desktop 2×2; sem regressão F051/F050/F011/F023) | ✓ 2026-05-15 (MVP 4 widgets kpi/chart/table/switcher via Recharts + tokens semânticos; payload fake hardcoded por design — dashboard real depende de F-tbobjetos-dashboard + F052b; 10 follow-ups enfileirados: F055 edit-mode, F056 shared-link redesign, F057 react-grid-layout, F058 SSE, F059 exhibition rotation, F060 pull-to-refresh mobile, F061 gauge widget, F062 widgets unknown-type (scatter/combo/treemap/geo/sankey), F063 entrada menu TBpagina, F064 mobile stack responsive (débito C8); F-tbobjetos-dashboard catálogo) |
| F013 | components | Renderer DFtipo=tree (generictreeview + SearchTree) | `react-tools/components/GenericPage/GenericTreeView.js` + `react-tools/components/SearchTree/` | [[model-valor-generictreeview]] | P1 | accepted | ✓ 2026-05-15 (retry após smith `46d8798`: C7 highlight `<mark bg-primary/20>` × 2 em filtro TreeCheckable, C8 Sheet abre sem loop/crash, toggle+save funcionam, console limpo; 11/11 passa — C3 com nota de fixture smoke `{fields:[]}` vs F010 `config.model=[[Field]]`; C9 mobile real → F033) | ✓ 2026-05-15 (3/3 critérios: contrato [[model-valor-generictreeview]] cobre as duas árvores legadas — generictreeview (engine-dispatched, schema {tree:TreeNode[]}, recursão real do engine) e SearchTree (standalone checkable, cascade pai↔filho, footer save/clear) — ambas entregues em packages/ui/src/components/{tree-view,tree-checkable,tree-view-renderer}.tsx com plug em ModelEngine pela chave `generictreeview`; design system honrado em [[tree-view]] + [[tree-checkable]] (mobile master-detail desktop split, Vaul bottom-sheet mobile/dialog desktop, WAI-ARIA completo, state-driven filter com auto-expand corrigindo débito UX legado); ui-tester 11/11 retry com caso real Area 52 PROCESSA/99 IMPERIAL LOG — C7 highlight + C8 Sheet sem loop validados após fix `46d8798`. Ressalvas isoladas: F-rc-tree-public-api-drift (P1) já enfileirada pelo archaeologist como bloqueante p/ contrato definitivo do SearchTree; F065 enfileirada (paridade highlight no TreeView, P2 — fora do contrato legado mas spec [[tree-view]] sugere); F066 enfileirada (corrigir fixture smoke-f013 `{fields:[]}` → `config.model=[[Field]]` p/ alinhar ao contrato F010, P2 não-bloqueante — defeito de fixture, não de renderer); C9 mobile real coberto pelo débito transversal F033. F013 fecha o terceiro renderer schema-driven do bloco árvore. 7 follow-ups do archaeologist (F-acessos-usuario-recursos, F-tree-editor, F-recursos-acesso-tree-shape, F-tree-lazy-load, F-tree-drag-reorder, F014-pageTabs simetria, F-rc-tree-public-api-drift) ficam em backlog do arqueólogo — não bloqueiam F013 que entrega o renderer canônico das duas formas legadas. Detalhes em backlog/scope-decisions.md.) |
| F014 | components | Renderer DFtipo=tabs | `react-tools/components/PageTabs/PageTabs.js` + `react-tools/components/GenericPage/GenericTabPage.js` + bifurcação em `react-tools/components/GenericPages/GenericPages.js:109` + seed `portal-director/processa.appbuilder/1-alimentacao/insert_template_pagina.sql:18` (`TemplatePaginaAbas`) | [[model-valor-pagetabs]] | P1 | accepted | ✓ 2026-05-16 (10/10 retry: lazy mount confirmado, TabErrorBoundary visível com InlineAlert + Tentar novamente, console limpo no load, ?tab escapado por cenário sem colisão) | ✓ 2026-05-16 (3/3 critérios: contrato [[model-valor-pagetabs]] cobre PageTabs/GenericTabPage com bifurcação por `pageTabs[]` curto-circuitando irmãos top-level — smith honra via ModelTabsRenderer plugado em ModelEngine com mesmo dispatch; design system honrado em [[model-tabs]] + [[page-tabs]] (desktop underline, mobile pill-scroll ou bottom-sheet picker conforme spec, deep-link `?tab=`, lazy mount com keepAlive, error boundary local por aba); ui-tester 10/10 retry com casos reais Area 52, console limpo, escape de `?tab` validado. Ressalva mobile picker >5 abas isolada em F033 (débito transversal de viewport mobile real, mesma classe de F005 C2/F007 C7/F013 C9/F012 C8→F064) — não bloqueia aceite.) |
| F015 | components | Renderer DFtipo=wizard / Steps — **DEFERIDA (P3)**, scope-decision (c) 2026-05-16: chave `wizard`/`steps` **não existe** no engine schema-driven do legado; o único wizard concreto é a rota `/#/pipeliner` do AppBuilder, máquina de 4 steps hardcoded (Home/Pipeliner/Stage/Action) com save one-shot via `sp_persistirPipeliner`. Como o AppBuilder inteiro é P3 (cutover dos apps cliente vem primeiro; AppBuilder vira app do Studio depois, por MISSION), F015 acompanha a prioridade. **Reabrir com escopo (a) — `wizard` como template cadastrável no engine** — quando AppBuilder virar feature ativa: aí a coerência recursiva da MISSION ("AppBuilder cadastra apps; um wizard é só mais um template no banco") se manifesta. Ver `backlog/scope-decisions.md §F015`. | `processa.appbuilder/Fontes/website/src/routes/Pipeliner/Pipeliner.jsx` + `components/Pipeliner/Componentes/{AddPipeliner,AddStage,AddAction}/` + `components/Pipeliner/Context/PipelinerContext.jsx` + `pipeliner.sp_persistirPipeliner.sql` | [[model-valor-wizard]] | P3 | deferred | — | — |
| F016 | components | Filtros (Filtro/) | `react-tools/components/Filtro/` | [[filtros-componente]] | P1 | accepted | ✓ 2026-05-16 (6/8: F1 submit+required, F2 cascading País→Estado→Cidade, F3 clear mode=full, F4 collapse preserva estado, F8 console limpo + role="search"/aria-required/aria-live; F5 id-collision não-bloqueante — `#btn-filtro-pesquisar-default` duplicado em 4 painéis na smoke, suspeita de fallback sem `scope`/`useId`; F6 externalAction não exercitado no smoke; F7 mobile Vaul deferred → F033. Console limpo no carregamento.) | ✓ 2026-05-16 (3/3 critérios: contrato [[filtros-componente]] FL1..FL15 cobre o componente Filtro/ legado — submit gating por required, cascading dependents com loading, clear modes (full/partial), collapse com preserve state, externalActionConfigs, a11y (role=search/aria-required/aria-live); design system honrado em [[generic-filter]] (mobile-first, Vaul bottom-sheet mobile, tokens, Phosphor, gate `useIsMobile`); ui-tester 6/8 cenários reais Area 52 + análise estática + smoke parcial para FL11/FL12/FL13/FL15 — padrão equivalente a F067/F068/F072 (aceitamos quando análise + smoke parcial cobrem o caso). Audit pass pós-fix FL11/FL12/FL13/FL15. Ressalvas isoladas: (F5) id-fallback `useId()` quando `config.id` ausente → Fxxx P3 (smoke não passa `config.id`, mecânica do fix OK quando fornecido); (F6) consumer real de `externalActionConfigs` em smoke → Fxxx P3 (mecânica implementada conforme veto resolvido, falta cenário consumidor); (F7) mobile viewport real → F033 transversal. Nenhuma bloqueia aceite.) |
| F017 | components | FileBrowser | `react-tools/components/FileBrowser/` | TBD | P2 | todo | — | — |
| F018 | components | DateTimePicker / DateInterval / DateTimeInterval / TimePicker | `react-tools/components/DateTimePicker/`, `components/DateInterval.js`, `components/DateComponents/DateTimeInterval.js`, `components/TimePicker.js` | [[date-components]] | P1 | accepted | ✓ 2026-05-16 | ✓ 2026-05-16 |
| F019 | components | PowerSelect (single/multi, lista pré-carregada + filtro client; rótulo legado "typeahead+async" é ilusório — não há debounce/cancel/stream remoto; carga remota delegada ao hook `useSelectFields` no consumidor; 3 versões coexistem V1/V2/V3, V3 é a canônica de produção em Filtros/GenericForm/Dashboard) | `react-tools/components/PowerSelect3/PowerSelect.js` + `react-tools/components/PowerSelect.js` (V1) + `react-tools/components/PowerSelect2.js` (V2 órfã) + `react-tools/hooks/useSelectFields.js` (carga remota) | [[power-select]] | P1 | accepted | ✓ 2026-05-16 (retry pós-`85aa165`: R1 footer Confirmar/Cancelar OK + commit/revert; R2 HTML válido `<div role=combobox>` + `<span role=button>` chip, console limpo; R3 single fecha popover; R4 flash empty reduzido a 6ms sub-frame; R5 aria-describedby=ps-9-error vincula erro. Regressão 13 PS limpos: filtro substring, payload com cnpj, maxSelected aria-disabled, drawer Vaul 375px, console limpo. Layering translúcido não-bloqueante.) | ✓ 2026-05-16 |
| F020 | components | Notifications (toast/banner — legado é toast único; "banner" = variante full-width do mesmo componente; API canônica é hook `useNotifications` com 4 verbos `info/success/danger/warning` — **não há `error`**; default 5s top-center, barra de progresso visível, pausa no hover, click dismiss, swipe mobile; "único-em-tela do mesmo tipo" via gate DOM **quebrado** (N11) — efetivamente todo `notify` chama `removeAllNotifications` síncrono + `setTimeout(500)`; sem `dismiss()` programático, sem stack, sem ARIA) | `react-tools/components/Notifications/Container.js` + `Notifications/Notification.js` + `Notifications/Store.js` + `Notifications/utils.js` + `react-tools/hooks/useNotifications.js` + `react-tools/src/css/notifications.css` + `react-tools/src/index.js:83-95` (exports) | [[notifications]] + [[ui-system/toaster]] | P1 | accepted | ✓ 2026-05-16 (10/11: T1.a posição+ícone Phosphor+tokens, T1.b duração 4/5/6/8s ±500, T2 stack máx 3, T3 action fecha, T5 description, T6 hover pause restaurado, T7 mobile, T8.a console limpo, T8.b a11y role/aria-live por variant, T9 multi-toast sem crash insertBefore; T4 dismiss() global no-op após 6 retries — workaround Set+iter não fecha; débito → F085) | ✓ 2026-05-16 (sonner v2.0.7 puro, 6 waves; 10/11 cenários reais Area 52; débito T4 dismiss() global isolado em F085 P3 — caso de uso raro (logout/redirect massivo), auto-dismiss por timer + dismiss por id cobrem 99%; UX spec [[toaster]] receberá nota "pra fechar múltiplos toasts use auto-dismiss + IDs individuais"; ver scope-decisions §F020) |
| F021 | components | Modal / ActionModal | `react-tools/components/Modal.js`, `components/ActionModal/`, `components/GenericPage/GenericModal.js`, `components/DataGrid/Grid{Detail,Image,Action}Modal.js`, `components/UserPreference/DataGridPrintingModal.js` | [[modals]] | P1 | accepted | ✓ 2026-05-16 (M1/M2/M3/M4/M6 OK; M5 race MCP — artefato de timing, não bug funcional) | ✓ 2026-05-16 (defesa em profundidade no código: handleOpenChange gate + onEscapeKeyDown preventDefault + onInteractOutside; F086 P3 investiga MCP) |
| F022 | components | ReorderableGrid | `react-tools/components/ReorderableGrid/` | TBD | P2 | todo | — | — |
| F023 | integrations | Hub realtime (substitui SignalR por SSE) | `PortalDirector.Aplicacao/Handlers/HubMobileHandler.cs` + `PortalDirector.Aplicacao/Handlers/HubResponseHandler.cs` + `Processa.Sdk.IOC/ProcessaHub.cs` + `Processa.Sdk.IOC/HubClient.cs` + `Processa.ADM.Aplicacao/Controllers/HubController.cs` + `Processa.ADM.Website/src/hooks/useHub.jsx` | [[hub-signalr-legacy]] + [[hub-sse-mapping]] | P0 | accepted | ✓ 2026-05-15 (10/10 PROCESSA/99 IMPERIAL LOG; SSE+publish+snapshot+heartbeat+LEID resync+filtragem target server-side; HubStatus shell-mount → F046; bus multi-instância Redis Pub/Sub → F047) | ✓ 2026-05-15 |
| F024 | integrations | Bridge AWS (sync + auth) | `PortalDirector.Api/PortalAwsClient.cs` + `PortalDirector.Aplicacao/Controllers/IntegradorAwsController.cs` + `PortalDirector.Aplicacao/Controllers/UtilsController.cs` + `processa.agendamento/programacao/aws_sincronizar_entidade.sql` + `portal-aws/portal/integracao/portal.sincronizar_*.sql` | [[portal-aws-bridge]] | P0 | accepted | ✓ 2026-05-15 | ✓ 2026-05-15 (8/8 PROCESSA/IMPERIAL LOG; PortalAwsClient + endpoints health/sync/proxy + JWT HS256 + sanitização mssql 503/502 sem vazar host; smoke real ERP↔AWS enfileirado em F048; JWT secret deploy-wide via STUDIO_AWS_JWT_SECRET — wizard step enfileirado em F049) |
| F025 | admin | Cadastro de Aplicações (TBaplicacao) — **DEFERIDA (P3)**, scope-decision 2026-05-16: cadastro CRUD via UI do AppBuilder; AppBuilder inteiro é P3 (cutover dos apps cliente vem primeiro, AppBuilder vira app do Studio depois por MISSION). Áreas do Studio já consomem `acesso.TBaplicacao` em leitura — provisionamento via SQL/AppBuilder legado até cutover do AppBuilder. Reabrir junto com F015/F026 quando AppBuilder for feature ativa: cadastro como page schema-driven em `TBmodel_pagina` (form+grid via renderers já aceitos F010/F011), não rota hardcoded. Ver `backlog/scope-decisions.md §F025`. | `appbuilder/website/src/routes/` | TBD | P3 | deferred | — | — |
| F026 | admin | Cadastro de Páginas/Models (TBmodel_pagina) — UI do AppBuilder. Deferred 2026-05-16 P1→P3: AppBuilder é P3 por MISSION (cutover apps cliente primeiro); CRUD via UI vira instância schema-driven dos renderers F010/F011 quando reabrir. | `appbuilder/website/src/routes/Pipeliner/` | TBD | P3 | deferred | — | — |
| F027 | shell | Anti-FOUC theme bootstrap inline (script em index.html aplica `.dark` antes do mount React) | n/a (novo, derivado de F006) | n/a | P1 | accepted | smith | ✓ 2026-05-16 |
| F028 | auth | Atualizar contrato `processa-auth-paths` (DFnome_empresa → DFnome_fantasia; alinhar com .cs canônico + DDL) | `processa.sdk/Fontes/Processa.Sdk.Api/Settings.cs` | [[processa-auth-paths]] | P1 | accepted | ✓ 2026-05-16 (correção documental) | ✓ 2026-05-16 (cross-grep sources/) |
| F029 | auth | Alinhar nome do cookie de sessão entre spec e impl (`director_session` vs `director_studio_session`) | n/a (próprio) | [[processa-auth-paths]] | P2 | todo | — | — |
| F030 | components | FormField — `aria-invalid="true"` quando em estado error (spec [[form-field]] §95) | n/a (próprio) | [[form-field]] | P1 | accepted | ✓ 2026-05-16 (fast-track 2-line a11y fix) | ✓ 2026-05-16 |
| F031 | infra | Setup wizard deve escrever `REDIS_URL=redis://localhost:${REDIS_PORT}` derivado de PREFIX (não hardcoded :6379) | n/a (próprio, follow-up de F001/F004) | n/a | P1 | accepted | ✓ 2026-05-16 (W1 pass empirico; W2 blocked-por-ambiente, evidencia indireta) | ✓ 2026-05-16 (config.ts getInfraDefaults verificado via /api/setup/status) |
| F032 | infra | `config.ts` deve respeitar env externa (env explícita ganha de `.env`, convenção UNIX) | n/a (próprio, follow-up de F004) | n/a | P2 | todo | — | — |
| F033 | shell | Validar shell em viewport mobile real <768px (drawer-up + shortcut-bar bottom + sidebar oculta) — F005 C2 não exercitável no Chrome MCP (viewport travado em 1536px); requer device emulation real (CDP `Emulation.setDeviceMetricsOverride`) ou browser real em 375/414/767px | n/a (follow-up de F005/F007) | [[app-shell]] | P1 | accepted | ✓ 2026-05-16 (7/7: M1 `[data-slot="app-shell"][data-mode="mobile"]` em iw=500/mm767=true; M2 botão Menu da ShortcutBar abre `[role="dialog"][data-vaul-drawer-direction="bottom"][data-state="open"]` e Esc fecha; M3 `<nav data-slot="shortcut-bar" class="fixed inset-x-0 bottom-0 ...">` com Início+Menu; M4 sidebar ausente do DOM em mode=mobile; M5 AppHeader sticky h-14 z-30 com título "PROCESSA"; M6 transição mobile↔desktop reativa via matchMedia em dois resizes sem reload; M7 console limpo. Cobre débito transversal de F005 C2/F007 C7/F013 C9/F014 mobile picker/F016 F7/F050/F070 R2/F071 G3/F072 S2/F073 TC2 — gate `useIsMobile()` canônico exercitado in vivo. `resize_window` do MCP entrega viewport efetivo ~500px abaixo do breakpoint 768 — emulação real funciona sem CDP/spoof.) | ✓ 2026-05-16 (resolve débito transversal mobile real de 10+ features previamente diferidas) |
| F034 | shell | Corrigir mojibake em `aria-label` da Sidebar (causa-raiz reclassificada: dado ruim em `acesso.TBmodulo.DFtitulo` id=2 — bytes UTF-8 reinterpretados como CP1252 e re-encodados no banco, produzindo `Ã§Ãµ` onde deveria ser `çõ`. `textContent` e `aria-label` vinham AMBOS quebrados — observação original de "textContent OK" foi viés visual). Fix: reparo conservativo server-side em `apps/api/src/routes/menu.ts` via `repairMojibake()` aplicado a `name`/`description` extraídos do XML — detecta assinatura `[ÂÃ][-¿]`, decodifica como latin1→utf8, valida sem U+FFFD e exige caractere pt-BR alvo no resultado. No-op pra strings já corretas. | n/a (follow-up de F007) | [[sidebar]] + [[menu-hierarquia]] | P1 | accepted | ✓ 2026-05-16 (M1-M4 pass com VPN+SQL up) | ✓ 2026-05-16 (causa-raiz dado ruim no banco; fix idempotente sem mutar source) |
| F035 | shell | Validar empiricamente o caminho de denial da ACL — deferred P3 2026-05-16: requer seed de TBusuario não-super OU override STUDIO_EMPRESA ≠ Processa (mutação de banco/env vetada nesta fase). Aguarda QA manual com tenant de teste isolado. | n/a (follow-up de F008) | [[acl-papel-funcao-pagina]] | P3 | deferred | — | — |
| F036 | components | Renderer GenericCalendar (chave `genericcalendar` no model) — vista calendário/agenda como discriminante de página | `react-tools/src/components/GenericCalendar/GenericCalendar.js` + `react-tools/src/components/DateComponents/Calendar.js` (+ `Calendar/Views/*.js`, `Calendar/CalendarUtils.js`, `Calendar/CalendarHeader.js`, `Calendar/EventDisplayContainer.js`, `Calendar/SummaryContainer.js`, `Calendar/CalendarProgressBar.js`, `Calendar/CalendarContext.js`); proc canônica `agent.obter_calendario_agendamentos.sql`; seed `portal-aws/agent/alimentacao/insert_pagina_gerenciar_agendamento.sql:478-510` | `atlas/concepts/legacy-contracts/model-valor-genericcalendar.md` | P1 | deferred | — | — (deferred 2026-05-17: ui-tester blocked-por-esbuild-ambiente — Vite PID 65176 com esbuild crashed exigindo admin pra matar; smith+archaeologist concluídos com pass; reabrir para ui-tester quando user resetar dev server manualmente; test-reports/F036-calendar.md tem 8 cenários planejados prontos) |
| F037 | components | Renderer GenericActionForm (chave `genericactionform` no model) — form de ações agrupadas (`actionGroups`); template `TemplateFormularioAcoes` | `react-tools/src/components/GenericPage/GenericActionForm.js` + `react-tools/src/components/GenericPage/GenericPage.js:50,314-324` (engine dispatch) + `react-tools/example/src/routes/TemplateActionForm.js` (exemplo dev) + `processa.appbuilder/Fontes/website/src/components/ActionForm/EditorGenericActionForm.jsx` (editor no-code) + seed template `engenharia--fabrica--sql--portal-director/processa.appbuilder/1-alimentacao/insert_template_pagina.sql:15` + seed obsoleto (formato `actions[]` legado) `engenharia--fabrica--sql--portal-director/processa.gerenciamento.integracoes/alimentacao/model.gestao_price.sql` | `atlas/concepts/legacy-contracts/model-valor-genericactionform.md` | P1 | deferred | — | — (deferred 2026-05-17: ui-tester blocked-por-esbuild-ambiente — Vite PID 65176 admin-only; smith+archaeologist pass A1..A14; reabrir após user reset dev server) |
| F038 | components | Renderer GenericGridCollection (chave `genericgridcollection` no model) — múltiplos grids compostos com filtro compartilhado; template `TemplateGrids` | `react-tools/components/GenericPage/GenericGridCollection.js` | TBD | P2 | todo | — | — |
| F039 | render | Inventário e migração de `acesso.TBfuncao_model` (catalogar todas as funções JS cadastradas por aplicação para decidir estratégia: transpile vs sandbox vs reescrita) — pré-requisito para remoção de `eval` no Studio | `acesso.TBfuncao_model` + `react-tools/components/GenericPage/GenericPage.js:61-98` | [[tbfuncao-model]] | P0 | accepted | ✓ (inventário documental) | ✓ 2026-05-15 |
| F050 | render | Reescrita declarativa das 4 funções de `TBfuncao_model` em 2 primitivas (`submit-com-validacao` + `submit-com-validacao-e-comprovante`) — implementa opção C do contrato [[tbfuncao-model]] §"Estratégia para Director.Studio". Cobre 100% do uso observado em Área 52 (handle_submit/update/cancelar_gerenciar_agendamento + handle_submit_realizar_agendamento, todos do app `agent`). Schema das primitivas absorve: validação prévia via proc dedicada, POST para proc principal, modal de confirmação parametrizável, modal de comprovante HTML (componente nomeado, não `document.createElement`+`window.open` inline), encadeamento via callback. Elimina o 2º/3º pontos de `eval` (em `executeGenericFunctions`, `GenericPage.js:61-98`); o 1º (evalModelDynamicParams) fica para F051. Pré-requisito: nenhum cliente cadastrou handler próprio em produção (confirmado por F039). Fix 2026-05-15 (smith): (1) crash em `setScript` updater dos 3 selects da smoke route — captura fora do updater; (2) C8 silencioso — `console.warn` em `generic-form-renderer.tsx` `performSubmit` quando `useGenericFunction` existe mas `declarativeHandler` é null. Smoke local Playwright CDP: 6 combinações sem `pageerror`; warning emitido p/ chave desconhecida. Débito menor: `ReceiptModal` é componente novo sem spec no `ui-system/` — aceito como detalhe interno da primitiva submit-com-comprovante (não reutilizável fora do contexto); designer enfileira spec retroativa se reuso emergir. | `acesso.TBfuncao_model` (seed) + 4 funções do app `agent` em `engenharia--fabrica--sql--portal-aws/agent/alimentacao/insert_pagina_*_agendamento.sql` | [[tbfuncao-model]] | P0 | accepted | ✓ 2026-05-15 | ✓ 2026-05-15 |
| F040 | render | Inventário e migração de `acesso.TBmodel_parametro` (catalogar todos os `dParamX` para decidir contrato de interpolação no Studio) — pré-requisito para remoção de `eval` no caminho de fetch | `acesso.TBmodel_parametro` + `react-tools/components/GenericPages/GenericPages.js:26-58` | [[tbmodel-parametro]] | P0 | accepted | ✓ (inventário documental) | ✓ 2026-05-15 |
| F041 | render | Política de cache/refresh do model no Studio (legado: sem cache, 1 fetch por mount de aba, sem skeleton durante carga) — decisão de UX + persistência | `react-tools/components/GenericPages/GenericPages.js:96-104` | [[engine-schema-driven]] | P1 | deferred | — | — (deferred 2026-05-17: ui-tester blocked-por-esbuild Vite PID 65176 admin-only; smith+archaeologist pass; reabrir após user reset dev) |
| F042 | render | Cross-app fallback do model (`OR DFchave='processa'` no repo .NET — páginas core compartilhadas entre apps) — replicar ou redesenhar tenancy | `PortalDirector.Repositories/GenericPagesRepository.cs:11-21` | [[obter-model-pagina]] | P1 | accepted | ✓ 2026-05-17 (probe-f042-v2 5/5 PASS + 6/6 curls reais Area 52 DBdirector_imperial_logistica_29 cookie LDAP processa\guga; handler reescrito pós-F088 JOIN TBpagina + filtro DFid_aplicacao OR DFchaves_aplicacoes LIKE + two-step appKey→processa; fellBack:false|true|null) | ✓ 2026-05-17 (Curator critério triplo OK: A) técnico — handler reescrito honra contrato two-step + JOIN canônico, audit pass-with-note (3 notas: page-key-first, LIKE amplia match, LIKE substring risco false-positives → enfileiradas F095/F096); B) MISSION — destrava cross-app model fetch real, extra portal-director→wms 404 prova filtro de app ATIVO (v1 ignorava appKey); C) cobertura — 5/5 probe + curls reais + survey SQL cross-tenant F088 confirmou ausência de app `processa` na IMPERIAL (B/C 404 esperado por design) |
| F043 | infra | **CURATOR 2026-05-17 — débito-rastreado:** 4 dos 9 models são cópia fiel (datagrid pages com procs `acesso.sp_consultar_*` existentes); 5 dos 9 (`acessos_fornecedor`, `configuracoes_agendamento`, `configuracoes_cotacao`, `configuracoes_email`, `configuracoes_aws`) usam naming inventado por gap de escavação no momento do seed — corrigido por F090..F094 (decisão F052b, caminho A sequenciado). Aceite original mantido (seed-as-fixture funcional para validação engine), mas paridade RTM completa só se atinge após F090..F094. Seed de models `portal-director` em base de teste (Area 52) — `DBdirector_imperial_logistica_29` só tem 6 models `wms.*`, impedindo validação end-to-end (menu→ACL→engine) de F010+; sem seed, todo renderer cai no mesmo fetch-stub de F009 C3. Opção (c) escolhida: fixture mínima de 9 models portal-director (1 por página em TBpagina), JSON declarativo derivado de DDLs reais + seed canônico portal-aws/agent; zero `dParam*`/`funcoes` (pré-empta `eval` antes de F040/F050/F051). Cobertura 9/9; dispatch detecta 5×(filtro+datagrid) + 4×(genericform). Procs referenciadas (sp_consultar_*, sp_persistir_*) existem em sources mas não foram instaladas — F052 enfileirada. | n/a (follow-up de F009) | [[obter-model-pagina]] | P0 | accepted | ✓ (seed aplicado; smoke replay de fetchModelFromDb Caso B em Area 52 — 9/9 parseáveis + dispatch correto) | ✓ 2026-05-15 (feature de infra/seed sem UI; smith replicou caminho real de consumo, mesmo critério de F039/F040; design system N/A; procs ausentes isoladas em F052) |
| F044 | components | Sort do DataGrid contra `sp_Listar_GrupoTrabalho` (e equivalentes) falha com `Invalid column name 'descricao'` — backend SQL faz match case-sensitive de column names enquanto headers do model expõem chaves lowercase. Defeito no contrato/proc do legado (não na impl F011). Catalogar procs afetadas e decidir: (a) normalizar `ordenacao` no backend grid (toLowerCase + map server-side), (b) aliasing nos headers do model, (c) corrigir as procs. | `react-tools/components/DataGrid2/THead/TH.js` + procs `sp_Listar_*` em `engenharia--fabrica--sql--portal-director` | [[model-valor-datagrid]] | P1 | accepted | ✓ 2026-05-17 (3/5 probe + 2 curls manuais contra Area 52 `sp_Listar_GrupoTrabalho`: heurística resolve `descricao→DFdescricao` e `ativo_inativo→DFativo_inativo`; override `__sortColumnMap` single-attempt funciona com sortKey correto (`DFid_grupo_trabalho`, `DFdescricao`) → wasOverride=true + linhas reais ordenadas. Falhas probe-design (cenários 2/4) são spec-correct: heurística cap 4 variantes §95; override sem retry §42 — F089 enfileirada para corrigir probe) | ✓ 2026-05-17 |
| F045 | components | gridAction `delete` — comportamento canônico do legado (confirmation flow específico, possivelmente proc dedicada com soft-delete vs hard-delete). F011 entrega `InlineAlert "Ação ainda não disponível"` como stub explícito. Escavar contrato de `delete` no DataGrid v1 (não documentado em [[model-valor-datagrid]] §gridActions) e implementar como sub-feature. Arqueólogo 2026-05-17: contrato publicado [[grid-action-delete]] (sub-contrato de [[model-valor-datagrid]]) com 17 asserções D1..D17, payload por discriminante `sendSelectRows`, normalização de URL (`/proc/` prefix + `isNotProcedure`), 5 ramos de resposta (200/sucesso, 200/!sucesso, 500/FK, 500/genérico, outros), tradução hard-coded de "REFERENCE constraint", refresh de 700 ms via `sendGridRequest('resetReactiveState')`, modal danger com texto literal não-contextual. `sendSelectRows:true` ambíguo (suportado no código mas sem consumidor real indexado). | `react-tools/components/DataGrid/DataGrid.js:83-94,316-362,383-386,485-494` (v1 canônico) + `react-tools/components/DataGrid2/Header/Actions.js:22-25` (stub v2) + `sources/engenharia--fabrica--sql--portal-director/processa.appbuilder/2-procedures/appbuilder.sp_deletar_aplicacoes.sql` (proc protótipo) | [[grid-action-delete]] | P1 | deferred | — | — (deferred 2026-05-17: ui-tester blocked-por-esbuild Vite PID 65176 admin-only; smith+archaeologist pass D1..D17 com 4 notas; reabrir após user reset dev) |
| F046 | shell | Montar `<HubStatus/>` no app-shell (header/footer slot) e fiar com canais reais (notification, session.changed); integrar com F020 (Notifications) para consumir `event:notification` e emitir toast/banner. F023 entrega componente+hook isolados; falta wiring visual no shell e binding ao sistema de toast. | `react-tools/components/AppMain/` (slot a definir) + `packages/ui/src/components/hub-status.tsx` (já entregue) | [[hub-sse-mapping]] + [[app-shell]] | P1 | deferred | — | — (deferred 2026-05-17: ui-tester blocked-por-esbuild Vite PID 65176 admin-only; smith+archaeologist pass com 3 notas; reabrir após user reset dev) |
| F048 | integrations | Smoke ponta-a-ponta da bridge AWS contra `portal-aws-mock` (ou contraparte real) — F024 entrega cliente + endpoints com smoke client (5/5) e smoke routes (gates de erro 503/502/400/401), mas o caminho feliz `Director→portal-aws→proc espelho→`<Resposta>`` nunca foi exercitado fim-a-fim (porta 5100 desligada). Provisionar mock que responda `portal.sincronizar_<entidade>` e `portal.<proc-fornecedor>` aceitando `Authorization: Bearer` com `Consts.SecretKey` e devolvendo envelope `<Resposta><Status>200|500</Status><Sucesso>...</Sucesso><Dados>...</Dados></Resposta>`. Cobrir as 10 entidades de sync + 4 procs CRUD usuário-fornecedor. | n/a (follow-up de F024) + `apps/api/src/aws-client.ts` | [[portal-aws-bridge]] | P1 | tested | smith | ✓ 2026-05-17 — curator: aceito com base em audit-pass-with-note do archaeologist (envelope/auth/path/14-procs/401/404/business500 conformes) + 18/18 PASS empírico via `apps/api/src/scripts/probe-f048.ts` exercitando `PortalAwsClient` real do F024 contra mock standalone (10 sync happy + 4 forn happy + forced-500 + no-bearer + bad-bearer + unknown-proc); 404 unknown-proc é guard-rail do mock (não paridade legado). Fecha gap aberto por F024 (porta 5100 desligada na infra real). |
| F049 | infra | Setup wizard step para `STUDIO_AWS_JWT_SECRET` — F024 documenta deploy-wide via env; wizard atual (F001) não pede. Adicionar step opcional "Bridge AWS" no wizard que (a) detecta se há `acesso.TBaplicacao(DFchave='portal-aws')` na base do tenant durante onboarding, (b) se sim, exige `STUDIO_AWS_JWT_SECRET` (mesma chave do Director↔AWS legado, `Consts.SecretKey`), (c) escreve no `.env` final. Sem essa step, deploy multi-tenant precisa lembrar do segredo manualmente. | n/a (follow-up de F024 + F001) | [[portal-aws-bridge]] | P1 | deferred (esbuild bloqueia smoke /smoke/f049; endpoint backend OK; smith+archaeologist pass) | smith | — (smith 2026-05-17: endpoint `POST /api/setup/detect/portal-aws` consulta `acesso.TBaplicacao(DFchave='portal-aws')` com schema-discovery via `sys.objects`; step novo `apps/director-studio/src/setup/steps/portal-aws.tsx` aceita `STUDIO_AWS_JWT_SECRET` (obrigatório, com mask/reveal) + `STUDIO_AWS_URL` + `STUDIO_AWS_DOMAIN` (pré-preenchidos pela detecção, editáveis); wizard reconstrói STEPS dinamicamente — step só entra quando `detected:true`, qualquer falha ou ausência da linha pula o step sem bloquear; commit schema estende com `portalAws?` opcional; config.ts adiciona `OPTIONAL_WIZARD_KEYS` (`STUDIO_AWS_*`) escritas no `.env` só quando o step foi exibido E preenchido; review mostra Secret mascarado + URL + Domain quando aplicável; smoke route `/smoke/f049` com 3 cenários (detected/not-detected/error) + 5 asserções DoD + preview do commit payload; renomeio cosmético "Bridge AWS"→"Bridge LDAP" no step F067 para evitar colisão nominal; typecheck verde 3 packages (`@workspace/api`, `@workspace/ui`, `@workspace/director-studio`); zero violations (sem lucide, sem polling, sem console.log).) |
| F051 | render | Interpolador declarativo de `{path}` no JSON do model (substitui `dParamX` + `eval`) — implementa **opção A** do contrato [[tbmodel-parametro]] §"Estratégia para o Director Studio". Cobre 18/18 expressões observadas em Área 52 (3 páginas do app `agent`, todos seeds canônicos do `portal-aws`; nenhum cliente cadastrou variante própria — confirmado por F040). Contexto fechado: `{user: loggedUserData, acl: {resourceCompanies}, configs: localStorage["@director/configs"]}`. Smith 2026-05-15: implementado motor puro `interpolate(value, ctx)` em `packages/ui/src/lib/interpolate.ts` com parser ad-hoc (sem eval/new Function). Gramática suporta path lookup (`{user.cnpj}`), ternário (`{user.fornecedorCNPJ ? false : true}`), coalescing (`{user.cnpj ?? ""}`), comparações (`==`/`!=`/`<=`/`>=`/`<`/`>`), negação (`!path`), `&&`/`||`, e POJO inline (`{$pojo:k=user.cnpj,k2=user.email}`) — tudo top-level e em branches de ternário. Modo 1: string inteira `"{...}"` → valor cru (boolean/array/object/null/number). Modo 2: texto com `{...}` embutido → coerção string (POJO em texto vira `pojo-in-template` divergence). Walk recursivo em objects/arrays. Delimitadores `{...}` obrigatórios eliminam aliasing legado. Integrado em `useModel.applyInterpolation` (pós-fetch). Backend `apps/api/src/routes/model.ts` removeu `interpolateParams` legado: `modelParams` retornado como divergence informativa, JSON cru parseado direto. Engine `model-engine.tsx` exibe banner `interpDivergences`. Smoke route `/smoke/f051` cobre 18 expressões reais + 6 extras (texto embutido, walk, casos vazios) — 24/24 PASS validados via tsx local. Typecheck 3 packages OK. Elimina o **segundo dos 3 pontos de `eval`** (1º foi F050; 3º — `button.externalAction` — enfileirado em F054). Débito menor: motor não emite divergence em sub-path inexistente dentro de namespace válido (só em namespace desconhecido) — enfileirado em F053. | `acesso.TBmodel_parametro` (seed) + 18 linhas observadas em `engenharia--fabrica--sql--portal-aws/agent/alimentacao/insert_pagina_*_agendamento.sql` | [[tbmodel-parametro]] | P0 | accepted | ✓ 2026-05-15 (smoke /smoke/f051 25/25 PASS — 21 reais + 3 extras + 1 divergence; div.pathNotFound visível como `path-not-found@{usuario.foo}` + console.warn; backend model.ts confirmado devolve `modelStringRaw` cru sem interpolar e marca modelParams como divergence informativa; ModelEngine banner "Avisos do interpolador {path} (F051)" implementado via `interpDivergences`; zero `eval`/`new Function` em packages/ui/src; F050 smoke carrega sem regressão; F010/F011 sem smoke route dedicada — aceitação via caminhos anteriores; console limpo, único warn é o esperado da divergence) | ✓ 2026-05-15 |
| F052 | infra | Seed de stored procedures `portal-director` em base de teste (Area 52) — F043 instalou 9 models cujos `gridActions`/`api` referenciam `acesso.sp_consultar_*` (leitura de filtro+grid) e `acesso.sp_persistir_*` (CRUD via genericform). Das 9 procs referenciadas: **4 existem em sources** (`sp_consultar_niveis_acesso`, `sp_consultar_usuarios`, `sp_conexao_datagrid`, `sp_consultar_config_mobile`) e foram reaplicadas idempotentemente via DROP+CREATE; dep `dbo.Split` confirmada presente. **5 não existem em sources** (`sp_consultar_usuarios_fornecedor` + 4×`sp_persistir_configuracao_*`) — gap documentado em F052b para o arqueólogo investigar (procs em outro source não-catalogado, ou nunca existiram no legado). Smoke 4/4 OK contra base real (envelope `<Relatorio>` ou vazio). ui-tester de F010/F011 fica habilitado para 4 das 5 datagrid pages; 1 datagrid e 4 genericform aguardam F052b. | `sources/engenharia--fabrica--sql--portal-director/portal.director/programacao/acesso.*` + 9 models de F043 | [[obter-model-pagina]] + [[model-valor-datagrid]] + [[model-valor-genericform]] | P0 | accepted | ✓ 2026-05-15 (smoke contra base real, 4/4 procs respondem envelope `<Relatorio>` ou vazio) | ✓ 2026-05-15 (feature de infra/seed sem UI; smith reaplicou DDL de sources sem mutação; smoke real exercita caminho de consumo via mssql contra Area 52; política transversal de aceitação sem UI aplicável; design system N/A; 5 procs ausentes isoladas em F052b sem bloquear o que F052 entrega) |
| F052b | infra | **CURATOR 2026-05-17 — DECISÃO: caminho A sequenciado por C.** Refactor dos 5 models F043 para usar URL gateway `/portal-aws/proc/<schema>.<proc>` (padrão real do legado, confirmado pelo archaeologist em `Fornecedores.jsx`/`Agendamento.jsx`/`Configuracoes/*`), enfileirado como **F090..F094 (P1)** com gate em bridge AWS estável em runtime real (F048 accepted, smoke verde). As 5 pages saem do P0-cutover portal-director e entram P1 pós-bridge — não desligadas (C puro violaria RTM), não em banner-débito (B violaria "sem mock, sem MVP"). Banner amarelo `proc-not-found` aceito como estado transitório nas 5 pages **apenas durante** janela F052b→F090..F094 (não é entregável, é placeholder de descoberta). F043 marcado com débito-rastreado: 5 dos 9 models são fixture inventada por gap de escavação, não cópia fiel — refactor F090..F094 corrige. Escavar/decidir destino das 5 procs `portal-director` referenciadas pelos models F043 mas ausentes em sources e na base — `acesso.sp_consultar_usuarios_fornecedor`, `acesso.sp_persistir_configuracao_agendamento`, `acesso.sp_persistir_configuracao_cotacao`, `acesso.sp_persistir_configuracao_email`, `acesso.sp_persistir_configuracao_aws`. **Archaeologist 2026-05-17 — conclusão: saída (c) corrigida.** Hipótese 1 (outro source não-catalogado): rejeitada — grep cross-sources zero hits para os 5 nomes literais; grep nos `.cs` controllers zero hits. Hipótese 2 (`.cs` direto): rejeitada nesse formato — endpoints existem mas não com esses nomes. Hipótese 3 (tenant-privativa): rejeitada — probe cross-DB em `172.27.0.121\SQL2k19` varreu 148 databases, zero matches para `sp_persistir_configuracao_%` ou `sp_consultar_usuarios_fornecedor%` (9 bases puladas: login/timeout, nenhuma plausível conter schema `acesso.*` distinto). **Verdade do legado:** as 5 funcionalidades existem, mas em **outro banco** (`portal-aws`, não `portal-director`), **outros schemas** (`agent`, `cotacao`, `portal` — não `acesso`), com **outros nomes** (`portal.obter_usuarios_fornecedores`, `agent.persistir_definicoes_agendamento`, `agent.persistir_config_email_agendamento`, `agent.persistir_data_bloqueio_agendamento`, `cotacao_persistir_config_email` etc.), e são chamadas pelo PortalDirector.Website **via gateway HTTP cross-app** (`/portal-aws/proc/<schema>.<proc>` → `UtilsController.ExecProc(appkey, proc)` em `sources/.../PortalDirector.Aplicacao/Controllers/UtilsController.cs:101-106` → `IHttpClientService.ExecProc`), nunca via SQL local no banco do portal-director. Páginas reais (`Fornecedores.jsx`, `Agendamento.jsx`, `Configuracoes/Cotacao/*`, `Configuracoes/Email/*`, `Configuracoes/IntegradorAWS/*`) usam esse padrão. Várias procs reais por page (não 1-para-1 com genericform do model F043). **Implicação:** os 5 models F043 estão com naming **inventado** (`acesso.sp_persistir_configuracao_*`) que não bate com nenhum proc real do legado — F043 não é cópia fiel do legado para essas 5 pages. **Caminhos válidos pós-F052b:** (A) refactor de F043 reescrevendo os 5 models para usar URL gateway `/portal-aws/proc/<schema>.<proc>` (mesmo padrão que `Fornecedores.jsx` usa hoje em produção — requer enfileirar feature de refactor por page, possivelmente múltiplas procs por page); (B) aceitar banner amarelo `proc-not-found` nas 5 pages do cutover até o refactor; (C) descartar as 5 pages do escopo P0 do cutover portal-director e adiar para fase pós-bridge-AWS (depende de F048 estável). **Não invento contrato.** Decisão de qual caminho seguir cabe ao curator. | `sources/engenharia--fabrica--dotnet-core--director/Fontes/PortalDirector.Aplicacao/Controllers/UtilsController.cs:60-106` (gateway cross-app) + `sources/.../PortalDirector.Website/src/routes/Acessos/Fornecedores.jsx` + `sources/.../PortalDirector.Website/src/routes/Configuracoes/Agendamento.jsx` + `sources/.../PortalDirector.Website/src/routes/Configuracoes/Cotacao/Cotacao.jsx` + `sources/engenharia--fabrica--sql--portal-aws/agent/programacao/agent.persistir_definicoes_agendamento.sql` + `sources/engenharia--fabrica--sql--portal-aws/agent/programacao/agent.persistir_config_email_agendamento.sql` + `sources/engenharia--fabrica--sql--portal-aws/agent/programacao/agent.persistir_data_bloqueio_agendamento.sql` + `sources/engenharia--fabrica--sql--portal-aws/agent/programacao/agent.sp_consultar_configuracao_email.sql` + `sources/engenharia--fabrica--sql--portal-aws/cotacao/programacao/cotacao_persistir_config_email.sql` + `sources/engenharia--fabrica--sql--portal-aws/cotacao/programacao/cotacao_sp_consultar_configuracao_email.sql` + `sources/engenharia--fabrica--sql--portal-aws/portal/programacao/portal.obter_usuarios_fornecedores.sql` + `sources/.../portal.persistir_usuario_fornecedor.sql` + `sources/.../portal.deletar_usuario_fornecedor.sql` | [[obter-model-pagina]] + [[model-valor-datagrid]] + [[model-valor-genericform]] | P1 | accepted | curator 2026-05-17 (descoberta + decisão; implementação delegada a F090..F094) | ✓ 2026-05-17 — curator: descoberta arqueológica conclusiva (3 hipóteses rejeitadas, padrão real do legado identificado: gateway HTTP cross-app via UtilsController.ExecProc); decisão A-sequenciado-por-C registrada; 5 features de refactor enfileiradas F090..F094 com gate F048-runtime-stable; F043 marcado débito-rastreado; sem código a auditar nesta linha (puramente cartográfica). |
| F053 | render | Interpolador `{path}` — emitir divergence `subPathNotFound` quando namespace existe mas sub-path resolve a `undefined` (hoje só emite `pathNotFound` quando o namespace raiz é desconhecido). Cobertura simétrica de telemetria: qualquer leitura que retorne `undefined` deve sinalizar ao banner do ModelEngine, não passar silenciosamente como string vazia/null. Follow-up direto de F051 (débito registrado pelo smith no aceite). | n/a (follow-up de F051) + `packages/ui/src/lib/interpolate.ts` | [[tbmodel-parametro]] | P1 | deferred (esbuild bloqueia /smoke/f053; smith+arch pass) | smith | — (smith 2026-05-17: `packages/ui/src/lib/interpolate.ts` `resolvePath` agora distingue 3 cenários: (a) namespace raiz desconhecido → reason=`path-not-found` com `path` preenchido (retrocompat); (b) namespace OK mas sub-path ausente do objeto (chave não presente via `in`) → reason=`sub-path-not-found` com `path` e `namespace` preenchidos; (c) segmento intermediário não-objeto → mesmo `sub-path-not-found`. Distingue presença com valor `null` literal (sem divergência) de ausência (`!(seg in obj)` — divergência). `InterpDivergence` estendido com campos opcionais `path?: string` + `namespace?: 'user'|'acl'|'configs'`. Banner ModelEngine (`packages/ui/src/components/model-engine.tsx:522-538`) consome `d.reason` direto — novo tipo aparece no banner sem mudança de código. Smoke `/smoke/f053` com 7 cenários (path-not-found retrocompat, sub-path-not-found em user/configs, valor null literal sem divergência, resolução normal, segmento intermediário não-objeto, embed mode com sub-path-not-found). Typecheck verde 3/3 packages (8.172s). PROIBIDO commit. Entregando para auditoria.) |
| F054 | render | [ACCEPTED 2026-05-15 — no-op confirmado: hipótese de 3º eval rejeitada pelo archaeologist; auditoria zero-eval validada pelo ui-tester (0 hits de `eval(` e `new Function(` em packages/ui/src + apps/api/src + apps/director-studio/src); smokes F050+F051 sem regressão; F051 25/25 PASS. F054b é redundante (mesmo escopo) — ver linha F054b abaixo.] Eliminar o 3º e último ponto de `eval` no engine schema-driven — `button.externalAction` no model. F050 cobriu funções de `TBfuncao_model` (executeGenericFunctions); F051 cobriu interpolação de `dParamX` (evalModelDynamicParams). Falta o caminho de `externalAction` (ação externa declarada no botão do model, executada via `eval` no legado). Inventariar uso real em Área 52 (TBmodel_pagina + seeds portal-aws), decidir estratégia (provavelmente C-like de F050: reescrita declarativa em primitivas nomeadas) e implementar. Conclui a remoção total de `eval`/`new Function` do engine. **Archaeologist 2026-05-15: hipótese rejeitada — `externalAction` é sempre função JS literal passada por host JSX, nunca string, nunca eval. Auditoria de `eval`/`new Function` em `react-tools/src/`: exatamente 2 hits (GenericPage.js:93 → F050; GenericPages.js:33 → F051). Probe cross-tenant (15 bases, 133+ models de `acesso.TBmodel_pagina` + amostras de `TBfuncao_model`): 0 ocorrências de `externalAction` no `DFvalor`. Seeds canônicos SQL: 0 hits. Trilho de eliminação de `eval` está **completo** com F050+F051. Curator deve fechar F054 como no-op ou converter em F054b (auditoria final: zero `eval`/`new Function` em `packages/ui/src`+`apps/api/src`).** | `react-tools/components/` (localizar binding) + `acesso.TBmodel_pagina` (campo `button.externalAction` no JSON) | [[button-external-action]] | P0 | accepted | ✓ 2026-05-15 (no-op, audit zero-eval confirmado: 0 hits de `eval(` e `new Function(` em packages/ui/src + apps/api/src + apps/director-studio/src; smokes /smoke/f050 e /smoke/f051 carregam sem erro de console, F051 reporta 25/25 PASS) | ✓ 2026-05-15 |
| F047 | integrations | Bus realtime multi-instância via Redis Pub/Sub — F023 entrega bus in-memory single-instance (fan-out por (channel,userId\|empresa) em memória do processo Node). Para escalar horizontalmente a API do Studio, fan-out precisa migrar para Redis Pub/Sub (channels `notif:user:{userId}`, `notif:empresa:{cnpj}`, `notif:all` conforme [[hub-sse-mapping]] §85-101). Buffer LRU de 100 por sessão pode permanecer in-memory (ou migrar para Redis Streams se reconnect cross-instance for requisito). Pré-requisito: deployment multi-réplica. | n/a (follow-up de F023) + `apps/api/src/hub-bus.ts` | [[hub-sse-mapping]] | P2 | todo | — | — |

## Backlog do arqueólogo

Features ainda não enumeradas — descoberta pendente:

- Catalogar **todos** os componentes em `react-tools/src/components/` que faltam acima
- Catalogar **todas** as procs em `sources/engenharia--fabrica--sql--portal-director/portal.director/programacao/acesso.*`
- Catalogar **todas** as tabelas em `sources/engenharia--fabrica--sql--portal-director/portal.director/criacao/acesso.TB*`
- Catalogar features fora do AppMain (e.g. Mobile, SignalR Hub, edoc, sped, fornecedor portal)

Cada item descoberto **vira linha nova nesta tabela**, com `Source` + `Contract` apontado.
| F054b | render | [REDUNDANTE — escopo idêntico a F054 (auditoria final zero-eval). Mantida no manifest por rastreabilidade (ui-tester observou duplicação; arqueólogo propôs a divisão antes do aceite). Aceite herdado de F054.] Auditoria final zero-eval: provar invariante `eval()` e `new Function()` ausentes em `packages/ui/src` + `apps/api/src` + `apps/director-studio/src` após F050+F051. Re-rodar smokes F050+F051 sem regressão. Aceite confirma cutover do engine livre de eval. | n/a (auditoria) | [[zero-eval-invariant]] | P0 | accepted (redundante c/ F054) | ✓ 2026-05-15 (herdado de F054 — mesma evidência) | ✓ 2026-05-15 |
| F055 | components | Dashboard edit-mode — drag/resize de widgets, configurar binding (proc/body/intervalSeconds), salvar layout em `acesso.TBdashboard.DFvalor` (`boxConfig`). F012 entrega só modo `view`. Provavelmente subdivide-se em catálogo (F-tbobjetos-dashboard) + grid-layout (F057). | `react-tools/components/DashBoard/` (legado tinha edit) | [[model-valor-dashboard]] | P2 | todo | — | — |
| F056 | components | Dashboard shared-link `#/dashboard?tkn=...&obj=...` — redesenhar JWT de 30 dias na URL para acesso público (kiosk/exhibition). F012 deixou fora por mandar redesign (legado usa hash anchor com token no fragmento, conflita com router moderno). Decidir: token em query vs path, escopo (1 dashboard vs N), revogação. | `react-tools/components/DashBoard/` (shared-link legado) | [[model-valor-dashboard]] | P1 | accepted | ✓ 2026-05-17 (5/5 probe + 6/6 curls backend; UI share-modal/share-page fora — Vite morto, escopo backend) | ✓ 2026-05-17 (3/3 critério triplo: A) técnico — audit pass-with-note (token UUID v4 opaco 122 bits, server-revogável, anti-enum 404 uniforme; reason exposto desvio consciente UX-driven), 3 dívidas explícitas registradas; B) MISSION — kiosk/exhibition público real destrava cenário legado que usava JWT-30d-na-URL inseguro, risk model redesenhado (token opaco revogável > JWT longo na URL); C) cobertura — 11 cenários reais (5 probe in-process + 6 curls com sessão real via Redis + LDAP bridge :4306, anti-enum C4/C6, modo single↔N validação Zod, resolve público, list filtra revogados, rotation [1,2]). Aceite backend; UI deferida por bloqueio Vite esbuild fora do escopo desta feature. 4 follow-ups enfileirados: F097 (P2) persistir SQL `acesso.TBshare_link`; F098 (P3) fallback cookie name hardcoded `share.ts:50`; F099 (P3) renderer real widgets em share-page; F100 (P3) rate-limit `/api/share/create`.) |
| F057 | components | Dashboard react-grid-layout (n×m configurável) — substitui Grid 2×2 fixo do F012 por layout livre persistido por dashboard. Dep `react-grid-layout` ou equivalente. Pré-requisito para edit-mode útil (F055). | `react-tools/components/DashBoard/` | [[model-valor-dashboard]] | P2 | todo | — | — |
| F058 | components | Dashboard SSE — eventos por objeto/widget substituem auto-refresh por `setInterval` em widgets que tenham canal correspondente no hub F023. Reduz round-trips e dá tempo-real onde o backend sinaliza mudança. Fallback para polling onde não há canal. | `react-tools/components/DashBoard/` (sem equivalente legado) + F023 | [[model-valor-dashboard]] + [[hub-sse-mapping]] | P2 | todo | — | — |
| F059 | components | Dashboard exhibition rotation — modo apresentação que rotaciona entre N dashboards favoritos em intervalo configurável (TV no CD/varejo). Legado tinha; F012 deixou fora. | `react-tools/components/DashBoard/` (rotation legado) | [[model-valor-dashboard]] | P2 | todo | — | — |
| F060 | components | Pull-to-refresh mobile (primitivo reusável) — gesto de arrastar para baixo no topo do scroll dispara refetch. Spec dashboard recomenda; designer já registrou como sinal. Reusável para grid/form/dashboard. | n/a (novo, primitivo de UX mobile) | TBD | P2 | todo | — | — |
| F061 | components | Gauge widget (custom SVG arc) — tipo de widget de dashboard sem equivalente direto em Recharts; legado tinha via Google Charts gauge. Implementar como SVG controlado, paleta semântica, sem dep externa. | `react-tools/components/DashBoard/` (gauge legado) | [[model-valor-dashboard]] | P2 | todo | — | — |
| F062 | components | Widgets unknown-type — scatter, combo (line+bar), treemap, geo, sankey. Designer sinalizou ausência de spec UI no `ui-system/`. Arqueólogo precisa confirmar uso real em Área 52 antes de priorizar; se nenhum cliente usa, vira no-op com banner amarelo "tipo não suportado neste tenant". | `react-tools/components/DashBoard/` + dependente de inventário em `acesso.TBdashboard.DFvalor` cross-tenant | [[model-valor-dashboard]] | P2 | todo | — | — |
| F063 | admin | Entrada de menu para `/app/dashboard` via `acesso.TBpagina` + ACL — F012 entrega rota mas não há `TBpagina` correspondente para o menu hierárquico exibir. Provavelmente sub-feature de admin (cadastro de páginas) ou seed pontual em F043. | `acesso.TBpagina` + F007 menu | [[menu-hierarquia]] + [[obter-model-pagina]] | P1 | accepted | ✓ 2026-05-17 (3 cenários DoD: probe-f063.ts end-to-end pass; idempotência seed 2× PageId=25 estável; curl `/api/menu/routes?app=portal-director` com login real identity=processa + temp-password 24h + cookie `director_session` retorna módulo `portal-director.dashboards` id=14 com página `portal-director.dashboards_index` id=25 path=`/dashboard`; sem UI — Vite morto, cobertura backend pura) | ✓ 2026-05-17 (3/3 critérios: (A) técnico — archaeologist audit-pass-with-note confirma schema TBpagina/TBmodulo canônico via DDL+pos-script, DFexibir_menu=0 convenção legada (cosmético não-render), padrão DFchave bate, `_index` convenção nova Studio para página-raiz de módulo; seed idempotente PageId=25 estável em 2 execuções; (B) MISSION — destrava entrada de menu para dashboard (F012 stub-na-rota agora visível no shell real via /api/menu/routes), fecha gap menu↔ACL↔route que era o último elo faltante para a frente dashboard ser navegável end-to-end pelo usuário do legado; (C) cobertura — caso real Area 52 IMPERIAL com super-user PROCESSA/1, módulo+página projetados no XML obter_acl_token, curl real autenticado via LDAP bridge + temp-password retorna módulo id=14 com child id=25 path=/dashboard. Sub-itens promovidos: F063-FU1/FU2/FU3 → F101/F102/F103 (P3) — ver smith backlog/F063-decisions.md.) |
| F064 | components | Dashboard mobile stack responsive (débito explícito de F012 C8) — desktop Grid 2×2 aplicado sem variante `<640px`; spec [[model-valor-dashboard]] §74-78 exige stack vertical full-width em mobile. Adicionar `grid-cols-1 md:grid-cols-2` ou equivalente. Mesma classe de F033 (concedido não-bloqueante pelo principal); enfileirado como débito de spec compliance. | n/a (follow-up de F012) | [[model-valor-dashboard]] | P1 | deferred (esbuild bloqueia /smoke/f064; smith+arch pass; revalidação visual requer resize_window MCP) | — | — |
| F-tbobjetos-dashboard | components | [ACCEPTED 2026-05-17 — curator: caminho A (seed sintético) aceito; critério triplo OK — (A) técnico: audit-pass-with-note, schema D1..D11 respeitados, débito D-7 super-user dbo.TBusuario divergence enfileirado em F104, DFmodel canônico Studio não-roundtrip com legado é intencional; (B) MISSION: dashboard "real" (sintético idempotente) substitui FAKE_BOX_CONFIG e destrava F012 end-to-end — renderer F012 consome backend SQL real, ciclo Studio completo funcional; (C) cobertura: caso real Area 52 IMPERIAL com login processa via temp-password 24h, 6 widgets dos 4 discriminantes MVP (kpi/chart/table/switcher) com SELECT real FROM acesso.TBdashboard, idempotência verificada (apply 2x payload idêntico). 2 follow-ups enfileirados: F104 (P2) sub-contrato `tbusuario-multi-tenant.md` resolver gap dbo.TBusuario vs acesso.TBusuario; F105 (P3) cosmético seed comentário-cabeçalho `studio.* → acesso.*`.] Catálogo `acesso.TBdashboard` — seed/migração dos dashboards reais. F012 entrega renderer + endpoint stub com payload fake; sem este catálogo, não há dashboard real para exercitar end-to-end. **Achado central da descoberta (archaeologist 2026-05-17)**: probe SQL2k19 ao vivo em 84 bases `DBdirector_*` ONLINE + `DBengenharia_Director_RC` + `DBx_appb_ti_teste` retornou `COUNT(*)=0` uniformemente em `acesso.TBdashboard` e `acesso.TBobjetos_dashboard` — **a feature dashboard do legado nunca foi adotada em produção pelos clientes do Director ERP inventariados**, não há boxConfig real para transcrever. Ponto-cego registrado: `DBappBuilder` e `DBappBuilder_Engenharia` inacessíveis com logins `sl`/`director_web`. Achado adicional: schema canônico não tem `DFdata_modificacao` (tarefa-pai presumiu — coluna inexistente); schema paralelo `appbuilder.TBdashboard` em 3 bases é resíduo de migração e deve ser ignorado. Estratégia recomendada: **Caminho A — seed sintético** de 6 widgets (kpi/grid/pie/bar/line/buttons) + 1 dashboard demo em `DBdirector_Imperial_Logistica_29`, idempotente por `DFchave` (padrão F063); **Caminho B — adapter `parseLegacyBoxConfig`** como código defensivo pronto para dado real quando aparecer. F062 (widgets unknown-type) rebaixada para P3/backlog em decorrência de D9 (sem evidência de uso real). | `acesso.TBdashboard` + `acesso.TBobjetos_dashboard` + `react-tools/components/DashBoard/` + procs `acesso.consultar_model_dashboards` + `acesso.sp_persistir_dashboard` + `acesso.sp_persistir_favorito_dashboard` + `acesso.sp_obter_dashboard_favorito` + `acesso.consultar_model_objetos_dashboard` + `appbuilder.sp_persistir_objetos_dashboard` + DDL `acesso.TBdashboard.sql` + DDL `acesso.TBobjetos_dashboard.sql` | [[tbdashboard-catalog]] + [[model-valor-dashboard]] | P1 | accepted | ✓ 2026-05-17 (5/5 DoD: probe 11/11 PASS, login real processa via temp-password 24h emite set-cookie director_session HttpOnly Max-Age=28800, curl GET /api/dashboards/me?app=portal-director autenticado retorna 1 dashboard "Demo Studio" id=1 favorite=true com boxConfig.widgets[6] types=[kpi,chart,chart,chart,table,switcher] chartKinds=[pie,bar,line] objetoChave=studio.demo.* + procedure acesso.dashboard_demo_* + intervalSeconds + layout, FAKE_BOX_CONFIG removido de dashboards.ts SELECT real linha 460 FROM acesso.TBdashboard, idempotência apply-tbdashboard-seed.ts 2x confirmada DashboardId=1 widgets id=1..6 modelLen=2088 estáveis re-curl pós-Run#2 payload idêntico. Vite morto — UI test deferido como débito transversal. Relatório: test-reports/F-tbobjetos-dashboard.md) | ✓ 2026-05-17 |
| F065 | components | TreeView — paridade de highlight de substring no filtro (hoje só TreeCheckable destaca matches com `<mark bg-primary/20>`). Fora do contrato legado (GenericTreeView.js não destacava), mas spec [[tree-view]] sugere e simetria com TreeCheckable melhora UX. Follow-up de F013. | n/a (follow-up de F013) + `packages/ui/src/components/tree-view.tsx` | [[tree-view]] | P2 | todo | — | — |
| F066 | components | Corrigir fixture do smoke `/smoke/f013` — usa `genericform:{fields:[]}` mas contrato F010 [[model-valor-genericform]] lê `config.model=[[Field]]`. Defeito de fixture (não de renderer); ui-tester confirmou que GenericFormRenderer reage corretamente ao schema vazio com InlineAlert "Form sem campos". Realinhar fixture para refletir o contrato real. | n/a (follow-up de F013) + `apps/director-studio/src/routes/smoke-f013.tsx` | [[model-valor-genericform]] + [[model-valor-generictreeview]] | P2 | todo | — | — |
| F067 | auth | LDAP bridge — autenticação via servidor AWS (porta 4306). Fecha gap arquitetural deixado por F003 aceito com caminho `ldap-bridge` stubado como `501 not-implemented` em `apps/api/src/routes/auth.ts:377-399` — usuários do legado (`processa\guga` etc.) dependem desse caminho. Próxima wave: archaeologist escava contrato real do `LDAPAuthMiddleware.cs` (bind+search+pwd validate; rede para servidor LDAP AWS:4306; mapeamento de DN → `acesso.TBusuario`; criptografia de senha em trânsito via `Cryptography.cs`). Smith implementa após contrato; ui-tester exercita com credencial AD real. Archaeologist 2026-05-16: contrato publicado em [[processa-auth-ldap-bridge]] (20 asserções A1..A20) + sub-contrato [[processa-cryptography]] (13 asserções C1..C13 de fidelidade AES). Probe ao bridge AWS:4306 confirmou shape: body é JSON-encoded string, resposta sempre 200 OK com envelope `{status, sucesso, dados}` (status semântico no body), bridge não exige header Authorization, mensagem de erro genérica não diferencia credencial-inválida de bridge-down. | `processa.sdk/Fontes/Processa.Sdk.Auth/LDAPAuthMiddleware.cs` + `processa.sdk/Fontes/Processa.Sdk.Api/Cryptography.cs` + `processa.sdk/Fontes/Processa.Sdk.Auth/AbstractBearerAuth.cs:95-137` | [[processa-auth-ldap-bridge]] + [[processa-cryptography]] | P0 | accepted | ✓ 2026-05-16 | ✓ 2026-05-16 |
| F068 | auth | Temp password — algoritmo offline `TokenUtils.GenerateTempPassword`. Fecha gap arquitetural deixado por F003 aceito com caminho `temp-password` stubado como `501 not-implemented` em `apps/api/src/routes/auth.ts:377-399`. Archaeologist 2026-05-16: contrato publicado em [[processa-auth-temp-password]] (25 asserções T1..T25). Algoritmo é base64 puro (não AES): plaintext = `base64(salt 6 chars de SecretKey, índice random ∈ [0,100)) + separator (SecretKey.Substring(50,7) = "OYOHJxX") + DateTime.Now.AddSeconds(N):"yyyy-MM-dd HH:mm:ss"`; segunda camada base64 sobre o todo (token final = 48 chars). Validação: split por separator, salt-base64-decode, conferir `SecretKey.Contains(salt)`, comparar `DateTime.Now < parsed`. Identity sentinela em sucesso: `DirectorIdentity(1, "processa", 1, "Processa"){Domain="Director"}` — **sem lookup em `acesso.TBusuario`**. Endpoints expostos: `GET /api/auth/generateTemp?segundos=N` (SDK EmbeddedController) e `GET /api/tools/passwordTemp/{hours}` (ADM ToolsController). Token reutilizável até expirar (sem "consumed" flag), sem auditoria, sem revogação. Inércias legadas registradas: SecretKey hardcoded compartilhada cross-tenant, salt curto (6 chars/pool 100), token = base64 puro (timestamp visível trivialmente), `DateTime.Now` em horário local não-zonado, mensagem 401 indistinguível causa-erro. Smith próxima wave: portar 1:1 em TS; ui-tester exercita round-trip legado→Studio. | `processa.sdk/Fontes/Processa.Sdk.Auth/TokenUtils.cs:131-177` + `processa.sdk/Fontes/Processa.Sdk.Auth/AbstractBearerAuth.cs:139-146` + `processa.sdk/Fontes/Processa.Sdk.Auth/AuthMiddleware.cs:63` + `processa.sdk/Fontes/Processa.Sdk.Controllers/EmbeddedController.cs:59-61` + `processa.ADM/Fontes/Processa.ADM.Aplicacao/Controllers/ToolsController.cs:16-21` | [[processa-auth-temp-password]] | P0 | accepted | ✓ 2026-05-16 | ✓ 2026-05-16 |
| F069 | auth | Cobertura completa do caminho `fornecedor-aws` — validação real de senha SHA2_256 contra `portal.UsuarioFornecedor`. Archaeologist 2026-05-16: contrato publicado em [[processa-auth-fornecedor-aws]] (25 asserções F1..F25). Discriminador exato: `identity.Contains('@')` (case-irrelevante, qualquer posição); prefixos LDAP `processa\` têm prioridade e literal `processa` desvia para temp-password antes. Query inline em `Settings.AuthFornecedorAWSQuery` (default em `Settings.cs:54-65`): `SELECT Id, Email AS Nome, 1 AS CodEmpresa, NomeUsuario AS NomeEmpresa FROM portal.UsuarioFornecedor WHERE Email=@nome AND Senha=CONVERT(NVARCHAR(256), HashBytes('SHA2_256', CAST(@senha AS VARCHAR(256))), 2) AND Status=1`. Hash: SHA2_256 sobre `CAST(@senha AS VARCHAR(256))` (collation-dependente para não-ASCII), CONVERT estilo `2` (hex uppercase, 64 chars, sem `0x`). Schema da tabela: `Id INT IDENTITY PK`, `NomeUsuario NVARCHAR(255)`, `Senha NVARCHAR(255→2048)`, `Email NVARCHAR(255→1024) UNIQUE`, `Fornecedores NVARCHAR(2048)` (CSV de CNPJ), `Status BIT`, `Cnpj NVARCHAR(14)`. Identity emitida: `CodEmpresa=1` literal hardcoded (não tenant real!), `NomeEmpresa = NomeUsuario` (semanticamente errado — é nome do contato, não da empresa), sem `Grupos`, sem `JWT`. **Sem audit log, sem rate-limit, sem lockout, sem trigger.** F003 já implementa F1+F2+F15 corretamente em `apps/api/src/routes/auth.ts:498-549` com query inline equivalente. Confusão importante registrada: `portal.UsuarioFornecedor` (este contrato) ≠ `portal.Usuario` (admin-empresa replicado do ERP via sincronização) — duas tabelas distintas, discriminador `@` no auth não as separa; smith deve marcar `path:'fornecedor-aws'` no principal para downstream discriminar. Inércias legadas: SHA2_256 sem salt, senha de vendedor sincronizado `'for-' + LEFT(CNPJ,3)` (1000 combinações), mensagem 401 indistinguível, WITH(NOLOCK) dirty-read, `CodEmpresa=1` sentinela. ACL fornecedor: sem grupos pré-carregados, resolvida sob demanda via `agent.UsuarioFornecedorDefinicao` e `agent.EquipeFornecedor`. Smith próxima wave: nenhuma (F003 já cobre F1+F2+F15); ui-tester wave: exercitar F21 end-to-end com credencial real de fornecedor em Area 52 + casos negativos F3/F4. | `processa.sdk/Fontes/Processa.Sdk.Auth/AuthMiddleware.cs:60-90` + `processa.sdk/Fontes/Processa.Sdk.Api/Settings.cs:54-65,175` + `sql--portal-aws/portal/criacao/portal.UsuarioFornecedor.sql` + `sql--portal-aws/portal/programacao/portal.persistir_usuario_fornecedor.sql:67-119` + `apps/api/src/routes/auth.ts:498-549` (F003) | [[processa-auth-fornecedor-aws]] | P1 | accepted | ✓ 2026-05-16 (parcial: F1/F3/F9/F10/F12/F20/F24/F25 OK; F2/F4/F5/F15/F21 happy path bloqueado por falta de fornecedor real na base IMPERIAL — happy path enfileirado em F077) | ✓ 2026-05-16 (parcial; 3/3 critérios atendidos no escopo exercitável: contrato [[processa-auth-fornecedor-aws]] cobre F1..F25 com discriminador, query SHA2_256 e identity emitida; F003 implementa F1+F2+F15 literal-copy da query legada em `apps/api/src/routes/auth.ts:498-549`; audit-pass estático confirmou cobertura de asserções verificáveis sem dados; ui-tester pass 8/8 em casos negativos reais Area 52 IMPERIAL — resolver→query→0 rows→401 exercitado fim-a-fim; happy path F2/F4/F5/F15/F21 não exercitável por gap **de dados** (zero fornecedores em `portal.UsuarioFornecedor` da base imperial_logistica_29), não de código. Política aplicada: aceite parcial análogo a F005/F007/F008 (débitos transversais não-bloqueantes) e F052 (gap isolado em follow-up). F077 enfileirada como P2 para fechar happy path quando fornecedor real for cadastrado ou Portal AWS real (`52.67.203.133`) for wired ao Studio em wave futura. PROIBIDO seed cross-contrato — base imperial não pode ser modificada para este fim.) |
| F070 | ui-system | Corrigir `receipt-modal.tsx` para usar `Dialog` no desktop — arquivo usa Drawer Vaul incondicional e não importa `useIsMobile`, resultando em drawer-up subindo no desktop. Conforme skill [[vaul]] cheat-sheet, modal de recibo é "ação curta de visualização" → desktop = `Dialog` (max-w-md ou similar), mobile = manter Drawer. Gate via `useIsMobile()`. | n/a (correção de impl) | n/a (skill [[vaul]] + [[mobile-first-page]]) | P1 | accepted | ✓ 2026-05-16 (R1/R3/R4 OK em desktop 1280×900: Dialog shadcn centralizado, overlay, iframe sanitizado, Esc/Fechar fecham, focus trap, console limpo; R2 mobile deferido a F033 — Chrome MCP viewport floor 1267px, mesmo padrão F005/F007/F013/F014; switch `useIsMobile` é o mesmo canônico já validado nessas features) | ✓ 2026-05-16 (R2 mobile coberto por F033; skill [[vaul]] honrada — Dialog desktop / Drawer mobile via useIsMobile; Dialog shadcn reusado de packages/ui; ui-tester pass 4/4 em consumidor real /smoke/f050 seção 2; zero 🚩 — drawer-up no desktop eliminado) |
| F071 | ui-system | Corrigir `generic-form-renderer.tsx` (2 Drawers) para Dialog/Sheet no desktop — arquivo tem dois Drawer Vaul incondicionais (linha 1035 confirm dialog; linha 1117 modal de detalhe) sem `useIsMobile`. Conforme skill [[vaul]]: linha 1035 (confirmação binária) → desktop = `Dialog`; linha 1117 (formulário complexo, conteúdo extenso/scrollável) → desktop = `Sheet side="right"`. Cuidado: arquivo grande (1000+ linhas); wave deve focar APENAS na troca dos dois Drawers, sem refactor adjacente. | n/a (correção de impl) | n/a (skill [[vaul]] + [[mobile-first-page]]) | P1 | accepted | ✓ 2026-05-16 (G1/G4/G5 pass; G2 deferred sem consumer formOnModal; G3 mobile→F033) | ✓ 2026-05-16 (parcial: G1 confirma Dialog desktop + gate `useIsMobile` canônico funcionando; G2 Sheet modal-detalhe não-exercitável por ausência de consumer com `config.formOnModal=true` no app — aceito por simetria de código (mesmo gate governa ambos branches confirm+detalhe), enfileirado em F078 para validação quando consumer real emergir (F015 wizard ou page legacy renderizada em modal de detalhe); G3 mobile→F033 (débito transversal); Esc-não-fecha-Dialog enfileirado em F079 (P3 microfeature de foco do portal); skill [[vaul]] honrada nos branches exercitados; zero regressão F050/F070; mesma política aceite-parcial de F005/F069.) |
| F072 | ui-system | Corrigir `RecentStackSheet` em `page-tabs.tsx` para Popover/Sheet no desktop — linha 118 define `RecentStackSheet` (nome promete Sheet, implementação é Drawer Vaul incondicional). Conforme skill [[vaul]]: lista curta (até ~6 abas) → desktop = `Popover` (align="end"); lista longa → desktop = `Sheet side="right"`. Smith decide qual baseado em UX no desktop; padrão recomendado é Popover. Gate via `useIsMobile()`. | n/a (correção de impl) | n/a (skill [[vaul]] + [[mobile-first-page]]) | P2 | accepted | ✓ 2026-05-16 (S1/S3/S4 OK em desktop 1267×706: Popover Radix com align=end, sideOffset=8, w-80, role=dialog, aria-label correto; onActivate/onClose/outside-click funcionais; aria-labels nos close buttons; PageTabsBar intocado no diff; console limpo; smoke route `/smoke/f072` criada; S2 mobile→F033 mesmo padrão F005/F007/F013/F014/F050/F070/F071; Esc-no-MCP observação não-bloqueante (paralela a F079)) | ✓ 2026-05-16 (S2 mobile→F033; Esc→F079; smoke route `/smoke/f072` mantida como regressão; skill [[vaul]] honrada — Popover desktop / Drawer mobile via `useIsMobile`; mesma política aceite-parcial de F070/F071) |
| F073 | ui-system | Migrar `tree-checkable.tsx` Drawer `direction="right"` desktop → `Sheet side="right"` canônico — linha 723 usa `<Drawer direction={isMobile ? "bottom" : "right"}>`. Funciona porque Vaul aceita direction=right, mas o canônico da skill [[vaul]] é `<Sheet side="right">` shadcn no desktop. Migrar para o padrão canônico ajuda consistência de estilo (animação, sombra, padding) entre componentes que abrem painéis laterais no desktop. P3: não-bloqueante, refinamento de consistência. | n/a (correção de impl) | n/a (skill [[vaul]] + [[mobile-first-page]]) | P3 | accepted | ✓ 2026-05-16 (TC1/TC3/TC4 OK em desktop 1280×900: `[data-slot="sheet-content"][data-side="right"]` confirmado + overlay shadcn + zero `[vaul-drawer]`; cascade/filtro `<mark>×2`/Limpar/Salvar/Esc/outside-click todos pass; console limpo sem `Maximum update depth`; TC2 mobile→F033 — mesma classe F005/F013/F014/F070/F071) | ✓ 2026-05-16 (TC2 mobile→F033) |
| F074 | auth | `processa.com\` identity normalization — follow-up de F067 C4. Discriminador (A1) aceita o prefixo `processa.com\`, mas o bind real no AD legado rejeita porque o domínio canônico esperado é `processa\` (sem `.com`). Strip `.com` antes do envio ao bridge, mantendo o envelope cifrado idêntico ao que o legado produziria. Baixíssimo uso (heurística textual rara — usuários normalmente digitam `processa\guga`); investigar se bridge legado já normaliza ou se sempre quebrou silenciosamente. | F067 (`apps/api/src/routes/auth.ts` resolveAuthPath + payload builder) | [[processa-auth-ldap-bridge]] | P3 | todo | — | — |
| F075 | ui-system | Title HTML por rota — follow-up de F067 (note ui-tester). `<title>` atual é placeholder `vite-monorepo` herdado do scaffold do Vite. Substituir por título branded por rota (ex: "Director.Studio — Login", "Director.Studio — /areas", "Director.Studio — {areaName}"); definir mecanismo (head manager por rota ou simples efeito por página). Cosmético, não bloqueia cutover. | n/a (apps/director-studio/index.html + per-route head) | n/a | P3 | todo | — | — |
| F076 | tooling | Fix `.claude/skills/gen-processa-password/` para refletir contrato [[processa-auth-temp-password]] T2 — skill atualmente gera token sem a camada b64-interna (`base64(salt6, idx)`), produzindo formato divergente do legado. F068 expôs o defeito durante implementação real do algoritmo. Skill é ferramenta operacional (não código de produção), por isso não bloqueia cutover, mas qualquer geração manual via skill atualmente produz token que o backend Studio (após fix do commit 7498bb0) **vai rejeitar**. Aplicar mesma fix do commit 7498bb0 nos templates/scripts da skill. | `.claude/skills/gen-processa-password/` | [[processa-auth-temp-password]] T2 | P3 | todo | — | — |
| F078 | ui-system | F071 follow-up — exercitar G2 (Sheet `side="right"` desktop / Drawer mobile no modal de detalhe do `generic-form-renderer.tsx` linha 1117) com consumer real disparando `config.formOnModal=true`. F071 aceito parcial 2026-05-16: smith implementou o gate `useIsMobile` que governa simetricamente os dois branches (confirm linha 1035 + detalhe linha 1117); G1 valida o gate funcionando no confirm, mas o branch de detalhe não tem consumer no app atual. Critério de aceite: G2 pass empírico com page real que abre form-on-modal (provável trigger: F015 wizard / page legada com modal de detalhe / smoke route dedicada `/smoke/f071-detail` com fixture `formOnModal=true` se nenhum consumer real emergir até então). Não-bloqueante para cutover do F071. | n/a (follow-up de F071) + `packages/ui/src/components/generic-form-renderer.tsx:1117` | n/a (skill [[vaul]]) | P3 | todo | — | — |
| F079 | ui-system | Esc não fecha Dialog em `generic-form-renderer.tsx` (confirm desktop) — observação não-bloqueante do ui-tester durante G1 de F071. Botão "Fechar" funciona; Esc não. Causa-raiz provável: foco do portal não está no Dialog quando aberto (Dialog shadcn depende de `onEscapeKeyDown` no `DialogContent` que só dispara se foco está dentro). Investigar: (a) `autoFocus` no primeiro botão, (b) `<DialogContent onEscapeKeyDown={...}>` explícito, (c) interação com Radix Portal focus management. Microfeature isolada. | n/a (follow-up de F071) + `packages/ui/src/components/generic-form-renderer.tsx` | n/a | P3 | todo | — | — |
| F077 | auth | F069 follow-up happy path — exercitar F2/F4/F5/F15/F21 do contrato [[processa-auth-fornecedor-aws]] end-to-end com credencial real de fornecedor. F069 aceito parcial 2026-05-16: cobertura estática + casos negativos pass 8/8, mas happy path bloqueado por gap **de dados** (zero fornecedores em `portal.UsuarioFornecedor` da base IMPERIAL LOG `DBdirector_imperial_logistica_29`). Desbloqueio possível por: (a) cadastrar fornecedor real em Area 52 via fluxo legado de admin (`agent.UsuarioFornecedorDefinicao`) — preferível, exercita o caminho de produção; (b) wire do Portal AWS real (`52.67.203.133` ou equivalente) ao Studio em modo bridge, exercitando query contra base AWS que já tem fornecedores ativos — fecha também F048 e parte de F024; (c) seed pontual em base de teste isolada (não na IMPERIAL — PROIBIDO modificar base de cliente). Critério de aceite: F2 (Email AS Nome em identity), F4 (CodEmpresa=1 literal), F5 (NomeEmpresa=NomeUsuario), F15 (Status=1 filter), F21 (sessão httpOnly emitida com path:'fornecedor-aws' marcado) todos pass com credencial real. Não-bloqueante para cutover de F069 (contrato e código já são literal-copy do legado). | n/a (follow-up de F069) + `apps/api/src/routes/auth.ts:498-549` | [[processa-auth-fornecedor-aws]] | P2 | todo | — | — |
| F080 | components | `select-options-endpoint` — convenção HTTP canônica para carga de opções de select/PowerSelect: discriminador `api.includes('/proc/')` → POST com body=state; `queryKey` → GET `/selectquery/{key}?parametro_id={body.id\|\|''}`; `api` cru → GET `{api}`; `selectDataType:'fixedList'` + `options[]` → inline sem HTTP. Sub-contrato sinalizado em F016 ([[filtros-componente]] §"Features novas") e reforçado em F019 ([[power-select]]) — vive hoje em `useSelectFields.js` e replicado parcialmente em V1 `PowerSelect.js`. Forma de resposta esperada: `{status:200, sucesso:true, dados:Option[]}`; falhas (status≠200 ou `sucesso:false`) silenciam UI. Arqueólogo escava endpoints reais `/select/<id>` / `/selectquery/<key>` por aplicação (cross-tenant) para fechar inventário antes de Smith implementar. | `react-tools/hooks/useSelectFields.js:100-174` + `react-tools/components/PowerSelect.js:80-109` + procs `acesso.obter_select_*` (a inventariar) | TBD | P2 | todo | — | — |
| F082 | components | F019 follow-up — eliminar 6ms residual flash empty no PowerSelect inicializando `isLoading=true` antes do debounce do `useSelectFields` (estado inicial deve assumir loading até primeira resposta resolver, em vez de empty→loading→data). Não-bloqueante, polish UX sub-frame observado pelo ui-tester em R4. | `packages/ui/src/components/power-select.tsx` + `packages/ui/src/hooks/use-select-fields.ts` | [[power-select]] PS4 | P3 | todo | — | — |
| F083 | ui-system | F019 follow-up — revisar tokens de layering do Popover (C3 do contrato): superfície translúcida torna conteúdo do popover dificil de ler sobre fundos coloridos. Designer revisa `--popover` / `--popover-foreground` e backdrop blur. Não-bloqueante. | `packages/ui/src/components/popover.tsx` + tokens em `packages/ui/src/styles/` | n/a (skill [[shadcn-tokens]]) | P3 | todo | — | — |
| F084 | docs | F019 follow-up — documentar PS22 (cache memo TTL) na UX spec `[[power-select]]`: comportamento atual do `useSelectFields` cacheia respostas indefinidamente por `queryKey`/`api`; auditor sugeriu adicionar nota sobre invalidação (linked-filters dependentes e revalidação manual). Não há TTL real, é cache de sessão — registrar isso explicitamente para evitar ambiguidade futura. | `mind/atlas/concepts/ui-system/power-select.md` PS22 | [[power-select]] PS22 | P3 | todo | — | — |
| F085 | components | F020 follow-up — investigar `dismiss()` global no-op no sonner v2.0.7. Após 6 retries em F020, `useToast().dismiss()` sem argumento não fecha toasts ativos, mesmo com workaround Set+iter de ids. Auto-dismiss por timer e action-close funcionam; bug isolado ao caminho global. Tarefas: (a) instrumentar caminho do dismiss com console.log p/ confirmar invocação no sonner; (b) sanity-check chamando `sonner.toast.dismiss()` direto sem wrapper; (c) avaliar swap para `react-hot-toast` se for bug estrutural do sonner v2 com nossa config (StrictMode/HMR Vite). Caso de uso raro (logout/redirect massivo) — workaround documentado: usar IDs explícitos + `dismiss(id)`. Não bloqueia cutover. | `workspace/director-studio/packages/ui/src/components/sonner.tsx` + `packages/ui/src/hooks/use-toast.ts` + smoke `apps/director-studio/src/routes/smoke-f020.tsx` | [[toaster]] §API conceitual ("Sem argumento, fecha todos") | P3 | todo | — | — |
| F081 | components | `linked-filters` (cascading selects) — semântica de filtros encadeados via descritor `linkedFilters[]` no field do model: cada item `{prop, value}` aponta o select irmão a recarregar quando o atual mudar. `value===null` dispara fetch ao endpoint `{rootApi}/select/{lFilter.prop}?filtro={selectedValue}` e popula cache; `value!==null` substitui valor/api direto (cascata fixa). Hoje vive em `<Filtros>` (`verifyLinkedFilters`) e padrão paralelo em `useGenericFormUtils` do `<GenericForm>`. Sinalizado por F016 ([[filtros-componente]] FL6 + §"Features novas") e reforçado por F019. Request síncrono dentro de `handleChange` (bloqueia UI, sem debounce, sem cancel). Candidato a primitiva compartilhada entre filtro e form. | `react-tools/components/Filtro/Filtros.js:125-174` + `react-tools/components/GenericForm/hooks/useGenericFormUtils.js` (a mapear) | TBD | P2 | todo | — | — |
| F086 | components | F021 follow-up — investigar race M5 (async dismiss bloqueado) no Chrome MCP: durante onConfirm Promise pendente, ui-tester reportou 2/3 runs onde Esc fecha o modal apesar de `confirming=true` setado. Defesa em profundidade no código (handleOpenChange gate + onEscapeKeyDown preventDefault + onInteractOutside) está intacta — provável artefato de timing do MCP. Reproduzir em browser real ou via Playwright pra confirmar/refutar; se confirmado em real, calibrar setState→render→handler. Não bloqueia cutover. | `packages/ui/src/components/modal.tsx` + smoke F021 | [[modals]] M5 | P3 | todo | — | — |
| F087 | shell | Corrigir acentos faltantes em strings hardcoded do PageShell (aria-labels e textContent): "Recolher navegacao" → "Recolher navegação"; "Notificacoes (0 nao lidas)" → "Notificações (0 não lidas)"; "Ir para inicio — ..." → "Ir para início — ..."; skip link "Pular para o conteudo" → "Pular para o conteúdo". Bug colateral: botão de conta tem template concatenado errado: `Pprocessavia temp-password` parece `` `P${user}via temp-password` `` sem espaços/quebras — investigar avatar-menu. Não é mojibake (bytes ASCII puros, não double-encoding); é typo de origem nos JSX/aria-label. | n/a (próprio) | n/a | P2 | todo | — | — |
| F088 | render | Resolver gap entre F042 e schema real de `TBmodel_pagina` em base Imperial Logística (sem coluna `DFid_aplicacao`). ui-tester 2026-05-17 confirmou handler two-step + flag fellBack implementado mas auto-declara não-conforme via divergence; appKey ignorado no lookup. Survey archaeologist 2026-05-17 (cross-tenant via `SERVERSQL\SQL2K19`, 99 bases candidatas, 90 com a tabela): **0/90 bases** têm `DFid_aplicacao` ou `DFchave_aplicacao` em `acesso.TBmodel_pagina` — a coluna nunca existiu no legado, Imperial não é caso atípico. Discriminação por aplicação vive em `acesso.TBpagina.DFid_aplicacao` + `TBpagina.DFchaves_aplicacoes`, joined via `TBpagina.DFchave = TBmodel_pagina.DFchave_pagina`. Saída (a) migration descartada (criaria divergência em 100% das bases). **Saída (b) confirmada**: reescrever contrato F042 — filtro de aplicação no JOIN com `TBpagina`, não em `TBmodel_pagina`. Survey documentado em [[obter-model-pagina]] §"Asserção F042 — schema survey". Bloqueia aceite final F042. | `acesso.TBmodel_pagina` (schema canônico cross-tenant) + `acesso.TBpagina` (DFid_aplicacao + DFchaves_aplicacoes) | [[obter-model-pagina]] §"Asserção F042 — schema survey" | P1 | accepted | ✓ 2026-05-17 (cross-tenant survey: 99 bases probadas, 90 com TBmodel_pagina, 0 com DFid_aplicacao — schema canônico cross-DB via INFORMATION_SCHEMA real) | ✓ 2026-05-17 (Curator critério triplo OK: A) técnico — survey determinístico cross-tenant audit pass-with-note descoberta; B) MISSION — destravou correção do contrato F042 com paridade real do legado, evitou migration em 100% das bases; C) cobertura — SELECT INFORMATION_SCHEMA real cross-DB cobre universo completo Area 52) |
| F089 | infra | F044 follow-up — corrigir `apps/api/src/scripts/probe-f044.ts`: (1) cenários 2 e 4 assumem hipóteses falsas — case-2 espera heurística resolver `id` mas coluna real é `DFid_grupo_trabalho` (fora do cap §95 de 4 variantes); case-4 declara `sortKey=Descricao` quando coluna real é `DFdescricao` (override single-attempt §42 falha loud por design). Trocar cenário 2 para usar `__sortColumnMap: { id: 'DFid_grupo_trabalho' }` e cenário 4 para `sortKey=DFdescricao`. (2) Dois defeitos pré-existentes do probe: `SESSION_COOKIE_NAME` default `director_studio_session` ≠ real `director_session`; payload de login usa `{login, senha}` mas API espera `{identity, password}`. Após fix, rerun esperado 5/5 PASS sem mudança em `grid.ts`. Não bloqueia cutover (implementação F044 já aceita). | `workspace/director-studio/apps/api/src/scripts/probe-f044.ts` | n/a (follow-up de F044) | P2 | todo | — | — |
| F090 | render | F052b refactor — page `acessos_fornecedor` (model F043 #1): reescrever model para usar gateway cross-app `/portal-aws/proc/portal.obter_usuarios_fornecedores` (listagem), `/portal-aws/proc/portal.persistir_usuario_fornecedor` (CRUD), `/portal-aws/proc/portal.deletar_usuario_fornecedor`. Padrão real do legado em `PortalDirector.Website/src/routes/Acessos/Fornecedores.jsx`. Possivelmente múltiplas procs por page (datagrid+form). Substitui naming inventado `acesso.sp_consultar_usuarios_fornecedor`/`acesso.sp_persistir_*`. Gate: F048 estável em runtime real (não só smoke mock). | `sources/.../PortalDirector.Website/src/routes/Acessos/Fornecedores.jsx` + `sources/.../portal/programacao/portal.obter_usuarios_fornecedores.sql` + `sources/.../portal.persistir_usuario_fornecedor.sql` + `sources/.../portal.deletar_usuario_fornecedor.sql` + model seed F043 | [[obter-model-pagina]] + [[portal-aws-bridge]] | P1 | accepted | ✓ 2026-05-17 | ✓ 2026-05-17 (Curator critério triplo OK: A) técnico — contrato `portal-aws-bridge` cobre as 3 procs reais, audit-pass-with-note arqueólogo, F106 follow-up enfileirado para survey `/api/portal-aws` vs `/portal-aws`; B) MISSION — primeiro dos 5 refactors F052b caminho A destrava, paridade exata com `Fornecedores.jsx` legado; C) cobertura — probe 7/7 contra DB real Imperial DBdirector_imperial_logistica_29 (id=15), curl autenticado retorna 3 URLs gateway, POST proc forward via PortalAwsClient confirmado (502 aws-unreachable é gate runtime F048, fora de escopo F090); DoD typo path `/acessos/usuarios_fornecedor` vs real `/acessos/fornecedores` é divergência orthogonal — model em si correto). smith 2026-05-17: seed F090 aplicado idempotente em DBdirector_imperial_logistica_29 (DFid_model_pagina=15); 3 URLs gateway no DFvalor confirmadas; nova rota literal `POST /portal-aws/proc/:proc` em apps/api/src/routes/portal-aws-proxy.ts (forwards via PortalAwsClient.sendRequest); probe-f090 7/7 PASS (1 db + 3 bridge happy-path em mock standalone bindable na config persistida + 3 route gates); contrato portal-aws-bridge atualizado §"Consumo pelos models F043"; F090-decisions.md em backlog; typecheck @workspace/api verde. **ui-tester 2026-05-17**: probe 7/7 PASS contra DB real (Imperial Logística); curl autenticado `GET /api/model?app=portal-director&path=/acessos/fornecedores` retorna modelJson com 3 URLs gateway corretas (datagrid.api, genericform.endPoint, gridActions.execProc); POST `/portal-aws/proc/portal.obter_usuarios_fornecedores` autenticado → 502 `aws-unreachable` (forward via PortalAwsClient correto, portal-aws real não up no ambiente local — gate F048 trata runtime). Discrepância DoD: path literal pedido era `/acessos/usuarios_fornecedor` mas TBpagina.DFcaminho real é `/acessos/fornecedores` (model em si correto, DoD typo). Relatório `test-reports/F090-acessos-fornecedor.md`. |
| F091 | render | **DESCARTADA — duplicata de F093.** Decisão curator 2026-05-17 (caminho 1): Agendamento.jsx no PortalDirector.Website é só tab email, consome o mesmo par `agent.sp_consultar_configuracao_email` + `agent.persistir_config_email_agendamento` que F093 (configuracoes_email). Procs `persistir_definicoes_agendamento`+`persistir_data_bloqueio_agendamento` existem em portal-aws/agent mas zero hits no Director — provavelmente consumidas pelo AppBuilder/agent-UI próprio, fora do cutover-fase-1 (Director). Trabalho dessas procs enfileirado em F107 (cutover-fase-2 AppBuilder, gate F015 reaberto). model F043 #2 (`configuracoes_agendamento`) será atendido pelo refactor de F093 — ver F093. | n/a (descartada) | n/a | — | discarded | — | — (curator 2026-05-17: fundida em F093; follow-up F107 enfileirado para AppBuilder/agent-UI quando F015 reabrir) |
| F092 | render | F052b refactor — page `configuracoes_cotacao` (model F043 #3) **EPIC AGRUPADOR**. Decisão curator 2026-05-17 (caminho B+gancho-C, ver `backlog/scope-decisions.md` §F092): F092 vira agrupador de **4 sub-features proc-based** (F092a/b/c/d) sob page-shell-tabs (enabler F108). 5 tabs Integrador REST não-proc (`/api/cotacaointegrador/*`) saem para **F109 deferred** (cutover-fase-2, shape gemelar F107). F052b caminho A **estendido** (não descartado): admite "1 model = 1 page-shell-tabs com N sub-models". Gates: F108 (engine meta-model `pageTabs`) + F110 (proxy multi-app `/api/:appKey/proc/:proc`) + F113 (survey TBaplicacao(cotacao) tenant Imperial). F092 aceita só com 4 subs aceitos. | `sources/.../Configuracoes/Cotacao/Cotacao.jsx` + `sources/.../cotacao/programacao/*.sql` + model seed F043 + 4 sub-models | [[obter-model-pagina]] + [[portal-aws-bridge]] + (a criar) [[engine-page-tabs]] | P1 | blocked | — | — (smith 2026-05-17: contract-mismatch original em `backlog/F092-decisions.md`. curator 2026-05-17: split em F092a-d + enablers F108/F110/F113 + deferred F109 + follow-ups F111/F112 — ver `backlog/scope-decisions.md` §F092) |
| F092a | render | F092 sub-feature 1/4 — tab `gerais` (`CotacaoConfig.jsx`): model genericform consumindo `cotacao_persistir_config_opcoes` + `cotacao_sp_consultar_opcoes` via gateway `POST /api/cotacao/proc/:proc` (appKey=`cotacao`, F110). Substitui invenção `acesso.sp_persistir_configuracao_cotacao` (parcial — só opções gerais). Gates: F108 + F110 + F113. | `sources/.../Configuracoes/Cotacao/CotacaoConfig.jsx` + `sources/.../cotacao/programacao/cotacao_persistir_config_opcoes.sql` + `cotacao_sp_consultar_opcoes.sql` + sub-model F043 sob `pageTabs[0]` | [[obter-model-pagina]] + [[portal-aws-bridge]] + [[engine-page-tabs]] | P1 | accepted | ✓ 2026-05-17 | ✓ 2026-05-17 (Curator critério triplo OK: A) técnico — seed F092a-model-cotacao-gerais.sql idempotente JSON_MODIFY $.pageTabs[0] preserva slots vizinhos, audit-pass-with-note arqueólogo 10 campos cf CotacaoConfig.jsx + pageTabs Studio-canonical F108 + stubs F092b/c/d engine-válidos; B) MISSION — primeiro slot Cotacao no Studio, destrava cascata F092b/c/d via template idempotente, completa enabler F108 com sub-model real consumindo F110; C) cobertura — model real via GET /api/model?app=portal-director&path=/configuracoes/cotacao retorna idModel=18 pageTabs=4 (keys gerais/usuarios/email/logo) slot[0]=gerais com endPoint+api /api/cotacao/proc/... + 10 campos ordem CotacaoConfig.jsx (nomeEmpresa, integradorIp, 7 checkboxes, planoPagamentoErpId), POST proc autenticado → 502 app-unreachable (gate runtime F110 cotacao :5000 não-up, fora escopo), apply 2x consecutivo idempotente, probe 6/6 PASS. Notas registráveis do audit: F092b/c/d herdam mesma idempotência $.pageTabs[N], selectDataType/defaultOption opcionais a confirmar engine, TBpagina pre-existência fora escopo seed.) (smith 2026-05-17: seed F092a-model-cotacao-gerais.sql idempotente JSON_MODIFY $.pageTabs[0]; apply 2x DFid=18 pageTabs=4 slot 0 gerais com endPoint+api /api/cotacao/proc/... + 10 campos espelhando CotacaoConfig.jsx (nomeEmpresa, integradorIp, 7 checkboxes, planoPagamentoErpId); probe 6/6 PASS (DB shape + bridge cotacao Basic R4+Domain R6+URL R7 ambas procs + 3 route gates multi-app-proxy); reuso F110 inteiro sem rota nova; stubs slots 1-3 para F092b/c/d; typecheck verde 3 packages; backlog F092a-decisions.md D1-D7 com debitos sistemicos D4/D5/D6 declarados). **ui-tester 2026-05-17**: DoD 1-3 OK. (1) probe 6/6 PASS contra DB real Imperial (DB shape DFid=18 10 campos + bridge cotacao Basic R4+Domain R6+URL R7 ambas procs + 3 route gates). (2) curl autenticado (identity=processa temp-pw 24h, cookie director_session) `GET /api/model?app=portal-director&path=/configuracoes/cotacao` → 200 com idModel=18 idPagina=7 pageTabs=4 (keys gerais/usuarios/email/logo) tab[0].key=gerais genericform.endPoint+api exatos + 10 campos ordem CotacaoConfig.jsx; `POST /api/cotacao/proc/cotacao_sp_consultar_opcoes` body `{}` → 502 app-unreachable (cotacao :5000 não-up dev, paridade F110 gate runtime). (3) idempotência apply 2x consecutivo → mesmo DFid=18 pageTabs=4 10 campos (JSON_MODIFY $.pageTabs[0] determinístico). Relatório `test-reports/F092a-cotacao-gerais.md`. |
| F092b | render | F092 sub-feature 2/4 — tab `usuarios` (`CotacaoUsuarios.jsx`): model filtro+datagrid+genericform consumindo `cotacao_sp_obter_usuarios` + `cotacao_sp_persistir_usuario` via gateway `POST /api/cotacao/proc/:proc` (appKey=`cotacao`, F110). Path divergente legado `/cotacao/proc/...` sem `/api` mitigado para canônico — F111 confirma se ajuste necessário (soft-gate). Substitui invenção `acesso.sp_*_usuario_cotacao`. Gates: F108 + F110 + F113. Soft-gate F111. | `sources/.../Configuracoes/Cotacao/CotacaoUsuarios.jsx` + `sources/.../cotacao/programacao/cotacao_sp_obter_usuarios.sql` + `cotacao_sp_persistir_usuario.sql` + sub-model F043 sob `pageTabs[1]` | [[obter-model-pagina]] + [[portal-aws-bridge]] + [[engine-page-tabs]] | P1 | accepted | ✓ 2026-05-17 | ✓ 2026-05-17 (Curator critério triplo OK: A) técnico — audit pass-with-note paridade byte-perfect `CotacaoUsuarios.jsx`, guards THROW 50930-50936 específicos, anti-violação F111 explícita SQL+TS+probe contra path legado `/cotacao/proc/`, idempotência JSON_MODIFY $.pageTabs[1] preserva slot 0 F092a + slots 2/3 stubs, apply 2x byte-perfect; B) MISSION — 2/4 do epic F092 (F092a+F092b accepted) consolidando template idempotente para cascata F092c/d; C) cobertura — model real via `GET /api/model?app=portal-director&path=/configuracoes/cotacao` retorna idModel=18 pageTabs=4 slot[1].model composto buttons (Add cil-plus externalAction) + filtro.id=cotacao-usuario 4 props (email/nome/nomeUsuario/status fixedList Todos=2/Ativo=1/Inativo='0') + datagrid.api=/api/cotacao/proc/cotacao_sp_obter_usuarios 4 headers (nome+nomeUsuario sortable) + gridActions 2 (Ativar/Inativar externalAction) + pageActions.persistir.endPoint=/api/cotacao/proc/cotacao_sp_persistir_usuario, slot[0] gerais F092a preservado, POST proc real → 502 app-unreachable (cotacao :5000 não-up dev, gate runtime F110 fora escopo). Nota D7 (engine genericform-in-modal incluir onDoubleClick edição) é débito sistêmico declarado, não afeta shape do model — engine-renderer fora escopo desta sub-feature. Débitos D7/D8 herdam para a fase de engine; D4/D5/D6 herdados de F090/F092a.) (smith 2026-05-17: seed F092b-model-cotacao-usuarios.sql idempotente JSON_MODIFY $.pageTabs[1] (slot 0 gerais F092a preservado, slots 2/3 stubs F092c/d intactos); apply 2x DFid=18 pageTabs=4 slot 1 usuarios com datagrid.api=/api/cotacao/proc/cotacao_sp_obter_usuarios + pageActions.persistir.endPoint=/api/cotacao/proc/cotacao_sp_persistir_usuario + filtro 4 props (email/nome/nomeUsuario/status fixedList Todos=2/Ativo=1/Inativo='0') + datagrid 4 headers (mesmos props, nome+nomeUsuario sortable) + 2 gridActions externalAction (Ativar/Inativar) + pageButtons Add cil-plus; probe 6/6 PASS (DB shape + bridge cotacao Basic R4+Domain R6+URL R7 ambas procs + 3 route gates multi-app-proxy); reuso F110 inteiro sem rota nova; mitigação F111 canônica `/api/cotacao/proc/...` (anti-violação `"/cotacao/proc/` no SQL+TS+probe); typecheck verde 3 packages; backlog F092b-decisions.md D1-D8 com débitos novos D7 (engine genericform-in-modal sistêmico) e D8 (renderer externalAction handler default), herdados D4/D5/D6 de F090/F092a) **ui-tester 2026-05-17**: DoD 1-3 OK. (1) probe 5/5 PASS + 1 SKIP (DB shape sem DB_* env em invocação direta, validado via DoD §3 dotenv-cli). (2) curl autenticado (identity=processa temp-pw 24h cookie director_session) `GET /api/model?app=portal-director&path=/configuracoes/cotacao` → 200 idModel=18 pageTabs=4 keys=gerais/usuarios/email/logo; slot[1].model contém buttons.pageButtons[Add cil-plus externalAction] + filtro.id=cotacao-usuario filtro.model=4 props (email/nome/nomeUsuario/status fixedList Todos=2/Ativo=1/Inativo='0') + datagrid.api=/api/cotacao/proc/cotacao_sp_obter_usuarios headers=4 (nome+nomeUsuario sortable) gridActions=2 (Ativar/Inativar externalAction) + pageActions.persistir.endPoint=/api/cotacao/proc/cotacao_sp_persistir_usuario; slot[0] gerais preservado com genericform.endPoint=/api/cotacao/proc/cotacao_persistir_config_opcoes; `POST /api/cotacao/proc/cotacao_sp_obter_usuarios` body `{}` → 502 app-unreachable (cotacao :5000 não-up dev, paridade F110 gate runtime; 1ª req transient 503 db-unavailable cold-cache registry, 2ª req 502 esperado). (3) idempotência apply 2x via dotenv-cli → mesmo DFid=18 pageTabs=4 slot 1 usuarios identical slot 0 preservado (JSON_MODIFY $.pageTabs[1] determinístico). Relatório `test-reports/F092b-cotacao-usuarios.md`. |
| F092c | render | F092 sub-feature 3/4 — tab `email` (`<ConfigEmail appKey='cotacao'>`): model genericform consumindo `cotacao_sp_consultar_configuracao_email` + `cotacao_persistir_config_email` via gateway `POST /api/cotacao/proc/:proc` (appKey=`cotacao`, F110) + botão Testar Email via `POST /api/teste-email` (F112). Substitui invenção `acesso.sp_persistir_configuracao_email_cotacao`. Sub aceitável sem botão teste-email (débito declarado) caso F112 atrase. Gates: F108 + F110 + F113. Soft-gate F112. | `sources/.../Configuracoes/Cotacao/CotacaoEmail.jsx` (tab) + `sources/.../components/ConfigEmail.jsx` + `sources/.../cotacao/programacao/cotacao_sp_consultar_configuracao_email.sql` + `cotacao_persistir_config_email.sql` + sub-model F043 sob `pageTabs[2]` | [[obter-model-pagina]] + [[portal-aws-bridge]] + [[engine-page-tabs]] | P1 | accepted | ✓ 2026-05-17 | ✓ 2026-05-17 (Curator critério triplo OK: A) técnico — seed F092c-model-cotacao-email.sql idempotente JSON_MODIFY $.pageTabs[2] preserva slots 0/1 (F092a/F092b) com guards THROW 50948/50949, audit-pass-with-note arqueólogo 6 campos byte-perfect ConfigEmail.jsx ordem+required, anti-violação F111+naming-inventado+appKey-errado em SQL+TS+probe, apply 2x byte-perfect; B) MISSION — 3/4 do epic F092 (F092a+F092b+F092c accepted) consolidando template idempotente; só falta F092d (tab logo) para fechar epic; C) cobertura — model real via `GET /api/model?app=portal-director&path=/configuracoes/cotacao` retorna idModel=18 pageTabs=4 slot[2].key=email genericform.api=`cotacao_sp_consultar_configuracao_email` endPoint=`cotacao_persistir_config_email` + 6 campos ordem ConfigEmail.jsx (smtp/portaSmtp(maskType=inteiro)/usuario/senha required + remetente opcional + ssl checkbox) + pageActions.testEmail.endPoint=`/api/teste-email` (F112 declarativo), slots 0/1/3 preservados, POST proc real → 502 app-unreachable (cotacao :5000 não-up dev, gate runtime F110 fora escopo), idempotência apply 2x byte-perfect (DoD §3 via dotenv-cli), probe 5 PASS + 1 SKIP esperado (DB shape sem DB_* env). Notas registráveis do audit: N1 pageActions shape Studio-canonical não documentado (sub-contrato pendente — enfileirado F116), N2 testEmail endpoint sem rota Studio (D9, soft-gate F112 já enfileirado), N3 ssl coerção dual-shape é responsabilidade renderer (engine-renderer fora escopo desta sub-feature). Débitos declarados D9 herda para F112; D4-D8 herdados de F090/F092a/F092b. Sub aceitável sem botão teste-email runtime cf manifest. Próximo: F092d (tab logo) para fechar epic F092.) (smith 2026-05-18: seed F092c-model-cotacao-email.sql idempotente JSON_MODIFY $.pageTabs[2]; apply 2x DFid=18 pageTabs=4 slot 2 email com genericform.api+endPoint /api/cotacao/proc/... + 6 campos byte-perfect ConfigEmail.jsx (smtp/portaSmtp/usuario/senha required + remetente opcional + ssl checkbox + portaSmtp maskType=inteiro) + pageActions.testEmail.endPoint=/api/teste-email (F112 declarativo); probe 6/6 PASS (DB shape + bridge cotacao Basic R4+Domain R6+URL R7 ambas procs + 3 route gates multi-app-proxy); reuso F110 integral sem rota nova; mitigação F112 declarativa (sub aceitável sem botão runtime cf manifest, débito D9); slots 0 gerais F092a + 1 usuarios F092b PRESERVADOS com guards THROW 50948/50949; anti-violação herdada F111 + naming inventado + appKey errado em SQL+TS+probe; typecheck verde 3 packages; backlog F092c-decisions.md D1-D6 + débito novo D9 (mount /api/teste-email F112) + herdados D4-D8) **ui-tester 2026-05-17**: DoD 1-3 OK. (1) probe 5 PASS + 1 SKIP esperado (DB shape sem DB_* env em invocação direta, validado via DoD §3 dotenv-cli). (2) curl autenticado (identity=`processa` temp-pw 24h, cookie `director_session` reaproveitado de F092b válido até 2026-05-18) `GET /api/model?app=portal-director&path=/configuracoes/cotacao` → 200 idModel=18 pageTabs=4 keys=`gerais/usuarios/email/logo`; slot[2].key=email genericform.api=`/api/cotacao/proc/cotacao_sp_consultar_configuracao_email` genericform.endPoint=`/api/cotacao/proc/cotacao_persistir_config_email` 6 campos ordem ConfigEmail.jsx (smtp/portaSmtp(maskType=inteiro)/usuario/senha required + remetente opcional + ssl checkbox; todos maxLength conforme legado) + pageActions={testEmail:{endPoint:`/api/teste-email`}}; slot[0] gerais preservado (genericform.endPoint=`cotacao_persistir_config_opcoes`); slot[1] usuarios preservado (datagrid.api=`cotacao_sp_obter_usuarios`); slot[3] logo stub intacto. `POST /api/cotacao/proc/cotacao_persistir_config_email` body `{}` → 502 `app-unreachable` (cotacao :5000 não-up dev, paridade F110 gate runtime; 2 chamadas consecutivas idem). (3) idempotência apply 2x via dotenv-cli → outputs byte-perfect, GET /api/model pós-apply confirma idModel=18 pageTabs=4 slot 0/1/2/3 identical. Relatório `test-reports/F092c-cotacao-email.md`. |
| F092d | render | F092 sub-feature 4/4 — tab `logo` (`ConfigLogo.jsx`): model genericform consumindo `cotacao_sp_consultar_logo_cliente` + `cotacao_persistir_config_logo_cliente` via gateway `POST /api/cotacao/proc/:proc` (appKey=`cotacao`, F110). Substitui invenção `acesso.sp_persistir_logo_cotacao`. Gates: F108 + F110 + F113. | `sources/.../Configuracoes/Cotacao/ConfigLogo.jsx` + `sources/.../cotacao/programacao/cotacao_sp_consultar_logo_cliente.sql` + `cotacao_persistir_config_logo_cliente.sql` + sub-model F043 sob `pageTabs[3]` | [[obter-model-pagina]] + [[portal-aws-bridge]] + [[engine-page-tabs]] | P1 | ready-for-test | ✓ 2026-05-17 | — (smith 2026-05-17: seed F092d-model-cotacao-logo.sql idempotente JSON_MODIFY $.pageTabs[3]; apply 2x DFid=18 pageTabs=4 slot 3 logo com shape logoUploader.api=/api/cotacao/proc/cotacao_sp_consultar_logo_cliente + logoUploader.endPoint=/api/cotacao/proc/cotacao_persistir_config_logo_cliente + payloadProp=novoLogo (cf linha 28) + currentImageProp=logo (cf linha 98) + maxLengthProp=maxLength (cf linha 45) + accept=.jpeg,.jpg,.png + acceptMimeTypes=[image/jpeg,image/png] (cf linha 60) + strings literais legado (emptySelectionMessage/selectFileLabel/extensionsHint/submitLabel); probe 6/6 PASS (DB shape + bridge cotacao Basic R4+Domain R6+URL R7 ambas procs + 3 route gates multi-app-proxy); reuso F110 integral sem rota nova; slots 0 gerais F092a + 1 usuarios F092b + 2 email F092c PRESERVADOS com guards THROW 50960/50961/50962; anti-violação herdada F111 + naming inventado (sp_persistir_logo_cotacao/sp_consultar_logo_cotacao) + appKey errado em SQL+TS+probe; typecheck verde 3 packages 3.476s; backlog F092d-decisions.md D1-D6 + débito novo D10 (engine renderer logoUploader sistêmico) + herdados D4-D9. FECHA EPIC F092 quando aceito.) **ui-tester 2026-05-17 (retry VPN ativa)**: DoD 1-3 OK. (1) probe 6/6 PASS — VPN ativa permitiu DB shape direto (`DFid_model_pagina=18; pageTabs=4; logo.logoUploader.api+endPoint OK; payloadProp/currentImageProp/maxLengthProp OK; accept=.jpeg,.jpg,.png; slots 0/1/2 preservados`) + bridge cotacao ambas procs Basic R4+Domain=imperial R6+URL /api/proc/<proc> R7 + 3 route gates (no-session→503/health→200/uppercase→503). (2) curl autenticado (identity=`processa` temp-pw 24h, cookie `director_session` reaproveitado válido até 2026-05-18) `GET /api/model?app=portal-director&path=/configuracoes/cotacao` → HTTP 200 idModel=18 idPagina=7 pageTabs=4 keys=`gerais/usuarios/email/logo`; **slot[3].key=logo** label=`Logo` model.logoUploader shape canônica: api=`/api/cotacao/proc/cotacao_sp_consultar_logo_cliente` + endPoint=`/api/cotacao/proc/cotacao_persistir_config_logo_cliente` + payloadProp=`novoLogo` + currentImageProp=`logo` + maxLengthProp=`maxLength` + accept=`.jpeg,.jpg,.png` + acceptMimeTypes=[`image/jpeg`,`image/png`] + strings legado preservadas (genericPageTitle/Description/emptySelectionMessage/selectFileLabel/extensionsHint/submitLabel); slot[0] gerais.endPoint=`cotacao_persistir_config_opcoes` preservado; slot[1] usuarios.datagrid.api=`cotacao_sp_obter_usuarios` preservado; slot[2] email.endPoint=`cotacao_persistir_config_email` preservado. (3) idempotência apply 2x via dotenv-cli → outputs byte-perfect (`OK — DFid_model_pagina=18; pageTabs=4; pageTabs[3]=logo com logoUploader.api+endPoint...; slots 0 gerais + 1 usuarios + 2 email preservados`); GET /api/model pós-apply confirma modelJson serializado pré=4138B / pós=4138B / equal=true. Anti-violação F111 confirmada no payload (sem /portal-aws/, sem /cotacao/proc/ legado, sem sp_*_logo_cotacao). Relatório `test-reports/F092d-cotacao-logo.md`. **FECHA EPIC F092 quando curator aceitar.** |
| F093 | render | F052b refactor — page `configuracoes_email` (model F043 #4) **+ absorve F091 (`configuracoes_agendamento` model #2)**: reescrever AMBOS os models F043 #2 e #4 para gateway `/portal-aws/proc/agent.sp_consultar_configuracao_email` + `agent.persistir_config_email_agendamento` + `/api/teste-email` (testEmailApi). Padrão real em `PortalDirector.Website/src/routes/Configuracoes/Agendamento.jsx` (tab email via `<ConfigEmail appKey='agent'>`) e demais consumos de ConfigEmail. Substitui `acesso.sp_persistir_configuracao_email` + `acesso.sp_persistir_configuracao_agendamento` (ambos inventados). Decisão curator 2026-05-17: F091 fundida aqui — Agendamento.jsx no Director é email-only; procs `persistir_definicoes_agendamento`+`persistir_data_bloqueio_agendamento` saem do escopo cutover-fase-1 (Director) e migram para F107 (AppBuilder/agent-UI quando F015 reabrir). Gate: F048 runtime-stable. **archaeologist follow-up (F113, 2026-05-17)**: appKey `agent` está presente em apenas **43/89** bases cross-tenant; **Imperial Logística NÃO tem `agent`** (asserção R24 do [[tbaplicacao-registry]]). Cutover F093 em Imperial bloqueia em runtime ao chamar `agent.sp_consultar_configuracao_email` se `acesso.TBaplicacao WHERE DFchave='agent'` retorna 0 linhas. Mitigação: (a) seed `agent` em Imperial pré-cutover; (b) F093 com fallback para tab-email-Cotacao via `<ConfigEmail appKey='cotacao'>`; (c) F093 detecta ausência e exibe estado vazio. Decidir antes da implementação. | `sources/.../Configuracoes/Email/*` + `sources/.../Configuracoes/Agendamento.jsx` (tab email) + `sources/.../agent/programacao/agent.persistir_config_email_agendamento.sql` + `agent.sp_consultar_configuracao_email.sql` + models seed F043 #2 e #4 | [[obter-model-pagina]] + [[portal-aws-bridge]] + [[tbaplicacao-registry]] | P1 | todo | — | — (gate: F048 runtime-stable; absorve F091 — DoD inclui ambos models #2 e #4 reescritos no mesmo refactor; nova dependência: decidir mitigação `agent`-ausente cross-tenant via F113 survey) |
| F094 | render | F052b refactor — page `configuracoes_aws` (model F043 #5, `Configuracoes/IntegradorAWS/*`): reescrever para gateway `/portal-aws/proc/<schema>.<proc>` — escavar procs reais usadas pelo `IntegradorAWS` no `PortalDirector.Website` antes da implementação (não foram listadas explicitamente no laudo F052b — sub-escavação pré-implementação obrigatória; arqueólogo confirma e congela escopo antes do smith iniciar). Substitui `acesso.sp_persistir_configuracao_aws` (inventado). Gate: F048 runtime-stable + sub-escavação de procs IntegradorAWS. | `sources/.../Configuracoes/IntegradorAWS/*` + procs portal-aws a confirmar + model seed F043 | [[obter-model-pagina]] + [[portal-aws-bridge]] | P1 | todo | — | — (gate: F048 runtime-stable + sub-escavação archaeologist) |
| F095 | render | F042 follow-up — survey de separador real em `acesso.TBpagina.DFchaves_aplicacoes` (vírgula? pipe? semicolon? espaço?) cross-tenant para refinar predicado LIKE atual (`DFchaves_aplicacoes LIKE '%<appKey>%'` arrisca false-positives por substring — ex: `wms` casa `wms2`, `app=admin` casa `appbuilder-admin`). Após survey, decidir: (a) trocar para split exato server-side via `STRING_SPLIT(DFchaves_aplicacoes, '<sep>')` + filtro `value=@appKey`; (b) manter LIKE com guards (`%<sep><appKey><sep>%` + sentinelas nas bordas); (c) coluna estruturada nova (descartado se quebrar paridade). Não bloqueia cutover — F042 já aceita. | `acesso.TBpagina.DFchaves_aplicacoes` cross-tenant survey + `apps/api/src/routes/model.ts` handler reescrito | [[obter-model-pagina]] §"Asserção F042 — schema survey" | P3 | todo | — | — |
| F096 | render | F042 follow-up — validar `acesso.TBpagina.DFchave` unicidade cross-app: rodar `SELECT DFchave, COUNT(*) FROM acesso.TBpagina GROUP BY DFchave HAVING COUNT(*) > 1` cross-tenant. Se duplicatas existirem, JOIN canônico `TBpagina.DFchave = TBmodel_pagina.DFchave_pagina` no F042 produz multi-row para a mesma página em apps diferentes — handler atual retorna primeira linha (não-determinístico). Mitigação: ORDER BY explícito + LIMIT 1 com critério de tie-break (e.g., app-key prefer exact > LIKE), ou JOIN composto incluindo DFid_aplicacao. Não bloqueia cutover — F042 já aceita. | `acesso.TBpagina` survey cross-tenant + handler `apps/api/src/routes/model.ts` two-step | [[obter-model-pagina]] §"Asserção F042 — schema survey" | P3 | todo | — | — |
| F097 | components | F056 follow-up — persistir share-links em SQL (`acesso.TBshare_link`). Hoje store em-memória (`Map` em-processo em `apps/api/src/routes/share.ts`) — perde estado em restart, não escala multi-instância. Migração: criar tabela com colunas (token UUID, dashboards JSON, mode, ttl_expires_at, rotate_interval, created_by, created_at, revoked_at). Archaeologist precisa decidir se tabela é nova canônica ou se existe equivalente legado. Bloqueia produção real de F056. | `acesso.TBshare_link` (a criar) + `apps/api/src/routes/share.ts` | [[model-valor-dashboard]] (share-link sub-contrato a documentar) | P2 | todo | — | — |
| F098 | components | F056 follow-up — fix fallback cookie name hardcoded em `apps/api/src/routes/share.ts:50` (`director_studio_session` ≠ `director_session` do `.env` `SESSION_COOKIE_NAME`). Trivial: ler de env ou usar mesma constante que o resto da API. Não bloqueia (em-prod o env override resolve), mas é defeito latente. | `apps/api/src/routes/share.ts:50` | n/a | P3 | todo | — | — |
| F099 | components | F056 follow-up — renderer real de widgets em share-page (hoje `ValidView` mostra só metadata do payload resolve). Substituir por `DashboardSurface` read-only com endpoint mediado `/api/share/:token/widget/:id/data` (precisa redesenhar `useDashboard` hook para aceitar credencial=token em vez de cookie de sessão). Sub-contrato dashboard-shared-link pendente. | `apps/director-studio/src/routes/share/*` + `packages/ui/src/components/dashboard-surface.tsx` + hook `useDashboard` | [[model-valor-dashboard]] | P3 | todo | — | — |
| F100 | components | F056 follow-up — rate-limit em `POST /api/share/create` para mitigar abuso (criação massiva de links). Sugerido: rate-limiter por session-user (e.g., 60/hora) + cap global. Não bloqueia cutover, é hardening. | `apps/api/src/routes/share.ts` (middleware rate-limit) | n/a | P3 | todo | — | — |
| F101 | admin | F063 follow-up (FU1) — listar papéis em produção (Imperial + demais tenants) que devem ganhar acesso ao Dashboard e adicionar `INSERT/UPDATE` real em `acesso.TBpapel_funcao_pagina_modulo` (DFid_paginas/DFid_modulos CSV), substituindo o bloco comentado no seed F063. Decisão de negócio cross-tenant. | `acesso.TBpapel_funcao_pagina_modulo` + `infra/sql/seed/F063-dashboard-menu.sql` | [[acl-papel-funcao-pagina]] | P3 | todo | — | — |
| F102 | admin | F063 follow-up (FU2) — replicar seed de menu dashboard em outras Áreas (WMS, agendamento, cotacao) conforme cada Área ganhar Dashboard próprio. Via `DFchaves_aplicacoes` string-list ou novas linhas de `TBpagina`. WMS já tem módulo órfão `wms.dashboard` id=10 sem filhos — candidato natural. | `acesso.TBpagina` + `acesso.TBmodulo` cross-app | [[menu-hierarquia]] | P3 | todo | — | — |
| F103 | components | F063 follow-up (FU3) — smoke route `apps/director-studio/src/routes/smoke-f063.tsx` com link visualmente verificável para `/app/dashboard` — útil quando ui-tester for revalidar end-to-end UI (após desbloqueio Vite). | `apps/director-studio/src/routes/smoke-f063.tsx` (novo) | [[menu-hierarquia]] | P3 | todo | — | — |
| F104 | auth | F-tbobjetos-dashboard follow-up (D-7) — escavar e publicar sub-contrato `tbusuario-multi-tenant.md` resolvendo divergência entre `dbo.TBusuario` (referenciada no seed/cruzamento super-user da Area 52) e `acesso.TBusuario` (schema canônico documentado em legacy-contracts). Determinar: (a) qual é a tabela canônica cross-tenant; (b) se `dbo.TBusuario` é resíduo de migração legada, alias/sinônimo, ou tabela paralela ativa em sub-conjunto de bases; (c) impacto no resolver de identity/ACL do Studio (potencial bug latente em features que assumem `acesso.TBusuario` exclusivo). Survey cross-DB obrigatório (`INFORMATION_SCHEMA.TABLES` para `TBusuario` em ambos schemas em todas as bases DBdirector_*). Não bloqueia cutover de F-tbobjetos-dashboard. | `dbo.TBusuario` + `acesso.TBusuario` cross-tenant survey + `sources/.../portal.director/criacao/acesso.TBusuario.sql` | [[tbusuario-multi-tenant]] (a criar) | P2 | todo | — | — |
| F105 | infra | F-tbobjetos-dashboard follow-up — cosmético: ajustar comentário-cabeçalho do seed `apply-tbdashboard-seed.ts` (ou equivalente) para alinhar nomenclatura `studio.* → acesso.*` (chaves de widget usam prefixo `studio.demo.*` mas inserem em schema `acesso`; cabeçalho deve refletir essa convenção para evitar confusão futura). Trivial, zero impacto funcional. | `apps/api/src/scripts/apply-tbdashboard-seed.ts` (cabeçalho) | [[tbdashboard-catalog]] | P3 | todo | — | — |
| F106 | render | F090 follow-up — survey cross-tenant do literal `datagrid.api` em models legados (e.g. `acessos_fornecedor`) para confirmar se URL é `/api/portal-aws/proc/...` (paridade verdadeira) ou `/portal-aws/proc/...` (decisão Studio). Se for `/api/*` no legado: ajustar mount Studio para `/api/portal-aws/proc/:proc` ou registrar divergência consciente em portal-aws-bridge.md. Corrigir frase contrato (linhas 159-163) sobre equivalência (é com PortalAwsClient.SendRequest hardcoded, não com ExecProc genérico). | n/a (follow-up F090) + `acesso.TBmodel_pagina` cross-tenant | [[portal-aws-bridge]] + [[obter-model-pagina]] | P3 | todo | — | — |
| F108 | render | **Engine meta-model `pageTabs`** — extensão do F043/F009 para pages compostas. JSON shape: `{ pageTabs: [{ key, label, model: <sub-model-F043> }] }`. Renderer-de-tab monta `<PageTabs>` (UI já existe em F014 accepted) e chama engine F009 recursivamente para cada sub-model. Habilita pages multi-tab schema-driven (Cotacao 4 tabs proc-based F092a-d; provavelmente também `configuracoes_aws` F094 se sub-survey confirmar shape composto). Decisão curator 2026-05-17 (F092 split B+gancho-C). Contrato novo `[[engine-page-tabs]]` a criar. Bloqueia F092 inteiro. | `apps/api/src/engine/` (renderer-tabs) + `packages/ui/src/components/page-tabs.tsx` (já existe F014) + `acesso.TBmodel_pagina` (sub-models aninhados) | [[engine-page-tabs]] (a criar) + [[obter-model-pagina]] | P0 | deferred (esbuild bloqueia /smoke/f108; smith+arch pass) | — | — (gate F092; novo contrato `engine-page-tabs` a publicar antes da implementação) |
| F109 | render | **Cutover-fase-2 (AppBuilder/Cotacao.Integrador)** — 5 tabs REST não-proc de `Configuracoes/Cotacao/*`: `consulta` (`CotacaoConsulta.jsx`), `sincronizar`, `monitorar`, `opcoes` (`CotacaoOpcoes.jsx` get/post), `utilitarios` (`statusweb`). Consomem WebAPI .NET `Cotacao.Integrador` (`/api/cotacaointegrador/*`) — app separada em `sources/engenharia--fabrica--dotnet*` (provável `processa.appbuilder/` ou módulo dedicado). Decisão curator 2026-05-17 (F092 split): saem do cutover-fase-1 (Director.Studio). Shape gemelar a F107. Gate F015 reaberto + survey archaeologist (base URL, handshake, config em `TBaplicacao(cotacaointegrador)`?) + renderer "REST não-proc com options-grid" novo. NÃO bloqueia cutover Director. | `sources/.../Configuracoes/Cotacao/{CotacaoConsulta,CotacaoSincronizar,CotacaoMonitorar,CotacaoOpcoes,CotacaoUtilitarios}.jsx` + WebAPI `Cotacao.Integrador` (a escavar) | n/a (futuro AppBuilder cutover) | P3 | deferred | — | — (curator 2026-05-17: gate F015 reaberto + survey archaeologist; shape gemelar F107) |
| F110 | infra | **Proxy multi-app** `POST /api/:appKey/proc/:proc` com discovery dinâmico em `acesso.TBaplicacao(<appKey>)` para resolver host/port/secret por tenant. Cobre cotacao (F092a-d), agent (F093 já mira), e qualquer appKey futuro registrado. Mantém `POST /portal-aws/proc/:proc` literal de F090 intacto (F106 reconcilia `/api/portal-aws/` débito em paralelo). Decisão curator 2026-05-17 (F092 split). Bloqueia F092a-d. Gate F113 ✓ resolvido 2026-05-17. Contrato [[tbaplicacao-registry]] publicado com R1-R24 — F110 implementação obrigada a replicar: (i) lookup por `DFchave` (case-sensitive kebab-case, R10/R23); (ii) auth-scheme switch `case "cotacao" → Basic("processa\|{DFdominio}:99")`, `default → Bearer JWT(DirectorIdentity fixa)`, header extra `Domain:{DFdominio}` em todas (R4-R6); (iii) URL `{DFendereco}/api/proc/{proc}` literal sem normalizar trailing slash (R7-R9); (iv) ignorar `DFhabilitado` (proxy não filtra, só menu filtra — R11/R13). | `apps/api/src/routes/multi-app-proxy.ts` (novo) + `apps/api/src/app-registry.ts` (novo, lookup+cache+auth-scheme) + `acesso.TBaplicacao` + integração com `PortalAwsClient` patterns de F024 | [[portal-aws-bridge]] §"Multi-app discovery" (a estender) + [[tbaplicacao-registry]] | P0 | accepted | ✓ 2026-05-17 | ✓ 2026-05-17 (Curator critério triplo OK: A) técnico — app-registry+multi-app-proxy implementados respeitando R1-R24 do [[tbaplicacao-registry]], audit-pass-with-note arqueólogo com 3 notas registráveis (R5 TTL JWT, R7 coexistência F090, R19 cotacao-integrador gap); B) MISSION — enabler P0 caminho crítico, desbloqueia F092a-d + qualquer appKey futuro; C) cobertura — probe 13/13 PASS + 6 curls reais autenticados cobrindo 5 appKeys (cotacao Basic R4, agent ausente R24 Imperial, Invalid_Key R10/R23, portal-aws coexistência F090, setup precedência rotas literais). Enfileirados F114 (P3 confirmar TTL JWT real legado — nota R5) e F115 (P3 case especial cotacao-integrador quando consumer real emergir — nota R19). Desbloqueia F092a-d.) — smith 2026-05-17: app-registry.ts (lookup TBaplicacao cache 60s + switch Basic/Bearer + headers Domain + URL literal sem normalizar trailing + kebab-case validator), multi-app-proxy.ts (POST /:appKey/proc/:proc + GET /:appKey/health, gate sessão, identity-de-serviço FIXA R5 não propaga usuário, classify errors sem vazar host), montado em /api por último (rotas literais ganham precedência), F090 literal /portal-aws/proc/:proc intacto. Probe probe-f110.ts 13/13 PASS — 5 helpers + 4 cenários DoD (cotacao Basic, portal-aws Bearer, invalido 404, disabled-still-works R11) + registry invalid-key + 3 route gates. STUDIO_AWS_JWT_SECRET reusado como Consts.SecretKey deploy-wide. Typecheck verde 3 packages. Backlog F110-decisions.md D1-D12. **ui-tester 2026-05-17**: probe 13/13 PASS reconfirmado. Curl autenticado (identity=`processa` temp-pw 24h, cookie `director_session`): `GET /api/cotacao/health` → 200 (appKey cotacao em Imperial, authScheme=basic R4); `POST /api/cotacao/proc/cotacao_obter_config_opcoes` body `{}` → 502 `app-unreachable` (backend cotacao real :5000 não-up no host local dev — gate runtime, fora do escopo F110); `GET /api/agent/health` → 404 `app-not-found` (Imperial sem appKey `agent`, paridade R24); `GET /api/Invalid_Key/health` → 400 `invalid-app-key` (R10/R23 kebab-case minúsculo); `GET /api/portal-aws/health` → 200 (coexistência F090, authScheme=bearer default R5); `GET /api/setup/status` → 200 sem cookie (precedência rotas literais sobre wildcard `/api/:appKey/*` confirmada). Relatório `test-reports/F110-multi-app-proxy.md`. Desbloqueia F092a-d.) |
| F111 | render | **F092b survey** — path divergente `/cotacao/proc/...` (sem `/api`-prefix) em `CotacaoUsuarios.jsx` legado. Archaeologist confirma: (a) bug legado (path errado, NGINX/IIS wildcard reverse-proxy salvou em produção); (b) intenção (mount separado em backend distinto); ou (c) artefato histórico. Mitigação imediata F092b: usar path canônico `/api/cotacao/proc/...` (paridade com `useAppClient` default). Survey decide se ajuste é necessário. NÃO bloqueia F092b (soft-gate, mitigação documentada). | `sources/.../Configuracoes/Cotacao/CotacaoUsuarios.jsx` + reverse-proxy config legado (se acessível) + `acesso.TBaplicacao(cotacao)` mount survey | [[portal-aws-bridge]] + (eventual) [[tbaplicacao-registry]] | P2 | todo | — | — (soft-gate F092b; mitigação canônica `/api/cotacao/proc/...` aplicada por default) |
| F112 | infra | **`/api/teste-email`** — escavar backend real do endpoint legado de teste de email (`testEmailApi` do `<ConfigEmail>`). Hipóteses: (a) rota plain REST em `Director.Website` controller dedicado; (b) proxy via gateway para serviço SMTP; (c) appbuilder/agent endpoint. Studio precisa montar paridade — sem ela, botão "Testar email" do tab email (F092c) quebra. Survey archaeologist + decisão de mount (proxy vs implementação Studio nativa). Soft-bloqueia F092c (sub aceitável sem botão teste-email, débito declarado). | `sources/.../*Email*Test*` + `sources/.../PortalDirector.Website/Controllers/*Email*` + `acesso.TBaplicacao` (se proxy) | [[portal-aws-bridge]] §"Endpoints utilitários" (a estender) | P2 | todo | — | — (soft-gate F092c) |
| F113 | infra | **Survey archaeologist** — `acesso.TBaplicacao(<appKey>='cotacao')` no tenant Imperial Logística (`DBdirector_imperial_logistica_29`): existe row com host/port/secret/auth-config para appKey `cotacao`? Cross-tenant survey de TBaplicacao geral também (descobrir universo de appKeys distintos em produção: `cotacao`, `agent`, `portal-aws`, `wms`, ...). Bloqueia F110 (sem dado de TBaplicacao(cotacao), proxy multi-app não tem o que descobrir; cutover Cotacao no Studio fica bloqueado por dado de tenant, não por código). Output: contrato `[[tbaplicacao-registry]]` documentando colunas reais + universo de appKeys. archaeologist 2026-05-17: survey cross-tenant 89/97 bases (`172.27.0.121\SQL2k19`, SQL auth `sl/123`), 970 linhas, **23 appKeys distintos**. **Imperial cotacao confirmado**: `DFid=5, DFendereco='http://127.0.0.1:5000', DFdominio='cotacao', DFhabilitado=0` — row existe (R2/R3), endereço é dev/loopback (prod típico `http://52.67.203.133:5000` com `DFdominio=<tenant-slug>`). Núcleo estável 9 appKeys (≥87 bases): `portal-aws, portal-director, edoc, pipeliner, cotacao-integrador, processaadm, cotacao, processa-sped, integrador-aws`; opt-in: `wms(56), agent(43), gerenciamento-de-integracoes(35), checkin(34)`. **Imperial NÃO tem appKey `agent`** (R24) → alerta para F093 cross-tenant. Auth-scheme legado mapeado: `case "cotacao" → Basic("processa\|{DFdominio}:99")`, `default → Bearer JWT(DirectorIdentity fixa "processa"/Domain=DFdominio)`, header extra `Domain` em todas as chamadas (R4-R6). `DFchave` é discriminador estável cross-tenant; `DFid_aplicacao` varia per-tenant (R16). 24 asserções publicadas no contrato. | `acesso.TBaplicacao` cross-tenant (89/97 bases DBdirector_* em SERVERSQL\SQL2k19) | [[tbaplicacao-registry]] | P1 | accepted | ✓ 2026-05-17 (archaeologist: survey cross-tenant 89/97 bases, 23 appKeys, Imperial cotacao confirmado existente, contrato com 24 asserções) | — (curator pendente) |
| F107 | render | **Cutover-fase-2 (AppBuilder/agent-UI)** — definições+bloqueio de agendamento. Após decisão F091-fundida-em-F093 (curator 2026-05-17), as procs `agent.persistir_definicoes_agendamento`, `agent.obter_definicoes_agendamento`, `agent.persistir_data_bloqueio_agendamento`, `agent.deletar_data_bloqueio_agendamento` (existem em `sources/.../portal-aws/agent/programacao/*.sql` mas zero consumo no PortalDirector.Website) saem do escopo cutover-fase-1 (Director.Studio). Quando F015 reabrir para AppBuilder, archaeologist faz survey do cliente real dessas procs (provável `sources/engenharia--fabrica--dotnet--processa.appbuilder/` UI de agent ou app interno) e curator cria features dedicadas no manifest do AppBuilder cutover. NÃO bloqueia cutover Director. | `sources/.../portal-aws/agent/programacao/agent.{persistir,obter}_definicoes_agendamento.sql` + `agent.{persistir,deletar}_data_bloqueio_agendamento.sql` + cliente real a escavar (não-Director) | n/a (futuro AppBuilder cutover) | P3 | deferred | — | — (curator 2026-05-17: gate F015 reaberto + survey archaeologist do cliente real fora do Director) |
| F114 | infra | **F110 follow-up — confirmar TTL JWT real legado** (nota R5 do [[tbaplicacao-registry]]). Smith assumiu TTL 24h para o Bearer JWT emitido contra apps non-`cotacao` (default scheme). Arqueólogo deve escavar `AppClientService.cs` / `JwtTokenBuilder` / equivalente no legado .NET (`sources/engenharia--fabrica--dotnet*` ou `sources/engenharia--fabrica--dotnet-core--director*`) e confirmar o `expires`/`SecondsValid` real usado em produção. Se divergir de 24h, ajustar `app-registry.ts`. NÃO bloqueia cutover — F110 aceito com asserção provisória registrada. | `sources/engenharia--fabrica--dotnet*` (AppClientService/JwtTokenBuilder) + `apps/api/src/app-registry.ts` | [[tbaplicacao-registry]] §R5 | P3 | todo | — | — (curator 2026-05-17: enfileirado a partir da nota R5 do audit F110) |
| F115 | infra | **F110 follow-up — case especial `cotacao-integrador`** (nota R19 do [[tbaplicacao-registry]]). Survey arqueológico mostrou `cotacao-integrador` como appKey distinto de `cotacao` (universo 23 appKeys, núcleo estável). Auth-scheme do `cotacao-integrador` não está mapeado no switch atual (cai no default Bearer). Quando consumer real emergir (provável F109 cutover-fase-2 AppBuilder, REST não-proc `/api/cotacaointegrador/*`), arqueólogo escava se o legado usa Basic (gemelar `cotacao`) ou Bearer (default). Se Basic, ampliar `case "cotacao"` para incluir `"cotacao-integrador"`. NÃO bloqueia cutover-fase-1 — gap declarado, fora do escopo F092a-d/F093. | `sources/.../Cotacao.Integrador*` (a escavar) + `apps/api/src/app-registry.ts` (auth-scheme switch) | [[tbaplicacao-registry]] §R19 | P3 | todo | — | — (curator 2026-05-17: enfileirado a partir da nota R19 do audit F110; gate F109 reabrir) |
