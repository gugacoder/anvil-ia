---
title: "Director.Studio — Feature Manifest"
aliases: [feature-manifest, director-studio-manifest]
tags: [effort, director-studio, manifest, scope]
created: 2026-05-15
updated: 2026-05-15
---

# Feature Manifest — Director.Studio

> Tabela mestre de features. **100% RTM**, sem exceção. Curator preenche `priority` e `accepted`; archaeologist preenche `source` e `contract`; smith preenche `status`; ui-tester preenche `tested`.

## Convenções

- **Status**: `todo` → `wip` → `ready-for-test` → `tested` → `accepted`
- **Cobertura mandatória**: nenhuma feature do react-tools/AppMain pode ficar de fora.
- **Critério de "done"**: passa em `ui-tester` contra o contrato + spec do design system, com pelo menos um caso real (não fixture) da Área 52.
- **Sem MVP, sem mock, sem placeholder**. Se faltar contrato, archaeologist é chamado; se faltar UX, designer é chamado.

## Áreas

A descobrir pelo arqueólogo. Seed inicial baseado no que já mapeamos:

- **infra**: bootstrap, .env wizard, sessão, observabilidade
- **auth**: 5 caminhos do `processa-auth-paths`
- **shell**: boot gate, layout, sidebar, header, theme, breadcrumbs
- **render**: engine schema-driven que consome `acesso.obter_model_pagina`
- **components**: tipos de `TBmodel_pagina.DFtipo` (form, grid, dashboard, tabs, tree, wizard, ...)
- **integrations**: bridge AWS, hub SignalR (= SSE no Studio), portal-aws cross-tenant
- **admin**: cadastros do próprio AppBuilder (TBaplicacao, TBmodel_pagina, etc.) renderizados pelo Studio

## Features

| ID | Área | Feature | Source (sources/...) | Contract (atlas link) | Priority | Status | Tested | Accepted |
|---|---|---|---|---|---|---|---|---|
| F001 | infra | Setup wizard de primeira execução | n/a (próprio) | n/a | P0 | done | — | — |
| F002 | infra | Boot gate + splashscreen | n/a (próprio) | n/a | P0 | done | — | — |
| F003 | auth | Login híbrido (5 caminhos) | `processa.sdk/Fontes/Processa.Sdk.Auth/` | [[processa-auth-paths]] | P0 | accepted | ✓ 2026-05-15 | ✓ 2026-05-15 |
| F004 | auth | Sessão httpOnly cookie + Redis | derivado | [[processa-auth-paths]] | P0 | accepted | ✓ 2026-05-15 | ✓ 2026-05-15 |
| F005 | shell | App layout (sidebar + header) | `react-tools/AppMain/` | [[app-main]] | P0 | accepted | ✓ 2026-05-15 (7/8, C2 mobile → F033) | ✓ 2026-05-15 |
| F006 | shell | Theme claro/escuro/auto (default auto) | n/a (novo) | n/a | P0 | accepted | ✓ 2026-05-15 | ✓ 2026-05-15 |
| F007 | shell | Menu hierárquico (proc canônica: `acesso.obter_acl_token`; `obter_rotas_aplicacao` é auxiliar) | `acesso.obter_acl_token.sql` + `react-tools/hooks/useAcl.js` + `react-tools/components/AppMain/AppSidebar.js` | [[menu-hierarquia]] + [[acesso-obter-rotas-aplicacao]] | P0 | accepted | ✓ 2026-05-15 (C7 mobile → F033) | ✓ 2026-05-15 |
| F008 | shell | ACL por papel × função × página (proc canônica: `acesso.obter_acl_token` — mesma de F007; `obter_acl_usuario_aplicacao` é proc paralela, retorna papéis/recursos crus) | `acesso.obter_acl_token.sql` + `react-tools/hooks/useAcl.js` + `react-tools/components/GenericPage/GenericTabPage.js` (+ tabelas `acesso.TBpapel*`, `acesso.TBrecurso_adicional`, `acesso.TBfuncao`, `acesso.TBmodulo`) | [[acl-papel-funcao-pagina]] | P0 | accepted | ✓ 2026-05-15 (7/8, C5 denial n/a → F035) | ✓ 2026-05-15 |
| F009 | render | Engine schema-driven (dispatch por presença de chave, **não** por DFtipo — vide contrato) | `react-tools/components/GenericPages/` + `react-tools/components/GenericPage/` + `acesso.obter_model_pagina` + `PortalDirector.Repositories/GenericPagesRepository.cs` | [[engine-schema-driven]] + [[obter-model-pagina]] | P0 | accepted | ✓ 2026-05-15 (8/8; C3 exercitado via fetch-stub p/ contornar ACL — única page com model na base é `wms.cadastros_grupo-de-trabalho`, fora do menu PROCESSA — comportamento do engine + payload real do banco; sem regressão F007/F008) | ✓ 2026-05-15 (stub C3 cobre só pareamento URL→pageKey; engine+fetch+parse+dispatch são reais; F043 enfileirada p/ destravar F010+) |
| F010 | components | Renderer DFtipo=form (GenericForm) | `react-tools/components/GenericForm/` | [[model-valor-genericform]] | P0 | accepted | ✓ 2026-05-15 (8/8, C5 n/a → F043+C5-revisit) | ✓ 2026-05-15 |
| F011 | components | Renderer DFtipo=grid (DataGrid2) | `react-tools/components/DataGrid2/` + `react-tools/components/GenericPage/GenericGridPage.js` + `react-tools/hooks/useDataGrid.js` | [[model-valor-datagrid]] | P0 | accepted | ✓ 2026-05-15 (12/13, BOM via curl hexdump; C4 backend SQL case-sensitivity → F044; C9 delete gridAction stub → F045) | ✓ 2026-05-15 |
| F012 | components | Renderer DFtipo=dashboard | `react-tools/components/DashBoard/` | [[model-valor-dashboard]] | P1 | wip | — | — |
| F013 | components | Renderer DFtipo=tree (SearchTree) | `react-tools/components/SearchTree/` | TBD | P1 | todo | — | — |
| F014 | components | Renderer DFtipo=tabs | `react-tools/components/PageTabs/` | TBD | P1 | todo | — | — |
| F015 | components | Renderer DFtipo=wizard / Steps | `react-tools/components/GenericPages/` | TBD | P1 | todo | — | — |
| F016 | components | Filtros (Filtro/) | `react-tools/components/Filtro/` | TBD | P1 | todo | — | — |
| F017 | components | FileBrowser | `react-tools/components/FileBrowser/` | TBD | P2 | todo | — | — |
| F018 | components | DateTimePicker / DateInterval | `react-tools/components/DateComponents/` | TBD | P1 | todo | — | — |
| F019 | components | PowerSelect (typeahead + async) | `react-tools/components/PowerSelect3/` | TBD | P1 | todo | — | — |
| F020 | components | Notifications (toast/banner) | `react-tools/components/Notifications/` | TBD | P1 | todo | — | — |
| F021 | components | Modal / ActionModal | `react-tools/components/ActionModal/` | TBD | P1 | todo | — | — |
| F022 | components | ReorderableGrid | `react-tools/components/ReorderableGrid/` | TBD | P2 | todo | — | — |
| F023 | integrations | Hub realtime (substitui SignalR por SSE) | `PortalDirector.Aplicacao/Handlers/HubMobileHandler.cs` + `PortalDirector.Aplicacao/Handlers/HubResponseHandler.cs` + `Processa.Sdk.IOC/ProcessaHub.cs` + `Processa.Sdk.IOC/HubClient.cs` + `Processa.ADM.Aplicacao/Controllers/HubController.cs` + `Processa.ADM.Website/src/hooks/useHub.jsx` | [[hub-signalr-legacy]] + [[hub-sse-mapping]] | P0 | accepted | ✓ 2026-05-15 (10/10 PROCESSA/99 IMPERIAL LOG; SSE+publish+snapshot+heartbeat+LEID resync+filtragem target server-side; HubStatus shell-mount → F046; bus multi-instância Redis Pub/Sub → F047) | ✓ 2026-05-15 |
| F024 | integrations | Bridge AWS (sync + auth) | `PortalDirector.Api/PortalAwsClient.cs` + `PortalDirector.Aplicacao/Controllers/IntegradorAwsController.cs` + `PortalDirector.Aplicacao/Controllers/UtilsController.cs` + `processa.agendamento/programacao/aws_sincronizar_entidade.sql` + `portal-aws/portal/integracao/portal.sincronizar_*.sql` | [[portal-aws-bridge]] | P0 | accepted | ✓ 2026-05-15 | ✓ 2026-05-15 (8/8 PROCESSA/IMPERIAL LOG; PortalAwsClient + endpoints health/sync/proxy + JWT HS256 + sanitização mssql 503/502 sem vazar host; smoke real ERP↔AWS enfileirado em F048; JWT secret deploy-wide via STUDIO_AWS_JWT_SECRET — wizard step enfileirado em F049) |
| F025 | admin | Cadastro de Aplicações (TBaplicacao) | `appbuilder/website/src/routes/` | TBD | P1 | todo | — | — |
| F026 | admin | Cadastro de Páginas/Models (TBmodel_pagina) | `appbuilder/website/src/routes/Pipeliner/` | TBD | P1 | todo | — | — |
| F027 | shell | Anti-FOUC theme bootstrap inline (script em index.html aplica `.dark` antes do mount React) | n/a (novo, derivado de F006) | n/a | P1 | todo | — | — |
| F028 | auth | Atualizar contrato `processa-auth-paths` (DFnome_empresa → DFnome_fantasia; alinhar com .cs canônico + DDL) | `processa.sdk/Fontes/Processa.Sdk.Api/Settings.cs` | [[processa-auth-paths]] | P1 | todo | — | — |
| F029 | auth | Alinhar nome do cookie de sessão entre spec e impl (`director_session` vs `director_studio_session`) | n/a (próprio) | [[processa-auth-paths]] | P2 | todo | — | — |
| F030 | components | FormField — `aria-invalid="true"` quando em estado error (spec [[form-field]] §95) | n/a (próprio) | [[form-field]] | P1 | todo | — | — |
| F031 | infra | Setup wizard deve escrever `REDIS_URL=redis://localhost:${REDIS_PORT}` derivado de PREFIX (não hardcoded :6379) | n/a (próprio, follow-up de F001/F004) | n/a | P1 | todo | — | — |
| F032 | infra | `config.ts` deve respeitar env externa (env explícita ganha de `.env`, convenção UNIX) | n/a (próprio, follow-up de F004) | n/a | P2 | todo | — | — |
| F033 | shell | Validar shell em viewport mobile real <768px (drawer-up + shortcut-bar bottom + sidebar oculta) — F005 C2 não exercitável no Chrome MCP (viewport travado em 1536px); requer device emulation real (CDP `Emulation.setDeviceMetricsOverride`) ou browser real em 375/414/767px | n/a (follow-up de F005/F007) | [[app-shell]] | P1 | todo | — | — |
| F034 | shell | Corrigir mojibake em `aria-label` da Sidebar (double UTF-8 encoding ao setar atributo — `Configurações` vira `ConfiguraÃ§Ãµes`; `textContent` OK). Causa-raiz no pipeline de encoding do atributo, não no menu. Afeta a11y do rail (tooltip/leitor de tela). | n/a (follow-up de F007) | [[sidebar]] + [[menu-hierarquia]] | P1 | todo | — | — |
| F035 | shell | Validar empiricamente o caminho de denial da ACL (`canAccessPath=false` → `<AccessDenied/>` com `InlineAlert variant=warning`; `<AclGate>` com `fallback`). F008 C5 não exercitável pois PROCESSA/id=1 é super-user (bypass total) e `TBfuncao`/`TBpapel_funcao_pagina_modulo.DFid_funcoes` da base `imperial_logistica_29/portal-director` está vazia. Requer (a) seed de TBusuario não-super com papel limitado em Area 52, ou (b) override `STUDIO_EMPRESA` ≠ Processa para forçar branch normal no user 1. | n/a (follow-up de F008) | [[acl-papel-funcao-pagina]] | P1 | todo | — | — |
| F036 | components | Renderer GenericCalendar (chave `genericcalendar` no model) — vista calendário/agenda como discriminante de página | `react-tools/components/GenericCalendar/` | TBD | P1 | todo | — | — |
| F037 | components | Renderer GenericActionForm (chave `genericactionform` no model) — form de ações agrupadas (`actionGroups`); template `TemplateFormularioAcoes` | `react-tools/components/GenericPage/GenericActionForm.js` | TBD | P1 | todo | — | — |
| F038 | components | Renderer GenericGridCollection (chave `genericgridcollection` no model) — múltiplos grids compostos com filtro compartilhado; template `TemplateGrids` | `react-tools/components/GenericPage/GenericGridCollection.js` | TBD | P2 | todo | — | — |
| F039 | render | Inventário e migração de `acesso.TBfuncao_model` (catalogar todas as funções JS cadastradas por aplicação para decidir estratégia: transpile vs sandbox vs reescrita) — pré-requisito para remoção de `eval` no Studio | `acesso.TBfuncao_model` + `react-tools/components/GenericPage/GenericPage.js:61-98` | [[tbfuncao-model]] | P0 | accepted | ✓ (inventário documental) | ✓ 2026-05-15 |
| F050 | render | Reescrita declarativa das 4 funções de `TBfuncao_model` em 2 primitivas (`submit-com-validacao` + `submit-com-validacao-e-comprovante`) — implementa opção C do contrato [[tbfuncao-model]] §"Estratégia para Director.Studio". Cobre 100% do uso observado em Área 52 (handle_submit/update/cancelar_gerenciar_agendamento + handle_submit_realizar_agendamento, todos do app `agent`). Schema das primitivas absorve: validação prévia via proc dedicada, POST para proc principal, modal de confirmação parametrizável, modal de comprovante HTML (componente nomeado, não `document.createElement`+`window.open` inline), encadeamento via callback. Elimina o 2º/3º pontos de `eval` (em `executeGenericFunctions`, `GenericPage.js:61-98`); o 1º (evalModelDynamicParams) fica para F051. Pré-requisito: nenhum cliente cadastrou handler próprio em produção (confirmado por F039). Fix 2026-05-15 (smith): (1) crash em `setScript` updater dos 3 selects da smoke route — captura fora do updater; (2) C8 silencioso — `console.warn` em `generic-form-renderer.tsx` `performSubmit` quando `useGenericFunction` existe mas `declarativeHandler` é null. Smoke local Playwright CDP: 6 combinações sem `pageerror`; warning emitido p/ chave desconhecida. Débito menor: `ReceiptModal` é componente novo sem spec no `ui-system/` — aceito como detalhe interno da primitiva submit-com-comprovante (não reutilizável fora do contexto); designer enfileira spec retroativa se reuso emergir. | `acesso.TBfuncao_model` (seed) + 4 funções do app `agent` em `engenharia--fabrica--sql--portal-aws/agent/alimentacao/insert_pagina_*_agendamento.sql` | [[tbfuncao-model]] | P0 | accepted | ✓ 2026-05-15 | ✓ 2026-05-15 |
| F040 | render | Inventário e migração de `acesso.TBmodel_parametro` (catalogar todos os `dParamX` para decidir contrato de interpolação no Studio) — pré-requisito para remoção de `eval` no caminho de fetch | `acesso.TBmodel_parametro` + `react-tools/components/GenericPages/GenericPages.js:26-58` | [[tbmodel-parametro]] | P0 | accepted | ✓ (inventário documental) | ✓ 2026-05-15 |
| F041 | render | Política de cache/refresh do model no Studio (legado: sem cache, 1 fetch por mount de aba, sem skeleton durante carga) — decisão de UX + persistência | `react-tools/components/GenericPages/GenericPages.js:96-104` | [[engine-schema-driven]] | P1 | todo | — | — |
| F042 | render | Cross-app fallback do model (`OR DFchave='processa'` no repo .NET — páginas core compartilhadas entre apps) — replicar ou redesenhar tenancy | `PortalDirector.Repositories/GenericPagesRepository.cs:11-21` | [[obter-model-pagina]] | P1 | todo | — | — |
| F043 | infra | Seed de models `portal-director` em base de teste (Area 52) — `DBdirector_imperial_logistica_29` só tem 6 models `wms.*`, impedindo validação end-to-end (menu→ACL→engine) de F010+; sem seed, todo renderer cai no mesmo fetch-stub de F009 C3. Opção (c) escolhida: fixture mínima de 9 models portal-director (1 por página em TBpagina), JSON declarativo derivado de DDLs reais + seed canônico portal-aws/agent; zero `dParam*`/`funcoes` (pré-empta `eval` antes de F040/F050/F051). Cobertura 9/9; dispatch detecta 5×(filtro+datagrid) + 4×(genericform). Procs referenciadas (sp_consultar_*, sp_persistir_*) existem em sources mas não foram instaladas — F052 enfileirada. | n/a (follow-up de F009) | [[obter-model-pagina]] | P0 | accepted | ✓ (seed aplicado; smoke replay de fetchModelFromDb Caso B em Area 52 — 9/9 parseáveis + dispatch correto) | ✓ 2026-05-15 (feature de infra/seed sem UI; smith replicou caminho real de consumo, mesmo critério de F039/F040; design system N/A; procs ausentes isoladas em F052) |
| F044 | components | Sort do DataGrid contra `sp_Listar_GrupoTrabalho` (e equivalentes) falha com `Invalid column name 'descricao'` — backend SQL faz match case-sensitive de column names enquanto headers do model expõem chaves lowercase. Defeito no contrato/proc do legado (não na impl F011). Catalogar procs afetadas e decidir: (a) normalizar `ordenacao` no backend grid (toLowerCase + map server-side), (b) aliasing nos headers do model, (c) corrigir as procs. | `react-tools/components/DataGrid2/THead/TH.js` + procs `sp_Listar_*` em `engenharia--fabrica--sql--portal-director` | [[model-valor-datagrid]] | P1 | todo | — | — |
| F045 | components | gridAction `delete` — comportamento canônico do legado (confirmation flow específico, possivelmente proc dedicada com soft-delete vs hard-delete). F011 entrega `InlineAlert "Ação ainda não disponível"` como stub explícito. Escavar contrato de `delete` no DataGrid v1 (não documentado em [[model-valor-datagrid]] §gridActions) e implementar como sub-feature. | `react-tools/components/DataGrid2/Header/Actions.js` + DataGrid v1 `Actions/delete` | [[model-valor-datagrid]] | P1 | todo | — | — |
| F046 | shell | Montar `<HubStatus/>` no app-shell (header/footer slot) e fiar com canais reais (notification, session.changed); integrar com F020 (Notifications) para consumir `event:notification` e emitir toast/banner. F023 entrega componente+hook isolados; falta wiring visual no shell e binding ao sistema de toast. | `react-tools/components/AppMain/` (slot a definir) + `packages/ui/src/components/hub-status.tsx` (já entregue) | [[hub-sse-mapping]] + [[app-shell]] | P1 | todo | — | — |
| F048 | integrations | Smoke ponta-a-ponta da bridge AWS contra `portal-aws-mock` (ou contraparte real) — F024 entrega cliente + endpoints com smoke client (5/5) e smoke routes (gates de erro 503/502/400/401), mas o caminho feliz `Director→portal-aws→proc espelho→`<Resposta>`` nunca foi exercitado fim-a-fim (porta 5100 desligada). Provisionar mock que responda `portal.sincronizar_<entidade>` e `portal.<proc-fornecedor>` aceitando `Authorization: Bearer` com `Consts.SecretKey` e devolvendo envelope `<Resposta><Status>200|500</Status><Sucesso>...</Sucesso><Dados>...</Dados></Resposta>`. Cobrir as 10 entidades de sync + 4 procs CRUD usuário-fornecedor. | n/a (follow-up de F024) + `apps/api/src/aws-client.ts` | [[portal-aws-bridge]] | P1 | todo | — | — |
| F049 | infra | Setup wizard step para `STUDIO_AWS_JWT_SECRET` — F024 documenta deploy-wide via env; wizard atual (F001) não pede. Adicionar step opcional "Bridge AWS" no wizard que (a) detecta se há `acesso.TBaplicacao(DFchave='portal-aws')` na base do tenant durante onboarding, (b) se sim, exige `STUDIO_AWS_JWT_SECRET` (mesma chave do Director↔AWS legado, `Consts.SecretKey`), (c) escreve no `.env` final. Sem essa step, deploy multi-tenant precisa lembrar do segredo manualmente. | n/a (follow-up de F024 + F001) | [[portal-aws-bridge]] | P1 | todo | — | — |
| F051 | render | Interpolador declarativo de `{path}` no JSON do model (substitui `dParamX` + `eval`) — implementa **opção A** do contrato [[tbmodel-parametro]] §"Estratégia para o Director Studio". Cobre 18/18 expressões observadas em Área 52 (3 páginas do app `agent`, todos seeds canônicos do `portal-aws`; nenhum cliente cadastrou variante própria — confirmado por F040). Contexto fechado: `{user: loggedUserData, acl: {resourceCompanies}, configs: localStorage["@director/configs"]}`. Smith 2026-05-15: implementado motor puro `interpolate(value, ctx)` em `packages/ui/src/lib/interpolate.ts` com parser ad-hoc (sem eval/new Function). Gramática suporta path lookup (`{user.cnpj}`), ternário (`{user.fornecedorCNPJ ? false : true}`), coalescing (`{user.cnpj ?? ""}`), comparações (`==`/`!=`/`<=`/`>=`/`<`/`>`), negação (`!path`), `&&`/`||`, e POJO inline (`{$pojo:k=user.cnpj,k2=user.email}`) — tudo top-level e em branches de ternário. Modo 1: string inteira `"{...}"` → valor cru (boolean/array/object/null/number). Modo 2: texto com `{...}` embutido → coerção string (POJO em texto vira `pojo-in-template` divergence). Walk recursivo em objects/arrays. Delimitadores `{...}` obrigatórios eliminam aliasing legado. Integrado em `useModel.applyInterpolation` (pós-fetch). Backend `apps/api/src/routes/model.ts` removeu `interpolateParams` legado: `modelParams` retornado como divergence informativa, JSON cru parseado direto. Engine `model-engine.tsx` exibe banner `interpDivergences`. Smoke route `/smoke/f051` cobre 18 expressões reais + 6 extras (texto embutido, walk, casos vazios) — 24/24 PASS validados via tsx local. Typecheck 3 packages OK. Elimina o **segundo dos 3 pontos de `eval`** (1º foi F050; 3º — `button.externalAction` — enfileirado em F054). Débito menor: motor não emite divergence em sub-path inexistente dentro de namespace válido (só em namespace desconhecido) — enfileirado em F053. | `acesso.TBmodel_parametro` (seed) + 18 linhas observadas em `engenharia--fabrica--sql--portal-aws/agent/alimentacao/insert_pagina_*_agendamento.sql` | [[tbmodel-parametro]] | P0 | accepted | ✓ 2026-05-15 (smoke /smoke/f051 25/25 PASS — 21 reais + 3 extras + 1 divergence; div.pathNotFound visível como `path-not-found@{usuario.foo}` + console.warn; backend model.ts confirmado devolve `modelStringRaw` cru sem interpolar e marca modelParams como divergence informativa; ModelEngine banner "Avisos do interpolador {path} (F051)" implementado via `interpDivergences`; zero `eval`/`new Function` em packages/ui/src; F050 smoke carrega sem regressão; F010/F011 sem smoke route dedicada — aceitação via caminhos anteriores; console limpo, único warn é o esperado da divergence) | ✓ 2026-05-15 |
| F052 | infra | Seed de stored procedures `portal-director` em base de teste (Area 52) — F043 instalou 9 models cujos `gridActions`/`api` referenciam `acesso.sp_consultar_*` (leitura de filtro+grid) e `acesso.sp_persistir_*` (CRUD via genericform). Das 9 procs referenciadas: **4 existem em sources** (`sp_consultar_niveis_acesso`, `sp_consultar_usuarios`, `sp_conexao_datagrid`, `sp_consultar_config_mobile`) e foram reaplicadas idempotentemente via DROP+CREATE; dep `dbo.Split` confirmada presente. **5 não existem em sources** (`sp_consultar_usuarios_fornecedor` + 4×`sp_persistir_configuracao_*`) — gap documentado em F052b para o arqueólogo investigar (procs em outro source não-catalogado, ou nunca existiram no legado). Smoke 4/4 OK contra base real (envelope `<Relatorio>` ou vazio). ui-tester de F010/F011 fica habilitado para 4 das 5 datagrid pages; 1 datagrid e 4 genericform aguardam F052b. | `sources/engenharia--fabrica--sql--portal-director/portal.director/programacao/acesso.*` + 9 models de F043 | [[obter-model-pagina]] + [[model-valor-datagrid]] + [[model-valor-genericform]] | P0 | accepted | ✓ 2026-05-15 (smoke contra base real, 4/4 procs respondem envelope `<Relatorio>` ou vazio) | ✓ 2026-05-15 (feature de infra/seed sem UI; smith reaplicou DDL de sources sem mutação; smoke real exercita caminho de consumo via mssql contra Area 52; política transversal de aceitação sem UI aplicável; design system N/A; 5 procs ausentes isoladas em F052b sem bloquear o que F052 entrega) |
| F052b | infra | Escavar/decidir destino das 5 procs `portal-director` referenciadas pelos models F043 mas ausentes em sources e na base — `acesso.sp_consultar_usuarios_fornecedor`, `acesso.sp_persistir_configuracao_agendamento`, `acesso.sp_persistir_configuracao_cotacao`, `acesso.sp_persistir_configuracao_email`, `acesso.sp_persistir_configuracao_aws`. Archaeologist precisa investigar 3 hipóteses: (1) procs vivem em outro source não-catalogado (candidatos: `processa.adm`, `processa.appbuilder`, `portal-aws`, `processa.sdk`); (2) procs nunca existiram — endpoints chamados direto em `.cs` controller sem stored procedure (verificar `PortalDirector.Api/Controllers/*` e `Processa.ADM.Aplicacao/Controllers/*`); (3) procs são privativas de tenants específicos não inclusos no dump. Saídas possíveis: (a) localizou em outro source → smith aplica via F052b igual a F052; (b) confirmou que vivia em `.cs` → vira feature de endpoint nativo do Studio (não-proc), reescrita em `apps/api/src/routes/`; (c) confirmou que nunca existiu de forma utilizável → descartar as 5 pages do cutover (refactor de F043 para remover `acessos_fornecedor` e 4× `configuracoes_*` da seed, ou aceitar que essas pages caem em proc-not-found visível como banner amarelo no engine). Bloqueia ui-tester end-to-end de F010 sobre 4 pages `configuracoes_*` e F011 sobre `acessos_fornecedor`. Não bloqueia cutover global — bloqueia apenas o subset de pages portal-director afetado. | n/a (follow-up de F052; archaeologist investiga sources não-catalogados) | [[obter-model-pagina]] + [[model-valor-datagrid]] + [[model-valor-genericform]] | P1 | todo | — | — |
| F053 | render | Interpolador `{path}` — emitir divergence `subPathNotFound` quando namespace existe mas sub-path resolve a `undefined` (hoje só emite `pathNotFound` quando o namespace raiz é desconhecido). Cobertura simétrica de telemetria: qualquer leitura que retorne `undefined` deve sinalizar ao banner do ModelEngine, não passar silenciosamente como string vazia/null. Follow-up direto de F051 (débito registrado pelo smith no aceite). | n/a (follow-up de F051) + `packages/ui/src/lib/interpolate.ts` | [[tbmodel-parametro]] | P1 | todo | — | — |
| F054 | render | [ACCEPTED 2026-05-15 — no-op confirmado: hipótese de 3º eval rejeitada pelo archaeologist; auditoria zero-eval validada pelo ui-tester (0 hits de `eval(` e `new Function(` em packages/ui/src + apps/api/src + apps/director-studio/src); smokes F050+F051 sem regressão; F051 25/25 PASS. F054b é redundante (mesmo escopo) — ver linha F054b abaixo.] Eliminar o 3º e último ponto de `eval` no engine schema-driven — `button.externalAction` no model. F050 cobriu funções de `TBfuncao_model` (executeGenericFunctions); F051 cobriu interpolação de `dParamX` (evalModelDynamicParams). Falta o caminho de `externalAction` (ação externa declarada no botão do model, executada via `eval` no legado). Inventariar uso real em Área 52 (TBmodel_pagina + seeds portal-aws), decidir estratégia (provavelmente C-like de F050: reescrita declarativa em primitivas nomeadas) e implementar. Conclui a remoção total de `eval`/`new Function` do engine. **Archaeologist 2026-05-15: hipótese rejeitada — `externalAction` é sempre função JS literal passada por host JSX, nunca string, nunca eval. Auditoria de `eval`/`new Function` em `react-tools/src/`: exatamente 2 hits (GenericPage.js:93 → F050; GenericPages.js:33 → F051). Probe cross-tenant (15 bases, 133+ models de `acesso.TBmodel_pagina` + amostras de `TBfuncao_model`): 0 ocorrências de `externalAction` no `DFvalor`. Seeds canônicos SQL: 0 hits. Trilho de eliminação de `eval` está **completo** com F050+F051. Curator deve fechar F054 como no-op ou converter em F054b (auditoria final: zero `eval`/`new Function` em `packages/ui/src`+`apps/api/src`).** | `react-tools/components/` (localizar binding) + `acesso.TBmodel_pagina` (campo `button.externalAction` no JSON) | [[button-external-action]] | P0 | accepted | ✓ 2026-05-15 (no-op, audit zero-eval confirmado: 0 hits de `eval(` e `new Function(` em packages/ui/src + apps/api/src + apps/director-studio/src; smokes /smoke/f050 e /smoke/f051 carregam sem erro de console, F051 reporta 25/25 PASS) | ✓ 2026-05-15 |
| F047 | integrations | Bus realtime multi-instância via Redis Pub/Sub — F023 entrega bus in-memory single-instance (fan-out por (channel,userId\|empresa) em memória do processo Node). Para escalar horizontalmente a API do Studio, fan-out precisa migrar para Redis Pub/Sub (channels `notif:user:{userId}`, `notif:empresa:{cnpj}`, `notif:all` conforme [[hub-sse-mapping]] §85-101). Buffer LRU de 100 por sessão pode permanecer in-memory (ou migrar para Redis Streams se reconnect cross-instance for requisito). Pré-requisito: deployment multi-réplica. | n/a (follow-up de F023) + `apps/api/src/hub-bus.ts` | [[hub-sse-mapping]] | P2 | todo | — | — |

## Backlog do arqueólogo

Features ainda não enumeradas — descoberta pendente:

- Catalogar **todos** os componentes em `react-tools/src/components/` que faltam acima
- Catalogar **todas** as procs em `sources/engenharia--fabrica--sql--portal-director/portal.director/programacao/acesso.*`
- Catalogar **todas** as tabelas em `sources/engenharia--fabrica--sql--portal-director/portal.director/criacao/acesso.TB*`
- Catalogar features fora do AppMain (e.g. Mobile, SignalR Hub, edoc, sped, fornecedor portal)

Cada item descoberto **vira linha nova nesta tabela**, com `Source` + `Contract` apontado.
| F054b | render | [REDUNDANTE — escopo idêntico a F054 (auditoria final zero-eval). Mantida no manifest por rastreabilidade (ui-tester observou duplicação; arqueólogo propôs a divisão antes do aceite). Aceite herdado de F054.] Auditoria final zero-eval: provar invariante `eval()` e `new Function()` ausentes em `packages/ui/src` + `apps/api/src` + `apps/director-studio/src` após F050+F051. Re-rodar smokes F050+F051 sem regressão. Aceite confirma cutover do engine livre de eval. | n/a (auditoria) | [[zero-eval-invariant]] | P0 | accepted (redundante c/ F054) | ✓ 2026-05-15 (herdado de F054 — mesma evidência) | ✓ 2026-05-15 |
