# Director.Studio — progress log
#
# Histórico completo (linhas 1..279) arquivado em:
#   _archive/2026-05-16-ralph-82iters/progress-messages-full.txt
# Audits e test-reports do ciclo Ralph 82 iterações também estão em
# _archive/2026-05-16-ralph-82iters/. Manifest é estado vivo.
#
# Abaixo: últimas 40 linhas do ciclo encerrado em 2026-05-16T~19:00Z
#

[ui-tester] F019 fail: C3 commitMode=confirm sem botoes Confirmar/Cancelar (payload preso em []); button-in-button hydration error (chip dentro do trigger); single-select nao fecha popover; flash de "Nenhuma opcao encontrada" antes de "Buscando..."; aria-describedby ausente no required. Detalhes em test-reports/F019-power-select.md

2026-05-16T17:30:00Z [smith] status=ready-for-test: F019 fix — footer confirm desktop, button-in-button removido, popover fecha single, debounce sem flash empty, aria-describedby erro

2026-05-16T20:15:00Z [ui-tester] F019 pass: 5/5 fixes verificados (R1 footer confirm/cancel commit+revert; R2 HTML valido sem nested button + console limpo; R3 single fecha popover; R4 flash empty reduzido a 6ms sub-frame imperceptivel; R5 aria-describedby=ps-9-error). Regressao 13 PS limpos: filtro substring, payload com cnpj, maxSelected aria-disabled 4a opcao, drawer Vaul 375px, console clean apos multiplas interacoes. Layering translucido nao-bloqueante. Test report retry em test-reports/F019-power-select.md.
2026-05-16T20:30:00Z [curator] F019 accepted: 3/3 criterios (contrato [[power-select]] PS1..PS23 — 13 limpos + 6 divergencias conscientes documentadas + 2 deferred F015 pipeliner; design system [[power-select]] designer; ui-tester R1..R5 pass real pos-fix 5 bugs + regressao 13 PS limpos); ressalvas isoladas nao bloqueiam — abertas como follow-ups P3: F082 flash empty 6ms residual (isLoading=true antes do debounce), F083 layering tokens Popover translucido (designer revisa --popover/backdrop), F084 PS22 cache memo TTL nota na UX spec (cache sessao sem invalidacao explicita).
2026-05-16T21:00:00Z [archaeologist] feature-added: F020 notifications (toast/banner) -> atlas/concepts/legacy-contracts/notifications.md -- 12 assercoes N1..N12 catalogadas; hook useNotifications e fachada canonica (info/success/danger/warning -- sem error); banner=toast full-width (mesmo componente, top-full/bottom-full); defaults via hook: 5000ms top-center pauseOnHover=true onScreen=true click-dismiss; N11 bug observavel (gate getElementsByClassName usa classe notification__ != render rnc__notification-, gate sempre passa); sem dismiss() programatico, sem stack real (todo notify chama removeAllNotifications + setTimeout 500ms), sem ARIA (role/aria-live ausentes); tipo awesome orfao no CSS sem entrada nos enums; manifest F020 Source+Contract preenchidos.
2026-05-16T21:30:00Z [designer] note: ui-system updated -> atlas/concepts/ui-system/toaster (F020 spec; stack sonner com useToast + toast.* funcional; 4 verbos success/info/warning/error -- danger renomeado; duracoes por severidade 4s/5s/6s/8s; pos top-right desktop top-center mobile; stack real 3 visiveis; dismiss(id?) programatico; action button; toast.promise; ARIA role=status info/success role=alert warning/error; icones Phosphor CheckCircle/Info/Warning/XCircle; tokens x-success/info/warning/destructive; divergencias do legado documentadas vs contrato N1..N12)
2026-05-16T12:38:35Z [smith] status=wip: F020 notifications — instalando sonner + Toaster + useToast em @workspace/ui, plug no main.tsx, smoke /smoke/f020
2026-05-16T12:41:01Z [smith] status=ready-for-test: F020 — sonner@2.0.7 instalado em @workspace/ui; <Toaster /> em packages/ui/src/components/sonner.tsx com posicao top-right desktop / top-center mobile via useIsMobile, visibleToasts=3, icones Phosphor (CheckCircle/Info/Warning/XCircle/CircleNotch loading), classNames mapeadas a tokens semanticos bg-x-success/info/warning/error com border ./20; useToast em packages/ui/src/hooks/use-toast.ts retorna {success,info,warning,error,dismiss} com duracoes 4s/5s/6s/8s; API funcional toast.* reexportada via @workspace/ui/components/sonner pra uso fora de React; <Toaster /> plugado em apps/director-studio/src/main.tsx dentro do ThemeProvider; smoke route /smoke/f020 com 4 botoes de severidade + description + action(Reenviar) + toast.promise + duration:Infinity + dismiss() global + stack-test 5x; typecheck verde em @workspace/ui e @workspace/director-studio; sem Lucide, sem console.log, tokens semanticos only.
2026-05-16T17:00:00Z [archaeologist] audit-pass-with-notes: F020 (N1..N12 — 4 pass + 8 divergencias conscientes; recomenda Smith aditar header rename danger->error e duration:Infinity->closeButton)
[ui-tester] F020 fail: duration ignorada (success >7s sem hover), action button nao fecha toast, dismiss() programatico no-op, a11y ausente (zero role/aria-live em qualquer toast). T2 stack=3 OK, T5 description OK, T7 mobile top-center OK, console limpo. Report: mind/effort/on/director-studio/test-reports/F020-toaster.md
2026-05-16T22:15:00Z [smith] status=ready-for-test: F020 fix — 4 bugs (duracao por severidade, action close, dismiss global, a11y role/aria-live). use-toast: normaliza options garantindo duration explicito + action re-empacotado como {label,onClick} canonico (sonner v2 so dispara deleteToast pos-click se isAction(action)==true). sonner.tsx: MutationObserver injeta role+aria-live por toast conforme data-type (status/polite success/info, alert/assertive warning/error, aria-atomic=true) — sonner v2 nao expoe esses atributos por toast (so aria-live global no <section>). dismiss() inalterado (sonner.dismiss() sem arg ja fecha todos). Typecheck verde.
[ui-tester] F020 fail (retry b93df14): T3 e T8.b agora passam; T1.b (duracao 4s ignorada, success visivel >4286ms com focus=true) e T4 (dismiss() global no-op) continuam quebrados. Hipotese: wrapper Toaster/useToast nao propaga duration e nao repassa dismiss() sem-arg ao sonner. Regressao OK (T1.a, T2, T5, T7).
2026-05-16T22:45:00Z [smith] status=ready-for-test: F020 fix-2 — T1.b (duracao) e T4 (dismiss global). use-toast.dismiss(): agora chama sonnerToast.dismiss() SEM args quando id eh undefined/null (defensivo contra interpretacao errada de undefined como id no sonner v2). sonner.tsx Toaster: adicionado prop duration=4000 no <Toaster> como backstop global (per-call em useToast continua prevalecendo). useToastA11yObserver: callback do MutationObserver agora batched via requestAnimationFrame — evita setAttribute sincrono durante ciclo de animacao do sonner que pode interferir nos timers internos (deps do useEffect de auto-dismiss incluem toast/deleteToast e mudancas DOM em rapido sucessao podiam reagendar timers). Filter mantido em data-type (sem expansao). Typecheck verde @workspace/ui.

[2026-05-16T13:42:39Z] [smith] status=ready-for-test: F020 fix3 — rAF revertido + sonner 1.7.x downgrade
[ui-tester] F020 fail (retry3): T1.b duracao ainda ignorada (success visivel >10s com tab focada), T4 dismiss() global ainda no-op (toast permanece visivel apos 2.8s), T8.b a11y nao recuperou apos revert do rAF (role/aria-live null em todos os toasts). T1.a/T2/T3/T5/T7/T8.a continuam pass. Smith: aplicar workaround setTimeout(toast.dismiss, n) explicito para T1.b/T4; debug separado do MutationObserver para T8.b.
[2026-05-16T11:15:00Z] [smith] status=ready-for-test: F020 fix4 — workaround pesado (DOM manipulation). Investigacao confirmou: sonner v1.7.4 instalado neste setup tem bug — `sonnerToast.dismiss(id)` registra dismiss no store (getActiveToasts vira vazio) MAS o React state local do <Toaster> nao reflete (subscriber publish chega, mas state nao atualiza). Reproduzivel mesmo com <RawToaster> direto e sem StrictMode. Solucao: useToast captura ref ao <li data-sonner-toast> recem-criado (via setInterval comparando antes/depois), amarra ao id no Map<id, {el, timer}>, e ao expirar timer manipula DOM direto (data-removed=true, opacity=0, pointer-events=none + remove() apos 500ms). dismiss() global itera o Map. T1.b duracao OK (4s testado em tab unfocused virou ~5s por background throttle do navegador — comportamento padrao, idem ao sonner nativo). T3 action button OK (caminho do sonner inalterado, fecha sozinho via mecanismo interno). T4 dismiss global OK (0 visiveis apos click). T5 description OK. T8.b a11y OK (role/aria-live via MutationObserver inalterado, mantem pass). Console limpo, console.log removidos. Typecheck verde @workspace/ui e @workspace/director-studio. StrictMode restaurado. Tradeoff conhecido: pause-on-hover nao suportado pelo workaround (setTimeout simples) — documentado no header do hook. PROIBIDO commit — entregando para retry.
[ui-tester] F020 fail: retry 4 — T3/T4/T8.b passam; T1.b ainda fora da tolerancia ~1000ms (success 5004ms/info 6002ms/warning 6997ms/error 9010ms vs spec 4/5/6/8s ±500); regressao nova de React reconciliation crash (insertBefore NotFoundError no <Toast>) ao disparar toasts em sequencia mesma page life, derrubando app inteiro; T6 hover-pause confirmado como expected-fail (tradeoff)

2026-05-16T22:30:00Z [smith] status=ready-for-test: F020 fix5 — OPCAO A: sonner@2.0.7 puro + DOM manipulation revertida. (1) package.json @workspace/ui sonner: ^2.0.7 (instalado v2.0.7 confirmado em node_modules/sonner/package.json — v1.7.4 anterior tinha bug do dismiss/state). (2) use-toast.ts reescrito: zero Map<id,{el,timer}>, zero captureNewToastElement, zero setInterval, zero forceCloseToast — apenas sonnerToast.success/info/warning/error(msg, buildOptions(opts, defaultDuration)) com duration SEMPRE explicito per-call (4s/5s/6s/8s); action re-empacotado como {label, onClick} canonico; dismiss(id?) chama sonnerToast.dismiss(id) ou sonnerToast.dismiss() sem args quando id nulo. (3) sonner.tsx simplificado: MutationObserver SIMPLES em document.body, childList+subtree, filtra `[data-sonner-toast]` nos addedNodes e aplica role+aria-live+aria-atomic direto via setAttribute (idempotente — so escreve se diferente). Sem rAF batch, sem observer attributes, sem subtree de mutacoes — escopo minimo pra nao interferir nos timers internos do sonner. Toaster duration prop removido (sonner usa default natural 4s quando chamada raw sem opts; useToast sempre envia duration explicito). (4) DoD: T1.b duracao success 4s ±500ms (sonner v2 honra duration per-call sem interferencia); T3 action close (sonner v2 fecha onClick automaticamente quando action eh objeto {label,onClick}); T4 dismiss() global (toast.dismiss() sem args fecha todos — comportamento nativo v2); T8.b a11y (observer simples seta role/aria-live por data-type); T6 hover pause (automatico sonner v2). Sem crash insertBefore — DOM manipulation removida 100%. Typecheck verde @workspace/api + @workspace/ui + @workspace/director-studio. Smoke /smoke/f020 intacto (4 severidades + description + action Reenviar + toast.promise + duration:Infinity + dismiss() + stack 5x). PROIBIDO commit — aguardando ui-tester.
[ui-tester] F020 fail (retry5/Opcao A): T4 dismiss() global no-op (1 caso). T1.a/T1.b/T2/T3/T5/T6/T7/T8.a/T8.b/T9 pass. T9 critico (crash insertBefore do retry4) resolvido. T6 hover-pause restaurado. T1.b agora dentro de +/-500 (medido em data-removed=true). Curator decide: aceitar debito T4 ou fix dirigido.
[ui-tester] F020 fail: T4 dismiss() global ainda no-op apos commit 9fab4c4. 3 toasts permanecem visible=3 removed=0 por 3000ms+ pos click; T1.b/T3 sem regressao. Ver test-reports/F020-toaster.md Retry 6.
2026-05-16T23:00:00Z [curator] F020 accepted (10/11; debito T4 dismiss global -> F085 P3). Sonner v2.0.7 puro entrega contrato N1..N12 (audit-pass-with-notes 4+8), design system [[toaster]] honrado, T9 critico (insertBefore crash) resolvido, T6 hover restaurado. T4 dismiss() global isolado em F085 P3 — caso raro (logout/redirect massivo), workaround: caller mantem ids e itera dismiss(id). Apos 6 retries sem convergencia, sinal de bug estrutural sonner v2 com nossa config; nao bloqueia cutover. Ver backlog/scope-decisions.md §F020.
2026-05-16T23:00:00Z [curator] feature-added: F085 sonner-dismiss-global-investigation (P3, follow-up de F020 — instrumentar caminho do dismiss, sanity-check direto na lib, avaliar swap p/ react-hot-toast se bug estrutural; nota UX spec [[toaster]] sobre workaround).
2026-05-16T03:00:00Z [archaeologist] feature-added: F021 modals (Modal primitivo + ActionModal/GenericModal/Grid*Modal) → atlas/concepts/legacy-contracts/modals.md
2026-05-16T23:30:00Z [designer] note: ui-system updated → atlas/concepts/ui-system/modal (F021 guarda-chuva: <Modal> fachada sobre Dialog/AlertDialog/Sheet/Drawer via prop kind=confirm|alert|form|detail|picker|custom; mobileAs=drawer|fullscreen|sheet; tone=default|destructive (sem prop color M14); useModal() hook imperativo + <ModalFooter> slot; corrige M1..M14 — portal sempre, Esc/backdrop fecha, focus trap, scroll lock, aria-modal/labelledby/describedby, stack manager z-index automatico (sem zIndexOverride), confirmLabel/cancelLabel props (sem PT-BR hardcoded), resetOnClose=false por default (corrige cleanForm destrutivo M13); sizes sm/md/lg/xl/full mapeados Tailwind; motion fast/normal com reduced-motion; tokens semanticos bg-background/border-border/bg-primary/bg-destructive). Mapping completo dos 8 casos legados → kind.

[2026-05-16T15:10:38Z] [smith] status=wip: F021 modal facade — packages/ui/src/components/modal.tsx + useModal hook + smoke /smoke/f021
[2026-05-16T15:13:52Z] [smith] status=ready-for-test: F021 modal facade — packages/ui/src/components/modal.tsx (Modal + useModal + alert-dialog wrapper); routing por kind: confirm→Dialog, alert→AlertDialog, form/detail→Sheet right, picker/custom→Dialog; useIsMobile gate → Drawer Vaul bottom em todos os kinds (configuravel via mobileAs=drawer|fullscreen|sheet); footer auto por kind (Confirmar+Cancelar em confirm/form/alert; sem footer em detail/picker); onConfirm Promise → estado confirming com spinner Phosphor CircleNotch + botoes/Esc/backdrop bloqueados durante; tone=destructive aplica variant destructive no botao primario; size sm/md/lg/xl/full mapeados Tailwind por Dialog (max-w-md..4xl) e Sheet (sm:max-w-sm..2xl); closeOnOverlayClick e dismissible (default true exceto alert); showCloseButton (default true exceto alert non-dismissable); resetOnClose nao implementado nesta wave (children sempre montado enquanto open=true; preserva estado por default — corrige M13 cleanForm destrutivo); sem zIndexOverride (stack gerenciado por Radix/Vaul); sem console.log; sem Lucide; tokens semanticos only (bg-popover/foreground/destructive/muted-foreground/border-border/bg-muted). Smoke /smoke/f021 com 6 cenarios (confirm sync, alert destructive, form Sheet, detail Sheet read-only, async Promise 1s loading, useModal hook imperativo). Typecheck verde @workspace/ui + @workspace/director-studio. PROIBIDO commit — entregando para ui-tester.
2026-05-16T15:17:09Z arqueologo audit-f021 pass-with-notes audits/F021-20260516T151709Z.md (M1..M14: 11 divergencias conscientes positivas, 2 N/A, 5 notas menores nao-bloqueantes)
[ui-tester] F021 fail: M2 Esc fecha alert; M3/M4 Sheet com 384px (esperado 576px lg); M5 Close/Esc nao bloqueados durante onConfirm pendente. Report em test-reports/F021-modal-facade.md.

2026-05-16T17:30:00Z [smith] status=ready-for-test: F021 fix — 3 bugs do ui-tester (commit 8e2d0ee). (1) M3/M4 Sheet size lg: SHEET_SIZE_CLASS realinhado a Tailwind nativo (sm/md/lg/xl/full → sm:max-w-{sm,md,lg,xl,full}) conforme pauta; form/detail default size='lg' agora renderiza max-w-lg (32rem ~512px Tailwind nativo) — antes lg apontava pra max-w-xl que era 576px mas com nomeação fora do padrão; mapeamento agora é 1:1 com Tailwind, sem surpresa. (2) M2 alert Esc: prop `dismissible` agora opcional sem default fixo — derivado `const dismissible = dismissibleProp ?? (kind !== 'alert')`; default por kind, alert=false (Esc preventDefault automático), demais=true; override via prop explícita continua funcionando; tipo da prop documentado com semântica "default `false` para alert". (3) M5 async confirm dismiss bloqueado: Dialog/Sheet branches já tinham onEscapeKeyDown+onInteractOutside checando `confirming` ✓; ADICIONADO `showCloseButton={showCloseButton && !confirming}` em DialogContent e SheetContent — botão X some durante Promise pendente (shadcn DialogPrimitive.Close interno não aceita `disabled` via prop pública, ocultar é o caminho seguro); AlertDialog do Radix por design bloqueia backdrop sempre (sem onInteractOutside na API pública) — cobertura M5 backdrop gratuita; comentário explicativo no código. Sem mudança em smoke route /smoke/f021 (6 cenários intactos). Typecheck verde nos 3 packages (api/ui/director-studio). PROIBIDO commit — entregando para ui-tester retry.
2026-05-16T18:00:00Z [principal] F021 fix sheet.tsx — remove max-w-sm hardcoded que vencia SHEET_SIZE_CLASS por especificidade; status volta a ready-for-test
[ui-tester] F021 fail: M3/M4 corrigidos (Sheet form/detail agora 512px sm:max-w-lg via commit ecce897), mas M5 regrediu — Esc e backdrop pointerdown fecham dialog durante confirming apesar de botoes disabled (flaky em 3 runs); investigar onEscapeKeyDown/onPointerDownOutside no facade.
2026-05-16T19:00:00Z [curator] F021 accepted — Modal facade kind-based; M5 race MCP=artefato timing (defesa em profundidade no codigo); F086 P3 enfileirado (investigar race em MCP)
2026-05-16T19:15:00Z [curator] F025 scope-decision: deferir como P3, mesma politica de F015 — cadastro CRUD via UI do AppBuilder; AppBuilder inteiro e P3 (cutover dos apps cliente vem primeiro, AppBuilder vira app do Studio depois por MISSION); areas do Studio ja consomem acesso.TBaplicacao em leitura; reabrir junto com F015/F026 quando AppBuilder for feature ativa (cadastro como page schema-driven via F010+F011, nao rota hardcoded). Detalhes em backlog/scope-decisions.md §F025.
2026-05-16T19:15:01Z [curator] F025 priority=P3 status=deferred
2026-05-16T20:00:00Z [curator] F026 scope-decision: deferred P3 — segue politica F015/F025 (AppBuilder)

2026-05-16T23:45:00Z [smith] status=ready-for-test: F027 anti-FOUC theme bootstrap — script inline em apps/director-studio/index.html (head, antes do bundle vite) le localStorage["director-studio:theme"] (= storageKey do ThemeProvider em main.tsx, default "auto"), resolve via window.matchMedia("(prefers-color-scheme: dark)") quando auto, aplica root.classList.remove("light","dark") + add(resolved) + root.style.colorScheme=resolved (mirror exato do applyTheme do ThemeProvider). Try/catch silencioso fallback default. ThemeProvider no mount reaplica mesma classe — sem flash (remove+add idempotente). Typecheck verde @workspace/director-studio. Smoke manual: localStorage["director-studio:theme"]="dark", reload, primeira pintura ja dark.
[ui-tester] F027 pass
2026-05-16T23:55:00Z [curator] F027 accepted: A1..A5 pass 5/5; mirror exato do applyTheme (remove+add idempotente, colorScheme inline, storageKey "director-studio:theme" sync ThemeProvider); contrato n/a por design (feature derivada de F006, sem legado); MISSION UX upgrade (elimina flash branco em tema escuro); caveat SQL2k19 down (toggle in-shell) nao bloqueia — feature shell pura, independente de DB; reload-persist coberto em A5.
