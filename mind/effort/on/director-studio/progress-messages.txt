# Director.Studio — Progress log (append-only)
# Formato: <ISO-8601 Z> [<agent>] <verb>: <detail> [→ link opcional]
# Verbos: started, feature-added, status=..., pass, fail, accepted, blocked, note

2026-05-15T04:00:00Z [principal] note: workspace bootstrapped — npm workspaces + turbo + Vite + Hono + shadcn (preset b0)
2026-05-15T04:30:00Z [principal] feature-added: F001 setup-wizard (own) → workspace/director-studio/apps/director-studio/src/setup/
2026-05-15T04:45:00Z [principal] feature-added: F002 boot-gate (own) → workspace/director-studio/apps/director-studio/src/routes/boot.tsx
2026-05-15T05:00:00Z [principal] note: infra com Caddy + Redis em docker-compose.platform.yml; padrão de PREFIX de portas adotado (PREFIX=30)
2026-05-15T05:30:00Z [principal] status=done: F001 (wizard escreveu primeiro .env real contra DBdirector_imperial_logistica_29)
2026-05-15T05:30:00Z [principal] status=done: F002 (boot gate roteia /setup ↔ /app conforme /api/setup/status)
2026-05-15T06:00:00Z [principal] note: time de agentes definido (archaeologist, designer, curator, smith, ui-tester); separação de mandatos formalizada
2026-05-15T07:00:00Z [principal] note: harness feature-locked wave configurado (/dwave); HOW-TO-RUN-WAVES.md publicado
2026-05-15T07:30:00Z [smith] status=ready-for-test: F006 theme (auto|light|dark, default auto, storageKey director-studio:theme, SO listener, atalho D, ThemeToggle Phosphor no header /app)
2026-05-15T08:00:00Z [ui-tester] F006 pass: all 7 criteria pass — default auto + .dark reflete SO; ciclo auto→light→dark→auto via click e atalho D; persistencia em localStorage director-studio:theme; D bloqueado em input editavel; console limpo. Ressalva nao-bloqueante: index.html sem boot script inline — em prod com rede lenta pode haver FOUC entre HTML parse e mount do React (em localhost imperceptivel). Criterio 6 (mudanca prefers-color-scheme em tempo real) validado por inspecao + comportamento de install/remove de listener; CDP Emulation.setEmulatedMedia nao disponivel no harness MCP.
2026-05-15T08:15:00Z [curator] F006 accepted: 7/7 criterios validados pelo ui-tester contra spec; ressalva FOUC nao bloqueia (imperceptivel em dev, melhoria separavel de bootstrap em prod).
2026-05-15T08:15:00Z [curator] feature-added: F027 anti-FOUC theme bootstrap inline (P1, derivado da ressalva nao-bloqueante de F006) — proxima acao: smith pega quando shell estabilizar.
2026-05-15T09:00:00Z [designer] note: ui-system updated → atlas/concepts/ui-system/login-page (spec da pagina F003, fluxo dos 5 caminhos invisibilizado, hints contextuais por padrao do identity)
2026-05-15T09:00:00Z [designer] note: ui-system updated → atlas/concepts/ui-system/form-field (primitivo label+control+hint/error, mobile-first com text-base 16px para anti-zoom iOS, suffix interativo para toggle senha)
2026-05-15T09:00:00Z [designer] note: ui-system updated → atlas/concepts/ui-system/button (deriva shadcn, extensoes: loading com label preservado, tooltip obrigatorio em size=icon, w-full so com justificativa)
2026-05-15T09:00:00Z [designer] note: ui-system updated → atlas/concepts/ui-system/inline-alert (feedback fixo no fluxo do form, 5 variants via tokens x-*, role=alert apenas para erros de submit, dismissible opt-in)
2026-05-15T09:30:00Z [smith] status=wip: F003 login híbrido (5 caminhos) — implementando primitivos form-field + inline-alert em @workspace/ui, rota /login, POST /api/auth/login com roteamento por padrão de identity (LDAP/fornecedor/temp/interno), tokens x-* semânticos.
2026-05-15T10:15:00Z [smith] status=ready-for-test: F003 login híbrido — primitivos FormField + InlineAlert em packages/ui (com tokens x-info/success/warning/error/critical em globals.css claro+escuro); página /login com hints contextuais por padrão do identity (\\ → LDAP, @ → fornecedor, domínio digitado → LDAP explícito), toggle "Outro domínio", toggle de visibilidade de senha (Phosphor Eye/EyeSlash), detecção de CapsLock, estados idle/submitting/success/error-* mapeados de respostas HTTP (401/403/429/501/5xx/network); POST /api/auth/login com 2 caminhos funcionais (internal-db via TBusuario+VALIDAR_CRIPT, fornecedor-aws via portal.UsuarioFornecedor+SHA2_256) e 2 stubs 501 documentados (ldap-bridge AWS:4306, temp-password); sessão httpOnly cookie assinada HMAC-SHA256 em store em memória (Redis = F004), /api/auth/me e /api/auth/logout; boot-gate roteia configured+autenticado → /app, configured+anônimo → /login; typecheck e vite build limpos. Fora-de-escopo cirúrgico: `_PROXY_PORT` em vite.config.ts (lint pré-existente desbloqueando build).
2026-05-15T06:10:00Z [ui-tester] F003 blocked: DB host 172.27.0.121 (Area 52 imperial_logistica_29) inacessivel desta maquina (ping/TCP timeout). C1 boot-gate pass, C2 layout pass, C3 hints (default/processa\/@/processa literal) e 501 ldap+temp pass via InlineAlert warning, C6 /api/auth/me=401 pass, C7 logout endpoint=200 pass, C8 a11y labels+autocomplete+toggle aria pass, C9 console limpo pass. C4 login real e C5 401 credenciais nao exercitaveis sem DB. Tested=- (parcial nao conta como pass).
2026-05-15T09:40:00Z [ui-tester] F003 fail: C4 login real e C5 erro-credencial nao podem passar. (a) auth.ts faz query rigida em acesso.TBusuario, mas DBdirector_imperial_logistica_29 mantem TBusuario/TBempresa apenas em dbo — SQL Server retorna 500 com "Invalid object name 'acesso.TBusuario'" tanto em C4 quanto em C5, mascarando o caminho de 401. (b) credencial sl/123 do .env e login de SQL Server gravado pelo wizard F001, nao usuario de TBusuario — nenhum DFnome_usuario=sl ativo na base; usuarios reais ativos sao PROCESSA, BRUNO SANTOS, JULIANALOUREIRO etc., sem senha combinada. Relatorio: mind/effort/on/director-studio/test-reports/F003-login-hibrido-rewave.md. Proxima acao: smith schema-aware (DB_SCHEMA configuravel ou descoberta) + curator combina credencial real de TBusuario.
2026-05-15T10:30:00Z [smith] status=wip: F003 rewave — schema-aware via probe INFORMATION_SCHEMA no boot da auth com cache em memoria; fallback acesso→dbo. Probe confirmou que na Area 52 (DBdirector_imperial_logistica_29) TBusuario/TBempresa vivem em dbo, DFativo_inativo eh a coluna correta, dbo.Validar_Cript existe, portal.UsuarioFornecedor NAO existe nesta base.
2026-05-15T10:35:00Z [smith] note: contrato de atlas processa-auth-paths.md desalinhado da realidade em duas colunas — diz DFnome_empresa em TBempresa, mas o .cs canonico (processa.sdk/Fontes/Processa.Sdk.Api/Settings.cs:45) e o DDL real usam DFnome_fantasia. Adotado DFnome_fantasia (source-of-truth = .cs + DDL). Archaeologist deve atualizar o contrato.
2026-05-15T10:40:00Z [smith] note: smoke test do endpoint POST /api/auth/login contra DBdirector_imperial_logistica_29 — (a) PROCESSA/wrongpassword → 401 invalid-credentials (era 500 antes); (b) nonexistent_user → 401; (c) email@x → 401 (path fornecedor-aws retorna null porque portal.UsuarioFornecedor nao existe nesta base — comportamento correto); (d) processa\\foo → 501 not-implemented (stub ldap-bridge). Schema discovery loggada uma vez no primeiro request: {auth: "dbo", fornecedor: null, cript: "dbo"}. C5 agora exercitavel.
2026-05-15T10:42:00Z [smith] status=ready-for-test: F003 rewave parcial — schema bug corrigido + C5 (senha errada) verificavel. C4 (login valido com 200+cookie) NAO pode ser smoke-testado pelo smith: nenhuma senha real de TBusuario combinada no fluxo (DFsenha eh hash via Validar_Cript e nao ha credencial documentada no contrato nem no .env). note: blocked-credential — curator precisa combinar com Processa uma senha de teste de um TBusuario (ex.: PROCESSA) em DBdirector_imperial_logistica_29 e repassar lateralmente ao ui-tester. Arquivos tocados: workspace/director-studio/apps/api/src/routes/auth.ts (schema-aware via INFORMATION_SCHEMA + cache + override env opcional DB_SCHEMA_AUTH/FORNECEDOR/CRIPT, e DFnome_empresa→DFnome_fantasia conforme .cs canonico).
2026-05-15T11:00:00Z [principal] note: F003 unblocked-credential — dbo.fn_Decript existe na base e reverte o algoritmo de dbo.Validar_Cript (XOR scramble de 2011). Senha de teste obtida: PROCESSA / 99 (cod_empresa=1, IMPERIAL LOG). Round-trip Validar_Cript(fn_Decript(DFsenha), DFsenha) = 1. ui-tester pode usar essa credencial direto pra fechar C4. Auditoria: qualquer SELECT em dbo.TBusuario expõe todas as senhas em plaintext — registrar no TODO.md como dívida P2 pós-cutover (rotacionar p/ bcrypt/argon2). Implicação pra Studio: ele já não decripta — só chama Validar_Cript — então a herança do legado fica contida.
2026-05-15T11:15:00Z [principal] note: /dwave atualizado com regra de pacing explícita pra ScheduleWakeup — tight (90s) quando há trabalho identificável na fila; long (1800s) só pra bloqueio externo; sentinela 3600s só em steady state. Antes, orquestrador estava usando o default conservador "1200-1800s pra idle" da doc do ScheduleWakeup mesmo com trabalho pendente. Reinicie o /loop /dwave em sessão fresca pra pegar a regra nova.
2026-05-15T12:56:00Z [ui-tester] F003 pass (C4 internal-db: login 200 + cookie httpOnly + /me 200 + redirect /app + logout clears + /me 401; nota: cookie name is 'director_session' not 'director_studio_session' — flag para curator se a spec exige nome literal)
2026-05-15T13:05:00Z [curator] F003 accepted: 9/9 criterios pass, contrato seguido (auth via processa-auth-paths), design system honrado (login-page + form-field + inline-alert + button). Divergencias observadas viraram follow-ups nao-bloqueantes: F028 (contrato DFnome_empresa→DFnome_fantasia, P1, archaeologist), F029 (alinhar nome do cookie director_session, P2, decisao de nomenclatura), F030 (FormField aria-invalid em error, P1, designer/smith). Divida fn_Decript fica fora do escopo Studio (herança contida — Studio so chama Validar_Cript). Cutover de F003 liberado.
2026-05-15T13:05:00Z [curator] feature-added: F028 contract-fix DFnome_fantasia (P1), F029 cookie-name-alignment (P2), F030 FormField-aria-invalid (P1).
2026-05-15T13:30:00Z [smith] status=wip: F004 sessao httpOnly cookie + Redis — Map em memoria substituido por ioredis store em apps/api/src/session-store.ts; sliding TTL (SESSION_TTL_HOURS=8 do .env, EXPIRE renovado a cada hit em /me); fail-loud no boot via ensureRedisReady (process.exit 1 se Redis down); /api/auth/login + /me respondem 503 session-store-unavailable se Redis indisponivel mid-flight; namespace de keys ds:sess:<sid>; HMAC do sid mantido (defense-in-depth). F029 (rename cookie) preservado fora-de-escopo.
2026-05-15T13:35:00Z [smith] note: F004 smoke test completo (todos pass) — (1) POST /login PROCESSA/99 → 200 + Set-Cookie director_session httpOnly SameSite=Lax MaxAge=28800; (2) GET /me #1 → 200 user.path=internal-db; (3) redis TTL=28799 confirmado; (4) GET /me #2 apos 3s → 200 + TTL renovado para 28800 (sliding session OK); (5) POST /logout → 200 ok; (6) GET /me apos logout → 401 no-session; (7) SCAN ds:sess:* → 0 keys (cleanup OK); (8) login com senha errada ainda → 401 invalid-credentials (caminho DB intacto); (9) boot com REDIS_URL=redis://localhost:1 → FATAL log + process.exit (fail-loud verificado). Arquivos novos: apps/api/src/redis.ts (singleton ioredis + ensureRedisReady) e apps/api/src/session-store.ts (createSession/getSession/destroySession async com EXPIRE sliding). Refactor em apps/api/src/routes/auth.ts (remove Map, async handlers, 503 gate) e apps/api/src/index.ts (await ensureRedisReady antes de serve). Nota lateral: o .env aponta REDIS_URL=redis://localhost:6379, que em dev cai no coletivos-redis (compartilhado); director-studio-redis vive em :3010 — separado por PROJECT_PREFIX. Keys namespaced com ds: nao colidem, mas a divergencia merece um issue separado (provavelmente F001 follow-up, nao escopo F004).
2026-05-15T13:35:00Z [smith] status=ready-for-test: F004 sessao httpOnly cookie + Redis — pronto para ui-tester.
2026-05-15T14:00:00Z [principal] note: harness migrado de /loop para Ralph Loop. Zero sleep entre waves (Stop hook re-injeta o prompt imediatamente). /dwave: Passo 6 reescrito — removida tabela de ScheduleWakeup (dead code em Ralph); adicionada regra única de parada via completion-promise <promise>STUDIO-RTM-DONE</promise>. HOW-TO-RUN-WAVES.md reescrito com comando Ralph canônico (--completion-promise "STUDIO-RTM-DONE" --max-iterations 1000). Motivo: blocos de tokens contratados — sleep entre waves é desperdício; Ralph queima 100% do tempo em trabalho. Estado durável em disco (progress + manifest + git) mitiga risco de context bloat na sessão única do Ralph.
[ui-tester] F004 pass
2026-05-15T14:30:00Z [curator] F004 accepted: 6/6 criterios pass — boot fail-loud, cookie httpOnly+SameSite+HMAC, sliding TTL, logout cleanup, 503 mid-flight, recuperacao. Contrato processa-auth-paths honrado. Observacoes nao-bloqueantes viraram follow-ups: F031 (wizard REDIS_URL derivado de PREFIX, P1), F032 (config.ts respeitar env externa, P2). Premissa de Redis volatil do criterio 6 documentada no test-report (compose tem appendonly+volume; sessoes persistem restart — decisao de infra, nao bug).
2026-05-15T14:30:00Z [curator] feature-added: F031 wizard-redis-url-from-prefix (P1), F032 config-respect-external-env (P2).
2026-05-15T11:30:00Z [archaeologist] feature-added: F005 contract → atlas/concepts/legacy-contracts/app-main.md (AppMain shell do react-tools — props, estado, storages @director/{appconfigs,acl,appmenu}, ciclo de vida boot, sub-componentes AppHeader/AppSidebar/AppMainTabs/Footer/PageBlur/AppMenu, modos especiais #/dashboard e #/auth, comportamento responsivo do legado anotado com TBD designer para mobile-first)
2026-05-15T15:00:00Z [designer] note: ui-system updated → atlas/concepts/ui-system/{app-shell,sidebar,app-header,page-tabs} (specs F005 mobile-first com expansao coerente: shell com 5 regioes + drawer mobile, sidebar persistente desktop / vaul-up mobile, header com breadcrumbs desktop + back-to-parent + search expansivel mobile, page-tabs reinventado: barra horizontal so desktop + recent-stack via bottom-sheet no mobile substituindo barra-com-x do legado). AppMenu switcher de apps Processa descartado (Studio app-unica). PageBlur imperativo do legado vira hook useBlocking() declarativo. Grupos do menu adotam comportamento acumulativo (era mutuamente exclusivo no legado).
2026-05-15T15:00:00Z [designer] note: ui-component-missing (sinalizado ao curator) — componentes derivados de F005 que poderiam virar features/items proprios do catalogo se reusados: notification-panel (popover desktop / drawer mobile), avatar-menu (popover com perfil+tema+logout, derivado direto do shadcn), shortcut-bar (mobile bottom com 5 slots editaveis + Menu), loading-state (skeleton+spinner+overlay para useBlocking), empty-state, toast (F020 ja prevista). Componentes referenciados nos specs como [[wiki]] mas ainda nao escritos — proximas waves se features demandarem.
2026-05-15T15:30:00Z [smith] status=wip: F005 app-layout — implementando AppShell (header + sidebar + page-tabs + shortcut-bar mobile via vaul), useBlocking hook, IsMobileProvider; componentes em packages/ui (sidebar, app-header, app-shell, page-tabs, shortcut-bar, avatar-menu, drawer, popover, dropdown-menu, tooltip, sheet/scroll-area conforme necessario).
2026-05-15T16:30:00Z [smith] status=ready-for-test: F005 app-layout — AppShell mobile-first em packages/ui orquestra header + sidebar (rail/expanded persistido localStorage director-studio:sidebar-collapsed) + page-tabs (sessionStorage studio:tabs:v1:<scope>) + shortcut-bar (mobile bottom, 5 slots+Menu, safe-area-inset-bottom) + drawer-up vaul para sidebar mobile + recent-stack-sheet (bottom-sheet mobile Stack chip no header). useIsMobile via matchMedia (breakpoint 768px). useBlocking declarativo com BlockingProvider+BlockingOverlay (substitui PageBlur imperativo). AvatarMenu com dropdown (perfil disabled placeholder + ThemeToggle slot + Sair via POST /api/auth/logout). Primitivos shadcn novos em packages/ui: drawer, dropdown-menu, popover, tooltip, avatar, separator, scroll-area. Phosphor only (House, List, Sidebar, CaretLeft, CaretRight, CaretDown, MagnifyingGlass, Bell, Stack, X, SignOut, UserCircle, CircleNotch, Check, Circle). Tokens semanticos via classes utility (bg-sidebar, bg-sidebar-accent, border-border, bg-background, text-muted-foreground). a11y: skip-link, header role=banner, nav aria-label, aria-current=page, aria-expanded, focus trap via vaul. /app (app-home.tsx) carrega /api/auth/me + injeta no shell (user.nome/usuario, role derivado de nomeFantasia+path). Menu hierarquico placeholder (F007 substitui). typecheck+build verdes. Arquivos: packages/ui/src/components/{drawer,dropdown-menu,popover,tooltip,avatar,separator,scroll-area,blocking-overlay,avatar-menu,app-sidebar,app-header,page-tabs,shortcut-bar,app-shell}.tsx; packages/ui/src/hooks/{use-is-mobile.ts,use-blocking.tsx,use-tabs-storage.ts}; packages/ui/package.json (vaul dep + hooks export); packages/ui/src/components/input.tsx (ref forwarding); apps/director-studio/src/routes/app-home.tsx (integra AppShell + logout).
[ui-tester] F005 blocked: backend API Hono down (502 em /api/* e /healthz); apenas caddy+redis no docker ps; nao foi possivel logar para validar shell
[ui-tester] F005 blocked: C1/C3/C4/C5/C7/C8 pass; C2 mobile nao testavel — viewport do MCP fixa em 1536px, matchMedia/innerWidth overrides nao re-disparam hooks responsivos. Pendente validacao manual em browser real <=767px.
2026-05-15T15:30:00Z [curator] F005 accepted: 7/8 criterios validados pelo ui-tester (C1 login→shell, C3 sidebar rail/expanded persistente, C4 AvatarMenu+ThemeToggle+Logout, C5 logout limpa cookie, C6 useBlocking n/a, C7 F006 tema intacto, C8 console limpo). Contrato app-main honrado, design system app-shell+sidebar+app-header+page-tabs aplicado, caso real PROCESSA/99→IMPERIAL LOG. C2 mobile drawer-up nao exercitavel por limitacao do harness (Chrome MCP viewport travado em 1536px) — codigo-completo segundo spec mas nao empiricamente validado. Vira F033 (P1) para validacao em viewport mobile real. Cutover de F005 liberado; F033 fica como compromisso de cobertura pre-cutover final.
2026-05-15T15:30:00Z [curator] feature-added: F033 validate-shell-mobile-viewport (P1, follow-up de F005 C2) — requer device emulation real (CDP Emulation.setDeviceMetricsOverride) ou browser real em 375/414/767px.
2026-05-15T17:00:00Z [archaeologist] feature-added: F007 contract → atlas/concepts/legacy-contracts/{acesso-obter-rotas-aplicacao,menu-hierarquia} (proc canonica do menu é obter_acl_token, NÃO obter_rotas_aplicacao — manifest corrigido; menu legado tem profundidade 2 fixa modulo→pagina, mutuamente exclusivo no expand, sem highlight de item ativo, sem badges, sem busca, cache de sessão sem invalidação; defaultMapper descarta funcoes/empresas/visible — requer customMapper para preservar; empresas e DFexibir_menu são bugs históricos prováveis)
2026-05-15T17:30:00Z [smith] status=wip: F007 menu hierarquico — endpoint GET /api/menu/routes consumindo acesso.obter_acl_token via XML param (idUsuario, empresa, chaveAplicacao); APP_KEY default=portal-director (probed: 2 modulos/9 paginas para PROCESSA na Area 52). Cache Redis ds:menu:<userId>:<appKey> TTL 60s. Hook useMenu() em packages/ui cacheia em sessionStorage. AppSidebar render real substituindo placeholder; grupos acumulativos persistidos em localStorage.
2026-05-15T18:00:00Z [smith] status=ready-for-test: F007 menu hierarquico — backend GET /api/menu/routes implementado (apps/api/src/routes/menu.ts): valida sessao via cookie, executa acesso.obter_acl_token(<idUsuario><empresa><chaveAplicacao>) com schema-aware discovery, parser XML dedicado (sem dep externa), normaliza pra { modules: [{id,label,icon?,description?,key?,children:[{id,label,path,icon?,description?,key?}]}] }, filtra DFexibir_menu=1, cache Redis ds:menu:<userId>:<appKey> TTL=60s (override MENU_CACHE_TTL); super-user (id=1, nome=PROCESSA) usa empresa='Processa', demais usam principal.nomeEmpresa, override STUDIO_EMPRESA. Frontend: hook useMenu() em packages/ui/src/hooks/use-menu.ts (sessionStorage studio:menu:v1 + refetch()), SidebarMenu em packages/ui/src/components/sidebar-menu.tsx (grupos acumulativos com localStorage director-studio:sidebar-groups + auto-open do grupo com rota ativa, popover lateral em rail, item-active border-l-2 border-primary + aria-current=page, divider entre grupos, icone Phosphor mapeado por chave CoreUI com fallback File/Folder). AppSidebar/AppShell aceitam menuSlot como ReactNode ou (ctx:{isRail})=>ReactNode pra adaptar rail vs expanded. Routing: /app/$splat catch-all renderizado por AppPage (substitui app-home.tsx), navegacao via handleNavigate→navigate({to:/app/$,params:{_splat:...}}), breadcrumbs derivados do menu, placeholder com mensagem "F009 substitui" pra paginas nao implementadas. Drawer mobile reutiliza mesmo menuSlot via AppShell. Smoke test backend: PROCESSA/99 → /api/menu/routes (cookie httpOnly) → 2 modulos (Configuracoes=6 paginas, Controle de Acessos=3 paginas) = 9 paginas total da Area 52; segunda chamada cached:true + TTL=47s. typecheck+build limpos. Nota nao-bloqueante: mojibake "ConfiguraÃ§Ãµes" em 1 label (Name do modulo) — origem nos dados do SQL Server, mesmo problema do legado; nao tocado. Arquivos novos: apps/api/src/routes/menu.ts, packages/ui/src/hooks/use-menu.ts, packages/ui/src/components/sidebar-menu.tsx, apps/director-studio/src/routes/app-page.tsx, mind/effort/on/director-studio/backlog/F007-decisions.md. Editados: apps/api/src/index.ts (route mount), packages/ui/src/components/app-sidebar.tsx (menuSlot render-prop), packages/ui/src/components/app-shell.tsx (menuSlot type), apps/director-studio/src/routes/tree.tsx (/app + /app/$). Removido: apps/director-studio/src/routes/app-home.tsx (substituido por app-page.tsx).
2026-05-15T17:00:00Z [ui-tester] pass: F007 8/8 exercitaveis pass — endpoint funcional, cache redis hit, sidebar render real (2 modulos / 9 paginas), expand acumulativo persistido localStorage director-studio:sidebar-groups, rail popover Radix com sub-itens, click navega /app/<path>, F005/F006 intactos, console limpo. C7 mobile drawer nao-exercitavel (herdado F033). Followup nao-bloqueante: mojibake em aria-label da sidebar (ConfiguraÃ§Ãµes em vez de Configurações; textContent OK).
