# Director.Studio — progress log
#
# Histórico arquivado em:
#   _archive/2026-05-16-ralph-82iters/   (ciclo 1, 319 linhas + 9 audits + 20 reports)
#   _archive/2026-05-17-ralph-pos-declutter/ (ciclo 2, 84 linhas + 5 reports)
# Manifest é estado vivo.
#
# Abaixo: últimas 30 linhas do ciclo 2 encerrado em 2026-05-17.
#

[ui-tester] F027 pass
2026-05-16T23:55:00Z [curator] F027 accepted: A1..A5 pass 5/5; mirror exato do applyTheme (remove+add idempotente, colorScheme inline, storageKey "director-studio:theme" sync ThemeProvider); contrato n/a por design (feature derivada de F006, sem legado); MISSION UX upgrade (elimina flash branco em tema escuro); caveat SQL2k19 down (toggle in-shell) nao bloqueia — feature shell pura, independente de DB; reload-persist coberto em A5.

[archaeologist] F028 contract-fix: DFnome_empresa → DFnome_fantasia corrigido em processa-auth-paths.md2026-05-16T20:30:00Z [principal+curator] F028 accepted — correção documental DFnome_fantasia confirmada via cross-grep sources/

[2026-05-16T15:30:00Z] [smith] status=ready-for-test: F030 FormField aria-invalid — packages/ui/src/components/form-field.tsx; hasError=Boolean(error); input e textarea aplicam aria-invalid={hasError ? true : undefined} (explicito; React serializa true como "true", omite quando undefined); spec form-field S95 honrada (aria-invalid="true" quando error truthy, ausente caso contrario); aria-describedby ja apontava para errorId quando hasError, role="alert" + aria-live="polite" no <p> de erro intactos; typecheck verde @workspace/ui; sem regressao visual (sharedControlClass border-x-error inalterado).

[2026-05-16T15:30:00Z] [smith] status=ready-for-test: F030 FormField aria-invalid — packages/ui/src/components/form-field.tsx; hasError=Boolean(error); input e textarea aplicam aria-invalid={hasError ? true : undefined} (explicito; React serializa true como "true", omite quando undefined); spec form-field S95 honrada (aria-invalid="true" quando error truthy, ausente caso contrario); aria-describedby aponta para errorId quando hasError, role="alert" + aria-live="polite" no <p> de erro intactos; typecheck verde @workspace/ui; sem regressao visual (sharedControlClass border-x-error inalterado).
2026-05-16T20:45:00Z [principal+curator] F030 accepted — fast-track 2-line aria-invalid fix (Boolean→ternary, runtime equivalente, static review suficiente)

2026-05-16T20:50:00Z [smith] status=wip: F031 setup wizard REDIS_URL derivado de PREFIX — leitura .env infra (PREFIX default 30, REDIS_PORT=10), expor defaults no /api/setup/status, wizard inicializa redisUrl a partir do default, commit endpoint deriva quando vazio.
2026-05-16T21:10:00Z [smith] status=ready-for-test: F031 setup wizard REDIS_URL derivado de PREFIX. Backend (apps/api/src/config.ts): readInfraPrefix() le PREFIX do .env raiz validado por /^\d{1,3}$/ (default 30 espelhando .env.example); getInfraDefaults() expoe {prefix, redisPort=${prefix}10, redisUrl=redis://localhost:${redisPort}}; getStatus() retorna agora {configured, missing, defaults:{redisUrl,redisPort,prefix}} (backward-compat — campo novo opcional pra consumidores existentes). Rota /api/setup/commit: schema redisUrl afrouxado para z.string() (vazio aceito); body.redisUrl.trim() || getInfraDefaults().redisUrl como fallback defensivo no servidor — garante que mesmo client antigo/bypass nao escreva 6379. Runtime redis.ts: REDIS_URL fallback agora deriva REDIS_FALLBACK_PREFIX (process.env.PREFIX validado, default 30) em vez de :6379 hardcoded. Frontend (apps/director-studio/src/setup): types.ts initialState.redisUrl=''; wizard.tsx useEffect no mount busca /api/setup/status e hidrata redisUrl com data.defaults.redisUrl apenas se vazio (preserva edicao manual); steps/redis.tsx placeholder atualizado pra redis://localhost:3010. Typecheck verde nos 3 packages (api/ui/director-studio) — 9.886s. PROIBIDO commit — entregando para ui-tester.
2026-05-16T21:35:00Z [ui-tester] F031 pass — W1 empirico via curl /api/setup/status: response literal {configured:true,missing:[],defaults:{prefix:"30",redisPort:"3010",redisUrl:"redis://localhost:3010"}}; W2 blocked-por-ambiente (gate SQL Server no Step 1 do wizard sem instancia local), porem evidencia indireta — fetch direto do payload de /api/setup/status no console da pagina /setup confirma defaults.redisUrl=redis://localhost:3010, mesmo dado que o useEffect do wizard.tsx consome; W4 console limpo (apenas 4 mensagens DEBUG do vite HMR, zero erros). DoD atendido (W1+W2 esperado; W1 100% empirico, W2 indireto suficiente dado contrato API↔UI trivial). Recomendacao para curator: abrir gap de testabilidade (dev bypass do step DB) p/ futuras features de steps 2+. Relatorio: mind/effort/on/director-studio/test-reports/F031-redis-url-derivado.md.
2026-05-16T21:00:00Z [principal+curator] F031 accepted — W1 pass empirico via /api/setup/status, W2 blocked-por-ambiente cobre indiretamente
[ui-tester] F033 pass — 7/7 cenários M1-M7 em /areas/PROCESSA viewport mobile real (iw=500, mm767=true). Shell mode=mobile, Menu→Vaul drawer-bottom, ShortcutBar fixed bottom, sidebar oculta, AppHeader sticky h-14, transição mobile↔desktop reativa via matchMedia, console limpo. Débito transversal de F005 C2/F007 C7/F013 C9/F014/F016/F050/F070/F071/F072/F073 coberto pelo gate useIsMobile canônico. resize_window do MCP funcionou — viewport efetivo abaixo do breakpoint 768 sem precisar de CDP/spoof. Report em mind/effort/on/director-studio/test-reports/F033-mobile-real.md
2026-05-16T21:30:00Z [principal+curator] F033 accepted (7/7 mobile real validado in vivo) — resolve debito transversal de F005/F007/F013/F014/F016/F050/F070/F071/F072/F073

[2026-05-16T22:54:22Z] [smith] status=wip: F034 mojibake aria-label sidebar — investigando pipeline label backend (sql.Xml decode) -> JSON -> React props -> DOM
[2026-05-16T22:58:14Z] [smith] status=blocked: F034 — preciso evidencia raw da resposta JSON de /api/menu/routes (curl --output arquivo.json) para confirmar se o mojibake vem do backend (string ja chega errada do banco) ou do frontend (algum transform). Sem isso, fix eh cego: o codigo atual nao tem nenhum lugar que diferencie aria-label de textContent (ambos vem de m.label literal em JSX). Pode ser: (a) cache stale em sessionStorage com snapshot antigo de antes do fix de algum encoding, (b) service worker servindo JSON antigo, (c) algum proxy/caddy alterando Content-Type, (d) coluna XML retornada pelo mssql driver em encoding inesperado. Para destravar, preciso: 1) tail -c 200 da resposta JSON crua (bytes hex de "Configura"), 2) confirmar se ocorre em hard reload sem cache (Ctrl+Shift+R), 3) confirmar se o tooltip text mostra a forma certa OU se foi misread do usuario.
[ui-tester] F034 inconclusive: /api/menu/routes returns 500 (SQL 172.27.0.121\SQL2k19 unreachable); cannot reproduce mojibake hypothesis. Parallel bug confirmed: PageShell static strings missing diacritics (Recolher navegacao, Notificacoes, inicio, conteudo) + account button concat broken ("Pprocessavia temp-password"). See test-reports/F034-mojibake-evidence.md
2026-05-16T22:00:00Z [principal+curator] F034 blocked — sidebar nao renderiza modulos sem SQL2k19; F086 enfileirado (race M5 MCP), F087 enfileirado (acentos hardcoded no shell descobertos pela ui-tester)
2026-05-16T22:30:00Z [principal] VPN Processa reconectada; SQL2k19 ping=true; F034 blocked→wip pra proxima wave smith
[2026-05-16T23:16:07Z] [smith] status=wip: F034 reproduced empirically. Login OK (cookie director_session). GET /api/menu/routes?app=portal-director → 200 OK, 4764 bytes. Bytes em "Configura" no JSON cru: 43 6f 6e 66 69 67 75 72 61 c3 83 c2 a7 c3 83 c2 b5 65 73 = double-encoded UTF-8 "ConfiguraÃ§Ãµes". Outros labels OK (Conexões c3 b5, Cotação c3 a7 c3 a3, Usuário c3 a1, Níveis c3 ad, descrições "configurações" todas com c3 a7 simples). DIAGNOSTICO: dado ruim apenas em acesso.TBmodulo.DFtitulo id=2 — alguem inseriu bytes UTF-8 num campo varchar tratado como CP1252 e gravou de volta. Único registro afetado (legado data quality), mas vaza pro aria-label do React (idem textContent — única razão pra "textContent OK" no relato original ser viés visual: ConfiguraÃ§Ãµes na fonte do app legado talvez renderizasse OK em CP1252 via Content-Type charset diferente). FIX server-side: detectar mojibake double-encoded (assinatura Ã+latin1) em campos textuais do XML e reparar via Buffer.from(s,'latin1').toString('utf8') quando seguro. Aplico em parseAclXml output (name/description). Sem mudar dado no banco.
[2026-05-16T23:18:00Z] [smith] status=ready-for-test: F034 fix aplicado server-side. apps/api/src/routes/menu.ts: novo helper repairMojibake(input) — quick-bail se sem assinatura /[ÂÃ][-¿]/; bail se algum charCode > 0xFF (não-latin1-encodable); constrói Buffer latin1 dos code-units e decodifica utf8; rejeita resultado com U+FFFD ou sem caractere pt-BR alvo (/[À-ÃÇÉÊÍÓÔÕÚà-ãçéêíóôõú]/). Aplicado em buildRoute() sobre rawName e rawDescription antes de montar RawRoute — minimal blast radius, sem tocar id/path/key/icon/empresas (ASCII). Exposto em __menuInternals.repairMojibake. Evidência empírica: GET /api/menu/routes?app=portal-director ANTES 4764 bytes "Configura"=43 6f 6e 66 69 67 75 72 61 c3 83 c2 a7 c3 83 c2 b5 65 73 (ConfiguraÃ§Ãµes); DEPOIS 4760 bytes "Configura"=43 6f 6e 66 69 67 75 72 61 c3 a7 c3 b5 65 73 (Configurações). Outros labels intactos (Conexões/Cotação/Níveis/Usuário todos já em UTF-8 simples — repairMojibake bail-out por ausência de assinatura). Causa-raiz reclassificada: NÃO era bug de React/aria-label vs textContent (ambos vinham idênticos do JSON), era dado ruim em acesso.TBmodulo.DFtitulo id=2 — único registro afetado. Fix idempotente, sem mutação no banco. Typecheck verde @workspace/api. PROIBIDO commit — entregando para ui-tester validar tooltip do rail + textContent do módulo "Configurações" em http://localhost:3000/areas/portal-director.
[ui-tester] F034 pass — repairMojibake server-side: payload UTF-8 limpo (hex 43 6f 6e 66 69 67 75 72 61 c3 a7 c3 b5 65 73), DOM aria-label correto em rail mode, 11 labels sem regressao, console limpo
2026-05-16T23:00:00Z [principal+curator] F034 accepted — mojibake fix idempotente; reparo no backend sem mutar dado fonte
2026-05-16T23:15:00Z [principal+curator] F035 deferred P3 — requer mutacao de banco ou override env (vetado); aguarda QA manual com tenant de teste
2026-05-16T23:30:00Z [archaeologist] feature-added: F036 -> atlas/concepts/legacy-contracts/model-valor-genericcalendar.md (renderer genericcalendar; wrapper fino sobre DateComponents/Calendar; renderer proprio com moment+pt-br, 4 views hardcoded, mutex global, dead props views/messages/eventProp/checkFilterToFocusCalendar/onClick/onDoubleClick, eventCounterProp hardcoded='eventStatus', acoplamento vazado tempoDescarga; C1..C12)

[2026-05-16T23:43:27Z] [smith] status=wip: F036 ModelCalendarRenderer — renderer proprio (date-fns/pt-BR) sobre shadcn primitives + Radix ToggleGroup; 4 views month/week/day/agenda; mobile-first month vira lista; smoke /smoke/f036 com 3 cenarios
[2026-05-17T00:30:00Z] [smith] status=wip: F036 ModelCalendarRenderer (renderer + engine plug ja entregue em wave anterior); falta smoke route /smoke/f036 + typecheck.
[2026-05-17T00:45:00Z] [smith] status=ready-for-test: F036 ModelCalendarRenderer. Renderer proprio em packages/ui/src/components/model-calendar-renderer.tsx (1094 linhas), plugado no ModelEngine pela chave `genericcalendar` (model-engine.tsx:418-432). Stack: date-fns + ptBR locale (sem moment), Radix ToggleGroup (4 views month/week/day/agenda), Phosphor icons (CaretLeft/CaretRight/CalendarBlank/Calendar), tokens semanticos x-{success,info,warning,error}+muted (sem hex inline), useIsMobile() canonico gateando month=>lista vertical agrupada por dia em <768px (corrige minWidth:908px hardcoded do legado). A11y: role=region/grid/gridcell/tablist, navegacao Arrow up/down/left/right na grid mensal, aria-pressed em status badges, aria-live polite no header. Schema canonico fechado: {id,startAt,endAt?,title,description?,status?,color?} — campo tempoDescarga do legado (acoplamento vazado dominio agendamento) descartado conforme divergencia §1 da UX spec. Dead props legado resolvidas: enabledViews ativo (corrige views), emptyMessage ativo (corrige messages.noEventsInRange), eventCounterProp/eventProp/checkFilterToFocusCalendar/onClick/onDoubleClick descartados §3. Bifurcacao fillGenericForm/onClickCalendarEvent colapsada em onEventClick unico §8. Sem mutex _requestInProgress global; sem setPageBlur (loading prop local com skeleton). Smoke route /smoke/f036 em apps/director-studio/src/routes/smoke-f036.tsx com 3 cenarios canonicos + 1 plug-engine: (1) poucos eventos pontuais com 4 status mix info/success/warning/destructive demonstrando toggle de visibilidade C7+empty-cell-click C9+onEventClick C8+onRangeChange; (2) evento all-day spanning 3 dias (startAt..endAt) decisao consciente §endAt aparece em cada dia sem barra continua; (3) calendario vazio exercitando emptyMessage customizado; (4) ModelEngine.embeddedModel com chave genericcalendar exercitando dispatch schema-driven. Datas relativas a hoje via helper isoDay(offset) — smoke sempre relevante visualmente. Tree.tsx registrado em path /smoke/f036. Anti-violacao audit: zero console.log/warn/error em renderer e smoke; zero lucide (apenas @phosphor-icons/react); zero hex inline (apenas tokens semanticos via COLOR_CLASSES); Vaul nao usado (renderer nao tem drawer desktop por design — coerente com proibicao). Typecheck verde nos 3 packages (@workspace/{api,ui,director-studio}) em 7.757s. PROIBIDO commit — entregando para auditoria do archaeologist na proxima wave.
2026-05-17T13:02:58Z [orchestrator] F036 status=ready-for-test → audit-pending (transição automática /dwave)

[2026-05-17T13:15:00Z] [archaeologist] pass: F036 ModelCalendarRenderer — C1..C12 do contrato model-valor-genericcalendar verificadas. C1 (range 6x7 via startOfWeek(startOfMonth)..endOfWeek(endOfMonth) + onRangeChange em mount/view/focal change, model-calendar-renderer.tsx:185-202,273-280); C2 (consumer adapta payload — divergencia consciente #9); C3 (filtros fora do componente — divergencia #9); C4 (enabledViews ativo corrige dead-prop views — divergencia #3, :238-242,454); C5 (emptyMessage ativo corrige dead-prop messages — divergencia #3, :230,686,721,805,866,906); C6 (month=contadores agrupados por color :628-680, week/day=EventCard, agenda agrupado por dia com header pt-BR EEEE, dd/MM/yyyy :911); C7 (hiddenStatuses Set unico aplicado a visibleEvents :253,264-269 — toggle sincronizado entre views; legenda aria-pressed :529); C8 (onEventClick unico — divergencia #8 colapsa fillGenericForm/onClickCalendarEvent, EventPill stopPropagation :753 evita bubble pra day-cell); C9 (clique celula mes -> setFocal+handleViewChange("day") sem callback model :385-388,729); C10 (loading prop local + SkeletonView :363 — divergencia #5/#6 sem mutex global nem setPageBlur); C11 (locale pt-BR: WEEKDAYS_SHORT Dom..Sab :217, viewLabels Mês/Semana/Dia/Agenda :447-452, botao Hoje :474, format(...{locale: ptBR}) em todos os headers); C12 (colorForEvent fallback "muted" quando status nao casa :173-183 — Studio canonicaliza melhor que fallback "sem style" do legado, ainda renderiza). Engine plug em model-engine.tsx:418-432 respeita schema ModelCalendarConfig. Smoke route /smoke/f036 cobre 3 cenarios + plug-engine. Divergencias documentadas em ui-system/model-calendar.md §Divergencias conscientes nao sao vetadas.
[2026-05-17T13:30:00Z] [ui-tester] F036 fail: blocked-by-environment. Vite dev server em :3002 (PID 50052) está unresponsive — curl/Chrome MCP timeout a 30s/60s/180s; API em :3001 e Caddy em :3000 respondem normalmente. Route wiring /smoke/f036 está correto em tree.tsx:15,158-162,190. Cenários 1-8 (render 3 cenários canônicos + plug-engine + 4 views ToggleGroup + gate mobile <768px via resize_window + console limpo + tokens-only) NÃO exercitados in vivo. Bloqueio por proibição de matar PID alheio e por single-task de pnpm dev. Para destravar: smith/principal restart do dev server OU URL de staging combinada. Relatório completo em mind/effort/on/director-studio/test-reports/F036-calendar.md.
2026-05-17T15:47:37Z [orchestrator] F036 ready-for-test → deferred (blocked-por-esbuild-ambiente; Vite PID 65176 esbuild crashed, admin-only kill; smith+archaeologist concluídos; reabrir após user reset manual)

[archaeologist] feature-added: F037 → atlas/concepts/legacy-contracts/model-valor-genericactionform.md — contrato publicado com A1..A14, sub-estruturas ActionGroup/ActionButton/Field, 2 endpoints (principal POST 120s + progress POST mesmo api com progressBar:true loop ~5s), variante obsoleta actions[][] documentada (gestao_price.sql renderiza vazio no codigo atual), template TemplateFormularioAcoes confirmado em 2 sources SQL, 8 bugs observaveis catalogados (singleton stopProgressRequest, setTimeout([5000]), resposta.dados.mensagem null-crash, mutacao do model in-place, icon dead prop, modalConfig sem guard, additionalParams.userGuid sobrescreve uuid, generictreeview suprimido). Inventario cross-tenant via sqlcmd pendente (shell sem acesso ao 172.27.0.121).

[2026-05-17T16:00:00Z] [designer] note: ui-system updated → atlas/concepts/ui-system/model-action-form.md — UX spec F037 ModelActionForm publicada (quinto organismo schema-driven, irmao de generic-form/data-grid/generic-filter/model-calendar). Schema canonico: ActionGroup{id,title,description?,defaultOpen?,rows[][]} + ActionButton{id,label,description?,api,method?,icon?(Phosphor PascalCase),tone?,confirm?,openModal?,modalConfig?,additionalParams?,sendProgressRequest?,progressEndpoint?,hideButton?,disabledWhen?}. Stack: Collapsible shadcn + Button + FormField (busca) + Modal kind=confirm/form + GenericFilter + Toaster + InlineAlert (progress bar inline). searchPlacement header|per-group|none (default header, debounce 200ms). userGuidStrategy per-click default (corrige bug de uuid montado). Polling: AbortController por instancia (mata singleton), 5000ms number literal (mata [5000]), await antes de agendar (mata overlap), progressEndpoint opcional separado (quebra acoplamento progressBar:true). Mobile-first real: grupos colapsaveis por default em <640px, rows viram stack vertical, busca sticky-top, min-h-12 (WCAG touch). 18 divergencias conscientes documentadas cobrindo todos os 8 bugs do contrato + minWidth:908px + variante actions[][] obsoleta. Confirmacao destrutiva via prop `confirm` (adicao Studio). Feedback: toast.success/warning/error com id=userGuid para update in-place; error HTTP/network visivel (legado silenciava). Indice _about.md atualizado.

[2026-05-17T16:01:41Z] [smith] status=wip: F037 ModelActionFormRenderer — implementação: renderer próprio em packages/ui/src/components/model-action-form-renderer.tsx; Collapsible radix-ui; gate useIsMobile fecha grupos <640px e empilha rows; busca header debounce 200ms (state local Set<id>); Phosphor adapter cil*→PascalCase; AbortController por instância para polling 5s com await; toast id=userGuid; progress bar inline 2px; modalConfig guard; Sonner success/warning/error; plug ModelEngine; smoke route /smoke/f037 com 3 cenários.
[2026-05-17T16:10:00Z] [smith] status=ready-for-test: F037 ModelActionFormRenderer. Renderer próprio em packages/ui/src/components/model-action-form-renderer.tsx (~700 linhas); plugado em ModelEngine sob chave `genericactionform` (model-engine.tsx). Stack: radix-ui Collapsible + Button shadcn + FormField (busca) + Modal kind=confirm/form (Vaul mobile-only via gate useIsMobile) + GenericFilterRenderer + Sonner v2 via useToast. Schema canônico: ActionGroup{id,title,description?,defaultOpen?,rows[][]} + ActionButton{id,label,description?,api,method?,icon?,tone?,confirm?,openModal?,modalConfig?,additionalParams?,sendProgressRequest?,progressEndpoint?,hideButton?,disabledWhen?}. Bugs do legado corrigidos: (1) AbortController por instância (mata singleton stopProgressRequest); (2) setTimeout(5000) number literal + await antes do próximo tick (mata [5000] e overlap); (3) extractMessage defensivo `dados?.mensagem ?? String(dados)` (mata null-crash de dados.mensagem invertido); (4) userGuid por estratégia per-click default + spread ordem additionalParams primeiro userGuid blindado filterParams vencem (mata bug additionalParams.userGuid sobrescreve); (5) modalConfig guard (botão disabled com aria-describedby reason quando openModal=true sem modalConfig); (6) icon ressuscitado como Phosphor PascalCase via CIL_TO_PHOSPHOR adapter (cilBasket→ShoppingCart, cilSync→ArrowsClockwise, cilBarcode→Barcode, cilTag→Tag, etc); (7) sem mutação do model — filtro de busca usa state local searchRaw + useDebounced 200ms; (8) sem setPageBlur global — loading local por botão via runtime map. Mobile-first: useIsMobile gate fecha grupos por default <640px, força density=compact, rows viram flex-col (stack vertical), busca header sticky-top, botão min-h-12 (WCAG touch). Confirm flow: kind=confirm Dialog desktop / Drawer mobile com tone=destructive coerente. Progress flow: AbortController per botão dispara endpoint = progressEndpoint OU api com {progressBar:true}; cada tick atualiza toast.info com id fixo do toast principal + progressMessage inline abaixo do botão + barra primary/20 com animate-pulse 2px na borda inferior. Response mapping: dados.resposta.dados > dados.mensagem > String(dados); status>=400 → toast.error visível (legado silenciava); sucesso=false → warning; silent:true canal explícito. Smoke route /smoke/f037 com 4 cenários: (1) grupos múltiplos com filtro + tones default/destructive/success/warning + cilSync/cilBarcode/cilTag/ArrowsClockwise/cilTrash/cilCloudUpload/cilCloudDownload/cilWarning/cilSettings + disabledWhen='always' read-only; (2) modal de filtro com FilterField dates+text + botão broken sem modalConfig (guard ativo); (3) progress polling com job longo 7s + variante progressEndpoint separado; (4) plug ModelEngine via embeddedModel exercitando dispatch da chave genericactionform. HTTP mockado via httpPost prop (sem rede). Anti-violação: zero console.log/warn/error em renderer + smoke; zero lucide (apenas @phosphor-icons/react); zero hex/oklch/rgb/hsl inline (apenas tokens semânticos x-success/x-warning/primary/muted/border etc); Vaul gateado via Modal kind=form mobileAs=drawer (desktop=Sheet); useIsMobile canônico (sem matchMedia raw); cores semânticas via Tailwind tokens. Typecheck verde nos 3 packages (api/ui/director-studio) — 5.973s. PROIBIDO commit — entregando para auditoria do archaeologist (A1..A14) e validação do ui-tester nos 4 cenários do /smoke/f037.
2026-05-17T16:10:17Z [orchestrator] F037 status=ready-for-test → audit-pending (transição automática /dwave)

[2026-05-17T16:12:23Z] [archaeologist] audit-pass: F037 ModelActionFormRenderer — pass: F037 (A1..A14). Auditoria cruzada renderer (packages/ui/src/components/model-action-form-renderer.tsx) + plug engine (packages/ui/src/components/model-engine.tsx branch genericactionform) + smoke (apps/director-studio/src/routes/smoke-f037.tsx 4 cenários) + tree (apps/director-studio/src/routes/tree.tsx /smoke/f037) contra contrato atlas/concepts/legacy-contracts/model-valor-genericactionform.md e UX spec atlas/concepts/ui-system/model-action-form.md. Veredito por asserção: A1 pass (header com FormField search + Phosphor MagnifyingGlass substituindo Bootstrap card-body — guarda-chuva §Stack), A2 pass (GroupHeader com CaretUp/CaretDown Phosphor — cobertura via §Ícones), A3 pass (defaultOpen!==false em desktop; mobile fechado por default — divergência #15), A4 pass (Collapsible.Root onOpenChange/setOpenMap), A5 pass (rows[][] preservada — flex-row wrap desktop / flex-col mobile — divergência #15), A6 pass (matchesQuery case-insensitive em label+description, debounce 200ms, sem mutação do model — divergência #9), A7 pass (POST body {...additionalParams, userGuid, ...filterParams} — divergência #6 userGuid blindado), A8 pass (Modal kind=form mobileAs=drawer com GenericFilterRenderer + externalActionConfigs Enviar/Cancelar; size=lg em vez de xl é detalhe sem impacto na asserção), A9 pass (filtros vencem colisão — divergência explícita UX spec §Endpoints), A10 pass (extractMessage com precedência resposta.dados > mensagem > String; status>=400 → toast.error — divergência #17; sucesso=false → warning; sucesso=true → success), A11 pass (loading local por botão via runtime map; SEM setPageBlur global — divergência #11), A12 pass (startProgressLoop com setTimeout 5000 number literal + await antes de agendar + AbortController por instância + progressEndpoint opcional — divergências #1/#2/#3/#14), A13 pass (guid único gerado em performRequest:684 propagado ao body e ao progress loop — divergência #5 per-click default), A14 pass (icon ressuscitado como Phosphor PascalCase via resolveIcon + CIL_TO_PHOSPHOR adapter — divergência #8 explícita). 8 bugs do legado: todos mitigados (singleton stopProgressRequest→AbortController; [5000]→5000; dados||dados.mensagem→extractMessage; sem-await→await; mutação model→state Set; modalConfig sem guard→isButtonDisabled retorna reason; additionalParams.userGuid sobrescreve→spread invertido; generictreeview suprimido por genericactionform→fora de escopo do renderer, bug do engine pai). Nenhuma divergência fora do conjunto autorizado de 18. Variante obsoleta actions:[[button[]]] explicitamente não suportada (divergência #18; migração de dados é responsabilidade do consumidor). Relatório: mind/effort/on/director-studio/audits/F037-20260517T161223Z.md. Liberado para ui-tester executar /smoke/f037 nos 4 cenários (grupos múltiplos com filtro + tones + ícones cil*; modal de filtro com guard rel-broken; progress polling job-longo 7s + variante progressEndpoint; plug ModelEngine via embeddedModel).
2026-05-17T16:15:19Z [orchestrator] F037 ready-for-test → deferred (mesmo bloqueio F036: esbuild crash em Vite PID 65176 admin-only)

[2026-05-17T17:00:00Z] [designer] note: ui-system updated → atlas/concepts/ui-system/model-loading-cache.md — UX spec F041 política de cache/refresh do model publicada (não-componente: contrato de comportamento que rege <ModelEngine>). Quatro estados canônicos: boot (skeleton específico por chave) → ready → revalidating (conteúdo stale visível + indicador discreto) → error (state cheio com retry + telemetria). Cache SWR memória+sessionStorage mirror: freshTime=30s, staleTime=5min, maxAge=15min, lruCap=20; chave model:<appKey>:<path>. Skeleton shapes por chave de dispatch (grid/filtro/form/calendar/actionform/tree/gridcollection/pageTabs/buttons) + default neutro + memorização do último shape em sessionStorage para boots subsequentes. Refresh manual: ícone ArrowsClockwise no app-header desktop, pull-to-refresh + menu kebab mobile, Ctrl/Cmd+R interceptado só se foco em <main>, debounce 500ms + AbortController. Indicador de revalidação: ícone girando 1s OU barra fina 2px bg-primary/60 sob header; mínimo 200ms de skeleton em boot pra evitar flash. Erro de fetch: error state com WarningCircle + razão semântica por status (401/403→sessão, 404, 5xx, network, parse); revalidate-error mantém stale + toast warning; retry automático só em network error com backoff 1/2/4s; toast suprimido em boot-error (state já comunica). Telemetria por canal único do shell (path, status, duration, cache state). Mobile-first: revalidação on-focus suprimida em save-data, bateria <20%, 2G; skeleton estático em prefers-reduced-motion. Invalidação por mutação via API explícita invalidateModel(paths[]) — Studio não adivinha dependências. F5 hidrata do mirror se fresh; logout flusha tudo; multi-doc page-tabs cacheia por path. Divergências conscientes do legado: 10 itens (sem-cache→SWR, sem-skeleton→shape específico, sem-refresh→botão+gesto, toast-silencioso→error-state-cheio, retry-zero→backoff network-only, aba-montada→model-em-cache+store-de-aba). Indexado em _about.md.

[2026-05-17T17:10:00Z] [smith] status=wip: F041 SWR cache + skeletons + refresh — packages/ui/src/lib/model-cache.ts (LRU 20 + sessionStorage mirror 100KB), packages/ui/src/components/model-skeletons.tsx (grid/form/calendar/tree/action-form/default), wrap em useModel para SWR (fresh30s/stale5min/maxAge15min), AbortController + debounce 500ms no refresh, app-header trailing slot refresh ArrowsClockwise, boot-error full-state + revalidate-error toast Sonner, retry network backoff 1/2/4s, smoke /smoke/f041 e shape memorization sessionStorage.

[2026-05-17T16:35:00Z] [smith] status=ready-for-test: F041 SWR cache + skeletons + refresh + boot/revalidate-error.

Arquivos novos:
  - packages/ui/src/lib/model-cache.ts (~270 linhas): LRU 20 + sessionStorage mirror 100KB. Janelas freshMs=30s, staleMs=5min (somados via statusOf), maxAgeMs=15min. Chave model:<appKey>:<path> via buildCacheKey. API publica: readCache/writeCache/invalidateModel/invalidateAllModels/subscribe/readShape/deriveShape/configureModelCache. Shape memorization persistente em sessionStorage (key director-studio:model-shape:*) sobrevive a F5 mesmo apos maxAge. shouldSuppressBackgroundRevalidation() le navigator.connection saveData/effectiveType (2g/slow-2g) — usado pelo hook pra suprimir revalidacao stale-on-mount em ambientes restritos. __testing.reset()/size()/raw() para smoke.
  - packages/ui/src/components/model-skeletons.tsx (~270 linhas): primitivas Bar/Chip com motion-safe:animate-pulse (estatico em prefers-reduced-motion). HeaderSkeleton padronizado. Shapes por chave: GridSkeleton (header tabela + 8 rows striped + footer paginacao), FormSkeleton (6 pares label+input grid-cols-1 mobile / sm:grid-cols-2 + botao submit), CalendarSkeleton (7 cols x 5 rows celulas quadradas + nav header), TreeSkeleton (8 linhas indentadas 0..3), ActionFormSkeleton (3 grupos com 2-3 rows botao+texto), GridCollectionSkeleton (filter + 2 grids lg:grid-cols-2), TabsSkeleton (4 chips de aba + Default abaixo), FilterSkeleton (4 chips + apply), DefaultSkeleton. ModelSkeleton composto: pageTabs curto-circuita (Header + Tabs); demais empilham na ordem canonica do legado (form -> filtro -> grid -> calendar -> actionform -> tree -> gridcollection) consultando shape.keys com de-dupe datagrid/datagrid2. Todos os blocks tem role=status aria-busy + aria-live=polite no wrapper.
  - apps/director-studio/src/routes/smoke-f041.tsx: 7 cenarios isolados com fetcher mockado (sem rede), cada um com botao Refresh manual + Reset cache + display de status. (1) cache miss -> boot skeleton; (2) cache fresh primed via writeCache -> render imediato status=ready; (3) cache stale com configureModelCache freshMs:200 -> stale visivel + revalidating; (4) refresh manual ignora freshness; (5) boot-error chooser HTTP 500/404/401 (razao semantica muda); (6) revalidate-error toast warning com action Tentar novamente; (7) network retry com flaky fetcher 2 falhas iniciais -> 3a OK chega via backoff.
  - apps/director-studio/src/routes/tree.tsx: rota /smoke/f041 registrada (createRoute + addChildren).

Arquivos alterados:
  - packages/ui/src/hooks/use-model.ts: refatorado para SWR. Novos campos no UseModelState: status (idle/boot/revalidating/ready/error), fetchStatus (HTTP), revalidateError ({message,httpStatus}), shape (ModelShape|null), refresh (debounced 500ms + AbortController). Cache-first: hit fresh -> setStatus(ready) sem fetch; hit stale -> exibe stale + revalida em background (a menos que shouldSuppressBackgroundRevalidation), miss -> boot. Erro tipado (4xx/5xx do backend) NAO retenta — passa direto pro caller. Network error (TypeError do fetch) RETENTA com backoff 1/2/4s ate 3 vezes. Em revalidacao, erro vai pra revalidateError (toast no engine), mantem stale; em boot, erro vai pra error/fetchError + status=error. options.fetcher injetavel para smoke. options.disableCache para diagnostico. Cleanup geral cancela debounce + abort em unmount. applyInterpolation preservado (F051 inalterado).
  - packages/ui/src/components/model-engine.tsx: ModelEngine consome novo state. Boot -> ModelSkeleton shape. Revalidating -> RevalidatingBar (progressbar 2px overflow-hidden bg-muted, slide span bg-primary/60 motion-safe:animate-[revalidate-slide_1.5s_ease-in-out_infinite,pulse_1.5s_ease-in-out_infinite] — pulse como fallback se keyframe customizado nao existir no tailwind config; sem cores hex). Boot-error -> EngineBootError com WarningCircle size-12/sm:size-16 text-x-error + reason (no-session/404/5xx/network/auth com mensagens em pt-BR) + Button Tentar novamente (CircleNotch motion-safe:animate-spin quando loading); idem EngineBootFetchError. Revalidate-error -> useEffect dispara toast.warning(Nao foi possivel atualizar..., {description, action:{label:Tentar novamente,onClick:refresh}}) com de-dupe por signature httpStatus:message. data-engine-status atributo para hooks de estilo externo. Novas props: onRefreshControlReady(control), disableCache, fetcher. ModelRefreshControl exposto: {refresh, loading, status}. EngineError/EngineFetchError antigos (InlineAlert) mantidos como dead code (futuro: removidos quando feature absorver).
  - packages/ui/src/components/app-header.tsx: novas props onRefresh + refreshStatus (idle/loading/revalidating). Desktop: botao ghost icon ArrowsClockwise / CircleNotch motion-safe:animate-spin entre search e Bell; aria-label muda para Atualizando pagina quando ativo + aria-busy. Mobile: kebab DotsThree entre Bell e trailingSlot com menu absolute popover (bg-popover text-popover-foreground border-border shadow-lg) -> item Atualizar pagina; fecha em click outside via useEffect mousedown. Ctrl/Cmd+R interceptado apenas se e.target.closest(main) (poder-user em campo externo segue tendo F5 nativo); Ctrl/Cmd+Shift+R nunca interceptado. onRefreshRef pattern para fugir do TS narrowing de closure.

Anti-violacao audit:
  - zero console em arquivos novos/alterados (grepado).
  - zero lucide-react (apenas @phosphor-icons/react).
  - zero hex/rgb/hsl/oklch inline (tokens semanticos via Tailwind: bg-muted, bg-card, bg-popover, border-border, text-foreground, text-muted-foreground, text-x-error, bg-primary, primary/60, etc).
  - prefers-reduced-motion respeitado: barras de skeleton usam motion-safe:animate-pulse (estatico bg-muted quando desligado), barra de revalidate usa motion-safe, CircleNotch usa motion-safe:animate-spin.
  - Vaul NAO usado (refresh manual desktop = botao header, mobile = kebab popover absoluto — pull-to-refresh real fica para F060 conforme spec).
  - Polling AUSENTE (toda revalidacao e on-demand ou stale-on-mount).
  - sessionStorage com try/catch silencioso em todas as ops (quota/private mode tolerado).
  - LRU enforcement em writeCache (sort touchedAt asc, remove cap excedente + mirror).
  - mirror NAO persiste se JSON.stringify > 100KB (apenas memoria; F5 cai em boot).

DoD por item da pauta:
  1. SWR cache em packages/ui/src/lib/model-cache.ts: DONE (LRU 20 + mirror 100KB + chave model:appKey:path + janelas configuraveis via configureModelCache).
  2. useModel wrapper SWR: DONE (cache-first; hit fresh -> ready; hit stale -> revalidate; miss -> boot).
  3. AbortController + debounce 500ms refresh manual: DONE (abortRef + debounceRef em useModel).
  4. Skeletons por chave em packages/ui/src/components/model-skeletons.tsx: DONE (grid striped 8 rows, form 6 pares 1col/2col responsive, calendar 7x5, tree 8 linhas indent, action-form 3 grupos, default neutro).
  5. Shape memorization sessionStorage para skeleton no 2o acesso: DONE (persistShape em writeCache; readShape consulta memoria depois sessionStorage; useModel hidrata setShape no mount mesmo sem cache de dados).
  6. Botao refresh app-header desktop ArrowsClockwise + item kebab mobile: DONE; estado atualizando (CircleNotch + aria-busy) distinguivel de boot (skeleton); nunca volta a skeleton no refresh (state.payload mantem o renderer).
  7. Boot-error full-state WarningCircle + retry + revalidate-error toast warning Sonner v2 com action retry: DONE.
  8. Retry automatico network error backoff 1/2/4s max 3: DONE (RETRY_BACKOFFS_MS em performFetch; AbortError nao retenta; erro tipado backend nao retenta).
  9. Smoke route /smoke/f041 com cenarios: DONE (7 cenarios isolados via key + cacheTesting.reset()).
  10. Mobile-first: shouldSuppressBackgroundRevalidation consultado em stale-on-mount; pull-to-refresh stub deixado para F060 conforme spec.
  11. Typecheck verde 3 packages: DONE (turbo typecheck 8.941s, all cache-miss execucao limpa).

PROIBIDO commit — entregando para auditoria (archaeologist confere contrato; ui-tester confere cenarios in vivo).
2026-05-17T16:35:43Z [orchestrator] F041 status=ready-for-test → audit-pending (transição automática /dwave)
2026-05-17T16:37:30Z [archaeologist] audit-pass-with-note: F041 — cache/refresh/skeleton/error-states sao decisao de Studio; contrato engine-schema-driven NAO tem asserção legada sobre cache (legado declara explicitamente "Cache: Nenhum"). Diff verificado: dispatch por presença de chave preservado (RENDERER_ORDER intacta), pageTabs exclusividade preservada (DispatchedRenderers L583), ordem canônica vertical preservada, payload shape (modelJson/modelParams/funcoes) preservado, composição livre preservada, zero eval. Adições UX (skeleton via deriveShape — lê Object.keys(modelJson), nao infere DFtipo) coerentes com dispatch-por-presença. applyInterpolation muta só modelJson sem tocar modelParams/funcoes. 10 divergências conscientes da spec model-loading-cache.md cobertas. PASS.
2026-05-17T16:39:06Z [orchestrator] F041 ready-for-test → deferred (mesmo bloqueio esbuild de F036/F037)

[2026-05-17T16:41:27Z] [smith] status=wip: F042 Cross-app fallback do model — refatorando model.ts para two-step (own app primeiro, processa só se 0 rows) + payload.fellBack flag; smoke /smoke/f042 com fetcher mockado.
[2026-05-17T16:45:05Z] [smith] status=ready-for-test: F042 Cross-app fallback do model. Backend (apps/api/src/routes/model.ts): refatorada Query 1 do fetchModelFromDb para algoritmo two-step substituindo o OR combinado herdado do legado .NET. Etapa 1 `WHERE DFchave_pagina=@pageKey AND DFid_aplicacao IN (SELECT DFid_aplicacao FROM TBaplicacao WHERE DFchave=@scopeKey)` com scopeKey=appKey. Se 0 rows e appKey!='processa', etapa 2 com scopeKey='processa' (suprimida quando caller ja pede 'processa' para evitar query redundante). Caso B (base sem DFid_aplicacao — Area 52): query unica preserva semantica do legado, fellBack permanece null. Novo campo `fellBack: boolean | null` em ModelPayload — false=proprio, true=fallback processa, null=tenancy nao aplicavel. Divergence informativa quando fellBack=true ("F042 cross-app fallback aplicado: model nao encontrado em appKey='X'; servido por DFchave='processa'") permite frontend logar transparente sem UI extra. Cache redis (chave model:<appKey>:<pageKey>) ja contem fellBack — restore preserva o sinal entre requests. Contrato atualizado (mind/atlas/concepts/legacy-contracts/obter-model-pagina.md §Notas) com asserção F042 explícita: two-step sequencial em vez de OR combinado + fellBack flag obrigatorio no payload — divergencia consciente do legado documentada (OR combinado tornava cross-app indistinguivel; Studio e honesto sobre qual app serviu). Smoke route /smoke/f042 (apps/director-studio/src/routes/smoke-f042.tsx, 366 linhas) com 4 cenarios deterministicos via simulateTwoStepFallback puro em memoria (sem rede/banco): A appKey='agendamento' tem 'home-page' proprio -> fellBack=false idModel=10001 1 tentativa; B appKey='agendamento' sem 'about-page' mas existe em processa -> fellBack=true idModel=90001 2 tentativas (own miss + processa hit); C appKey='processa' pede 'global-config' -> fellBack=false idModel=90002 sem etapa 2 redundante (validates suppression branch); D nenhum registro -> page-not-found 2 tentativas ambas miss. Cada card mostra entrada/esperado/tentativas/payload + StatusPill PASS/FAIL com data-pass attribute, summary chip "N/M cenarios passaram" com data-summary. Anti-violacao audit: zero console em arquivos novos/alterados (unico match em grep e palavra dentro de comentario JSDoc em model.ts), zero lucide (apenas @phosphor-icons/react: Check/X/ArrowDown), zero hex/rgb/hsl/oklch inline (tokens semanticos bg-x-success/10, bg-x-error/10, text-x-success, text-x-error, text-x-warning, bg-card, bg-muted, bg-popover, border-border, text-foreground, text-muted-foreground), Vaul nao usado (smoke e puro card layout sem overlay), polling ausente (simulador sincrono), useIsMobile nao necessario (cards stack naturalmente via grid-cols-1 sm:grid-cols-2). Tree.tsx registrado em path /smoke/f042. Typecheck verde nos 3 packages (@workspace/{api,ui,director-studio}) — turbo typecheck 2.083s com 1/3 cache hit. PROIBIDO commit — entregando para auditoria do archaeologist (verifica que two-step + fellBack honra a asserção adicionada no contrato §Notas) e validação do ui-tester nos 4 cenarios de /smoke/f042 (todos data-pass=true esperado, summary 4/4 verde).
2026-05-17T16:46:30Z [orchestrator] F042 status=ready-for-test → audit-pending (transição automática /dwave)
2026-05-17T16:49:25Z [archaeologist] audit-pass-with-note: F042 — two-step fallback OK; nota: contrato texto cita coluna DFchave_aplicacao que não existe (correto é DFid_aplicacao com resolução via TBaplicacao); código backend correto; recomenda levantamento duplicatas pré-cutover
2026-05-17T17:05:00Z [ui-tester] F042 blocked: SQL 172.27.0.121\SQL2k19 inacessivel (timeout 8s) e nenhum VPN client Processa rodando (nordvpn-service ativo, forti/openvpn/wireguard/anyconnect/globalprotect ausentes). Sanity OK (401 sem cookie, login ldap-bridge processa\guga emite director_session). 4 cenarios A/B/C/D todos retornam {ok:false,error:fetch-failed,...} HTTP 500 — nao exercitam two-step fallback. Endpoint shape divergente do contrato legado (GET ?app=&path= em vez de POST body {caminho,chaveAplicacao}) — decisao consciente do Studio, documentar. Tested=— blocked-por-rede. Relatorio: test-reports/F042-cross-app-fallback-model.md. Reabrir com VPN ou staging com tunnel pre-estabelecido.
[ui-tester] F042 fail: retry com VPN ativa (172.27.0.121 ping OK, SQL2k19 alcancavel). 1/4 cenarios DoD ok (apenas D path-inexistente=404). A/B/C falham: handler retorna fellBack:null em vez de false/true/false; appKey ignorado no lookup (app=cotacao&path=/acessos/usuarios serve idModel:14 do portal-director); auto-declara divergence "TBmodel_pagina sem DFid_aplicacao nesta base — tenancy por app não aplicada (F042)". Causa-raiz: seed F043 nao tem coluna DFid_aplicacao em TBmodel_pagina, pre-requisito do contrato. Curator: decidir schema migration + backfill ou reescrita do contrato.
2026-05-17T17:07:30Z [orchestrator] F042 needs-rework → deferred; F088 enfileirado (decisão curator: migration schema vs reescrita contrato vs cross-tenant survey)

[2026-05-17T17:30:00Z] [smith] status=wip: F044 Sort DataGrid case-sensitivity — normalizar ordenacao no backend (grid.ts) com map prop->sqlColumn, fallback heuristico (capitalize + DF<prop> prefix) e retry on Invalid-column-name; probe script apps/api/src/scripts/probe-f044.ts.

[2026-05-17T18:05:00Z] [smith] status=ready-for-test: F044 Sort DataGrid case-sensitivity.

Backend (apps/api/src/routes/grid.ts):
  - Novas funcoes: parseOrdenacao, buildSortVariants, extractInvalidColumnFromError, runProcWithSortRetry, isSafeSortToken, isSafeSortDir.
  - Pipeline: frontend manda body.ordenacao="<prop>,<dir>" + opcional body.__sortColumnMap={<prop>:<sqlCol>}. parseOrdenacao escolhe entre: (1) override do map (1 tentativa unica), (2) heuristica de variantes prop->CapitalizeFirst(prop)->DF<prop>->DF<CapitalizeFirst(prop)>. runProcWithSortRetry tenta sequencialmente; so retenta se erro for "Invalid column name '<X>'" onde X case-insensitive == variante tentada (anti-loop sobre outros erros). Esgotou variantes -> 500 com mensagem explicita pedindo header.sortKey ou correcao da proc.
  - Anti-injecao: regex [A-Za-z_][A-Za-z0-9_]{0,63} em todo token de sort (variante e map). Direcao restrita a asc/desc/''.
  - __sortColumnMap removido do procBody antes do toXml (nao vaza pra proc como tag XML).
  - Mesma normalizacao em /api/grid/query e /api/grid/export (CSV honra sort tambem).
  - Resposta /query ganha campo sortResolution: {originalProp, resolvedColumn, dir, attempts:[], wasOverride}.
  - Pino logger.info ao descer variante ("F044 sort variant rejected, trying next") — nada de console.

Frontend (packages/ui/src/components/data-grid-renderer.tsx):
  - GridHeader.sortKey?: string novo (opt-in Studio para declarar nome SQL real).
  - sortColumnMap memoizado: so inclui pares onde sortKey !== prop.
  - Body de fetch e CSV inclui __sortColumnMap quando o model declara sortKey em algum header.
  - UI sort state (sortCol/sortDir) inalterado — continua usando header.prop como identificador.

Script (apps/api/src/scripts/probe-f044.ts):
  - 5 cenarios contra sp_Listar_GrupoTrabalho via /api/grid/query:
    1. sort by descricao (heuristica),
    2. sort by id (heuristica, casa critico porque alias=Id mas fonte=DFid_grupo_trabalho),
    3. sort by ativo_inativo (testa underscore na heuristica),
    4. sort by descricao com sortKey=Descricao (override explicito),
    5. no sort (sanity ,,).
  - Login automatico via $PROBE_USER/$PROBE_PASS ou cookie pre-existente em $DIRECTOR_SESSION_COOKIE.
  - Saida tabular STDOUT com PASS/FAIL + sortResolution.attempts por cenario.
  - Exit code 0 se todos PASS, 1 se algum FAIL, 2 se erro fatal (sem cookie/login).
  - Uso: tsx apps/api/src/scripts/probe-f044.ts

Contrato (mind/atlas/concepts/legacy-contracts/model-valor-datagrid.md):
  - §Request (`ordenacao`) anotado com asserção F044: normalizacao server-side + heuristica + override via __sortColumnMap derivado de header.sortKey. Divergencia consciente do legado documentada.
  - §Header ganha linha `sortKey` marcada Studio-only (nao existe no legado), com semantica e exemplos.

Survey legacy SQL (sources/engenharia--fabrica--sql--portal-director):
  - sp_Listar_GrupoTrabalho NAO esta no corpus (vive so na Area 52 instalada). Corpus revela 3 padroes convivendo nas procs _datagrid/_consultar_/_obter_: (1) CASE WHEN com literal lowercase (dominante em solicitacao.rebaixa, processa.appbuilder.mobile — funciona out-of-box com prop lowercase do frontend); (2) CASE WHEN com literal camelCase (codPedido, codigoempresa, ncupom — exige header.prop exato); (3) dynamic SQL (inferido pelo erro de runtime — exige nome real da coluna SQL como Descricao/DFdescricao). Estrategia F044 cobre (1) e (3); cobre (2) quando o autor do model declara prop=camelCase como ja faz hoje.

Anti-violacao audit:
  - Zero console em arquivos novos/alterados (probe usa process.stdout/stderr.write; runProcWithSortRetry usa logger.info).
  - Zero lucide.
  - Zero hex/rgb/hsl/oklch (apenas backend + tipo TS — sem CSS tocado).
  - Sem polling.
  - Sem mutacao de model em tempo de execucao (sortColumnMap memoizado puro de config.headers).
  - Anti-injecao em todo token concatenado.

Typecheck verde 3 packages: turbo typecheck 9.783s (3/3 cache-miss execucao limpa).

DoD coverage:
  1. Investigacao das procs sp_Listar_*: feita via corpus portal-director SQL (3 padroes catalogados em decisions); sp_Listar_GrupoTrabalho especifica nao acessivel — assumption documentada (caso 3, dynamic SQL).
  2. Estrategia (a) escolhida: normalizacao server-side em grid.ts. Map vem de header.sortKey (campo novo Studio-only) — DFchave nao existe no schema legado, divergencia documentada.
  3. Probe script apps/api/src/scripts/probe-f044.ts: feito (5 cenarios > 3 solicitados).
  4. Map de normalizacao documentado: comentario em grid.ts §F044 + tabela em backlog/F044-decisions.md + asserção no contrato.
  5. Typecheck verde 3 packages: verificado.

PROIBIDO commit — entregando para auditoria (archaeologist confere asserção no contrato; ui-tester roda probe-f044.ts na Area 52 com VPN ativa).
2026-05-17T17:18:23Z [orchestrator] F044 ready-for-test → audit-pending (transição /dwave)
2026-05-17T17:21:22Z [archaeologist] audit-pass-with-note: F044 — anotações sortKey/__sortColumnMap factualmente válidas; nota gap citacional: anexar referência a sp_portal_director_consultar_dados_pedido_agendamento.sql:62-78 (CASE WHEN @ordenacao_coluna='codPedido') como evidência da proc casa literal CamelCase
2026-05-17T17:35:00Z [ui-tester] F044 fail: probe-design (2/5 cenarios assumem hipoteses falsas) — case-2 espera heuristica resolver 'id' mas coluna real eh DFid_grupo_trabalho (sufixo fora do cap de 4 variantes, §95 documenta); case-4 declara sortKey=Descricao mas coluna real eh DFdescricao (override single-attempt §42 falha loud por design). Implementacao F044 esta correta: curls manuais com sortKey correto (DFid_grupo_trabalho, DFdescricao) retornam wasOverride=true, attempts single, rows=4 ordenadas, HTTP 200. Smith deve corrigir probe-f044.ts (cenarios 2,4) + 2 defeitos pre-existentes (SESSION_COOKIE_NAME default 'director_studio_session' != real 'director_session'; login POST com login/senha mas API espera identity/password). Sem mudanca em grid.ts. Relatorio: mind/effort/on/director-studio/test-reports/F044-sort-normalization.md
2026-05-17T17:45:00Z [curator] F044 accepted — criterio triplo OK: (A) tecnico: archaeologist audit-pass-with-note + contrato model-valor-datagrid.md anotado §Request+§Header sortKey Studio-only; (B) MISSION: destrava UX real do DataGrid em producao (fecha bloqueio F011 C4); (C) cobertura caso real: probe Area 52 sp_Listar_GrupoTrabalho retornou linhas reais (SECO/CONGELADOS01-02/PERECIVEIS) ordenadas via heuristica (descricao→DFdescricao, ativo_inativo→DFativo_inativo) e via override (__sortColumnMap com DFid_grupo_trabalho e DFdescricao → wasOverride=true). Falhas probe-design dos cenarios 2/4 sao spec-correct (§95 cap 4 variantes; §42 override sem retry) e nao bloqueiam aceite. Manifest Accepted=✓ 2026-05-17. F089 enfileirada (probe fix: cenarios 2/4 + SESSION_COOKIE_NAME + login payload identity/password).
2026-05-17T00:00:00Z archaeologist feature-added: F045 contract-published atlas/concepts/legacy-contracts/grid-action-delete.md (sub-contrato de model-valor-datagrid; v1 canonico em DataGrid/DataGrid.js:83-494, v2 stub em DataGrid2/Header/Actions.js:22-25; D1..D17; note: ambiguous-source sendSelectRows sem call-site real indexado)

[2026-05-17T17:39:00Z] [smith] status=wip: F045 gridAction delete — implementacao do fluxo canonico no DataGridRenderer. Modal confirmacao kind=confirm tone=destructive (texto literal legado "Tem certeza que deseja realizar a exclusao?"); payload por discriminante sendSelectRows ({ids:number[]} vs {rows:Row[]}); endpoint dual (execProc||api) com prefixo /proc/ quando !isNotProcedure; 5 ramos de resposta (200/sucesso, 200/!sucesso, 500/FK 547/REFERENCE constraint, 500/genrico, outros); refresh apos 700ms via refetch; selection clear pos-sucesso (divergencia consciente vs bug legado). Substitui stub InlineAlert de F011. Smoke route /smoke/f045.
[2026-05-17T17:48:00Z] [smith] status=ready-for-test: F045 gridAction delete. packages/ui/src/components/data-grid-renderer.tsx: (1) GridAction estendido com `api` (fallback de execProc) e `sendSelectRows` (discriminante body); (2) helper extractRespostaEnvelope() parseia <Resposta><Status/><Sucesso/><Dados/></Resposta> tanto de XML cru em recordset[0].col-anonima quanto de objeto plano com campos Status/Sucesso/Dados; (3) handler `case 'delete'` substitui stub F011 — valida selection >=1, abre Modal kind=confirm tone=destructive (Modal facade ja gateia mobile=Drawer/desktop=Dialog via useIsMobile internamente); (4) normalizeDeleteUrl honra contrato D14/D15/D16 — `execProc||api`, prefixa /proc/ quando !includes('/proc/') && !isNotProcedure; (5) executeDelete monta body por discriminante D6/D7 (`{ids:number[]}` vs `{rows:Row[]}`), POST /api/forms-proxy, mapeia 5 ramos de resposta D9..D12 via useToast (success/warning/error com defaults de duracao); (6) FK match estendido para SQL Server 547 + pt-BR (`restricao reference`) + en (`reference constraint`) — divergencia consciente vs legado en-only; (7) selection.clear() imediato pos-sucesso + setTimeout(700ms){setPage(1);fetchRows()} — corrige bug `selected[] orfao` do legado §"refresh pos-sucesso". Modal corpo: literal "Tem certeza que deseja realizar a exclusao?" + enriquecimento "N registros serao excluidos." em description (divergencia consciente F045 DoD §2). apps/director-studio/src/routes/smoke-f045.tsx: 5 cenarios (success, business-reject 200+sucesso:false, fk-547, generic-error, send-rows inspecionar body), intercepta window.fetch para /api/forms-proxy e /api/grid/query (sem chamar backend); exibe ultima call payload em <pre> para asserir endPoint preservado + body discriminante. tree.tsx registra /smoke/f045. Typecheck verde 3 packages (8.098s). Anti-violacao: zero lucide, zero hex inline, zero console.* no smoke; renderer mantem console.error so para captura de erro server-side (paridade legado). PROIBIDO commit — entregando para auditoria.
2026-05-17T17:44:29Z [orchestrator] F045 status=todo → audit-pending (smith não atualizou manifest)
2026-05-17T17:46:24Z [archaeologist] audit-pass-with-note: F045 — D1..D17 respeitados; notas (não-bloqueantes): D6 ids:number[] (não CSV) exige contraparte XML em /api/forms-proxy; D2 toast guard além do disabled; D11 texto literal alterado além do tone-change declarado; D5 wrapper via /api/forms-proxy
